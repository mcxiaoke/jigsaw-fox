# CHANGES 2026-09-19

## 2026-09-19 09:42 彻底清理历史自制 sample 拼图遗留与取图错误提示优化

- **自制拼图历史 sample 数据物理清理**：
  - `lib/data/game_repository.dart`：在 `_initCustomPuzzles()` 启动扫描时，对历史遗留的 `sample_` 关联键值进行物理级联清理（同步清理 collections 存储、ProgressStore 关卡进度记录与 SnapshotStore 快照缓存），防止已有设备仍读出已弃用的 sample 示例；
  - `lib/pages/tabs/my_center_tab_view.dart`：自制卡片装配循环增加防御过滤与异步清理，确保 UI 彻底不显示任何 sample 历史卡片。
- **优化取图失败 Toast 提示**：
  - `lib/pages/tabs/my_center_tab_view.dart`：点击卡片加载图片失败时，智能识别网络与本地素材。仅对 http/https 网络图提示“图片未就绪，请联网后重试”；对本地自制或失效资产精准提示“图片未找到，该记录暂无法游玩”，修复误导玩家为网络故障的文案缺陷。

## 2026-09-19 09:23 代码审查专项系统性修复与安全加固

按照近期 Code Review 审计报告，在严格保持架构稳定与向后兼容的前提下，分步完成了关键缺陷修复、体验细节优化及内置 sample 素材彻底下线：

1. **彻底下线 sample 内置素材与解耦（Step 1）**：
   - `lib/logic/image_source.dart`：清空 `assetSamples` 列表并置为弃用，杜绝运行时对 sample 静态资产的依赖；
   - `lib/data/game_repository.dart`：`_initCustomPuzzles()` 移除 `sample_01/02/03` 的自动植入逻辑，新安装与重置后首启直接标记 `kKeyPresetsInitialized=true`；`_initLevels()` 与首通兜底彻底移除对 sample 的依赖；
   - `lib/widgets/app_cached_image.dart`：移除 `sample_01.jpg` 的错误回退，改为优雅渲染无图占位符，并在图片 Provider 处以 1x1 内存透明图安全兜底，避免触发 asset 404；
   - 测试用例同步更新：调整 `my_center_tabs_ui_test.dart`、`game_collections_migration_test.dart` 与 `game_state_migration_test.dart`，对齐无内置 sample 的全新安装/重置预期。

2. **核心业务健壮性与竞态加固（Step 2）**：
   - `lib/pages/event_levels_page.dart`（D-4）：补齐活动页断点续玩与难度选择链路（`ResumeHelper.tryHandleResumeFlow`、`SnapshotStore.loadJsonString`），向 `GamePage` 传 `initialSnapshotJson`，修复此前点击已在玩活动关卡强行新开局丢档的问题；
   - `lib/widgets/game_toast.dart`（D-5）：在 `show()` 和 `_ToastOverlayState` 中增加 `identical(_currentEntry, entry)` 单例比对保护，并将退场延迟与 `math.max` 下限 clamp，捕获 `TickerCanceled` 异常，彻底杜绝连续弹出 toast 时旧实例退场回调误杀新 toast 的闪退隐患；
   - `lib/pages/game_page.dart`（Review 2.3）：`_onHintPressed` 顶部增加 `if (_game == null || _isSolved || _isPaused) return;` 防御守卫，并在游戏未就绪或已通关时禁用 AppBar 提示按钮，杜绝快速点击提示引发空指针；
   - `lib/logic/content/network/content_http_client.dart`（Review 5.6）：下载落盘流程改为带 `.bak` 备份回滚保护的原子替换，消除先删旧图再 rename 导致网络异常时新旧两空的隐患。

3. **数据口径校准与状态流转一致性（Step 3）**：
   - `lib/services/achievement_service.dart`（Review 4.3）：`claimReward` 增加 `_claimingIds` 并发锁保护，成就发放金币增加 `bypassCap: true` 保证奖励全额发放，调换执行顺序为先落盘加币再标记 `markClaimed`，杜绝丢币；
   - `lib/pages/achievements_page.dart`：为领取按钮增加 `isClaiming` 禁用与加载态，防止玩家因连续点击触发多次并发调用；
   - `lib/game/jigsaw_puzzle_game.dart` & `lib/pages/game_page.dart`（D-6 & D-7）：
     - 引擎侧新增 `onMoveMade` 独立回调并在拖拽落位/提示吸附时触发；`GamePage` 实时维护真实有效操作步数 `_movesCount`，修复通关结算 `recordDifficultyCompletion` 误将已拼碎片数当做步数上报的历史缺陷；
     - `didChangeAppLifecycleState` 纳入 `AppLifecycleState.inactive`，在 Windows 窗口失焦时立即暂停游戏计时器，切回窗口 `resumed` 时恢复，杜绝玩家切窗摸鱼导致用时虚高并惩罚评星；
   - `lib/pages/pack_levels_page.dart`（D-8）：卡片渲染统一从 `ProgressStore.instance.getLevelProgress(level.id).isCompleted` 查询实时完成态，并在 `initState`/`dispose` 监听 `progressNotifier`，解决图包关卡通关后绿色对勾永不出现的缺陷；
   - `lib/logic/content/pipelines/daily_content_pipeline.dart`（Review 5.10）：解压每月每日图片 ZIP 包后增加 `extracted == 0` 空文件校验，解压失败时安全清理临时目录并回退，防止空目录原子覆盖正式目录。

4. **工具链安全与 UI 体验防御（Step 4）**：
   - `lib/update/update_installer.dart`（Review 4.6）：移除候选路径中的本机绝对路径硬编码，防止本地开发目录泄露；
   - `scripts/publish/gitee_publish.py`（Review 5.16）：将鉴权脚本改为安全临时文件，并注册 `atexit` 与退出清理，杜绝在磁盘留下包含敏感令牌的明文脚本；
   - `lib/widgets/continue_dialog.dart`（U-2）：去除 `$progressPercent%` 旁边冗余重复的 `$progressPercent/100` 文本，并将弹窗固定宽 360 调整为 `maxWidth: 360` 自适应弹性约束；
   - `lib/pages/crop_puzzle_page.dart`（U-3）：图片解析失败（`_decodeFailed`）时展示包含错误图标、提示文案和“返回重新选图”按钮的错误面板，替代原先无休止的转圈等待；
   - `lib/widgets/choose_difficulty_sheet.dart`（U-4）：图片解码尺寸失败时置位 `_decodeFailed` 状态并禁用“开始拼图”按钮，预览图增加 `errorBuilder` 优雅占位，杜绝损坏图片白屏或点击崩溃。
