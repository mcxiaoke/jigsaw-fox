# 变更记录 2026-08-25

## [2026-08-25 19:18 GMT+8] 彻底重塑桌面散落模式体验：禁用底部停放与缩放逻辑（原地停放锁定原尺寸）、升级大开阔全域散布算法

### 变更内容
1. **桌面散落模式下彻底禁用底部托盘收纳与缩放跳变 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - **隐藏托盘背景**：桌面散落模式（`isTabletop == true`）下不渲染 `TrayBackgroundComponent`；
   - **彻底禁用尺寸跳变**：在 `updateHoldingPiecePosition` 中全程锁定当前棋盘 `_zoom`（1.0），无论鼠标拖拽/吸附到屏幕哪个角落，绝对不再发生根据 Y 坐标缩小为 0.35 的跳变；
   - **原地停放与禁用自动飞回**：在 `handlePieceDragEnd` 与 `cancelPieceDrag` 中彻底禁用托盘自动收纳与 `_realignTrayPieces`，玩家在桌面任意位置拖动未吸附的碎片，松手后**100% 停留在松手当下的原地坐标**，绝不再飞回托盘左侧开头；
   - **一键整理盘面适配**：桌面散落模式下点击「一键整理」，自动调用全域开阔散布算法将未归位碎片整齐重新铺满四周桌面。
2. **大开阔全域散布算法升级 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - **步长开阔化**：步长扩大至 $1.18 \cdot \text{pieceSize}$，确保碎片之间拥有舒适物理间隔，彻底杜绝拥挤重叠；
   - **外围四角优先散布**：扫描全屏候选点并按 8 扇区归类后，按距棋盘中心的距离**从远到近排序（优先占满最外围边界与四角）**，彻底消除“碎片围绕棋盘外框挤在一堆”的痛点，视野极其开阔大气。
3. **自动化测试与静态检查 (`test/game_layout_test.dart`)**：
   - 全量 58 项单元测试 100% 全部通过，`flutter analyze` 0 警告 0 错误。



### 变更内容
1. **重构桌面打散算法为【8 扇区环形全域发散系统】(`lib/game/jigsaw_puzzle_game.dart`)**：
   - **问题修复**：彻底解决原先桌面模式下碎片堆叠在左右单一竖直线上、上下空置的问题；
   - **8 扇区全方位环绕**：将屏幕除中央棋盘安全区域外的所有可用空间划分为 8 个方位扇区（左上、上、右上、右、右下、下、左下、左）；
   - **轮流交替分发 (Round-Robin Interleaving)**：采用四向交替轮流注入槽位机制，保证碎片在上下左右 360° 均匀分散；
   - **错落多行多列与自然微扰动 (Natural Jitter)**：采用 $0.78 \cdot \text{pieceSize}$ 步长形成丰富的多行多列层次，并加入基于关卡种子的 $\pm 10\%$ 自然微随机抖动，再现实体拼图散落桌面的美感；
   - **棋盘工作区完美留白**：优化宽屏桌面模式下棋盘居中比例（0.56 黄金比例），给四周留出充裕的环形操作桌面，中央拼图区域严格保持 100% 纯净留白。
2. **自动化测试与静态检查 (`test/game_layout_test.dart`)**：
   - 更新单元测试，严格断言碎片在上下左右 4 个象限均有分布、多列多行且绝不遮挡中央棋盘；
   - 全量 58 项单元测试 100% 全部通过，`flutter analyze` 0 警告 0 错误。



### 变更内容
1. **主装配体识别与“定海神针”单向吸附 (`lib/logic/engine/puzzle_engine.dart`)**：
   - **主装配体判定机制 (`isInMainAssembly`)**：当两个集群发生拼合吸附时，系统智能识别大集群（主装配体）与小集群；
   - **单向平移对齐**：永远由小集群/单块碎片向大集群平移对齐，大集群坐标绝对静止，彻底消除半成品拼图被小碎片拉跑移位的违和感。
2. **边缘外框闭环自动感知与平滑复原 (`lib/logic/models/puzzle_state.dart`, `lib/game/jigsaw_puzzle_game.dart`)**：
   - 在 `PuzzleBoardState` 增加 `isEdgeComplete` 闭环计算属性；
   - 玩家开启「仅看边缘」筛选模式时，当所有外围边框碎片全部拼完归位，引擎自动感知闭环并解除筛选，平滑淡入复原所有内部碎片，无需手动点击关闭。
3. **桌面环形打散模式与自适应独立开关 (`lib/data/game_repository.dart`, `lib/pages/settings_page.dart`, `lib/game/jigsaw_puzzle_game.dart`, `lib/pages/game_page.dart`)**：
   - **独立设置项与持久化**：在 `SettingsPage` 和 `GameRepository` 中新增「碎片初始排布模式」切换（`底部托盘收纳` vs `桌面环形散落`）；
   - **桌面环形散落算法 (`_getScatterPositionForIndex`)**：在宽屏/平板/桌面端开启时，碎片围绕棋盘外围散开排布，中央拼图核心工作区保持纯净留白；空间不足时自动优雅回退为托盘收纳，完美适配移动端窄屏。
4. **“失踪碎片”后台自检与防丢自愈 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - 当剩余未拼碎片 $\le 2$ 块时，若检测到碎片因平移/缩放被甩出可视视口之外，后台自检自动将其平滑弹回屏幕可视区域，彻底杜绝“找不到最后一块碎片”的挫败痛点。
5. **自动化测试与静态检查 (`test/game_layout_test.dart`, `test/new_features_test.dart`)**：
   - 新增主装配体单向吸附静止、边缘闭环解除筛选、桌面打散留白、失踪碎片自愈弹回等专项测试；
   - 全量 58 项测试 100% 全部通过，`flutter analyze` 0 警告 0 错误。



### 变更内容
1. **解决鼠标点击无反应问题 (`lib/game/puzzle_piece_component.dart`)**：
   - **混入 `TapCallbacks`**：不仅依赖 `DragCallbacks`，同时混入 `TapCallbacks` 并在 `onTapDown` 中直接拦截响应；
   - 彻底解决 Windows 鼠标短促轻点（PointerDown 到 PointerUp 耗时 $< 50\text{ms}$ 且位移 0）时手势竞技场忽略 DragStart 的缺陷，实现轻点 100% 灵敏秒响应。
2. **解决碎片缩放变大变小后光标与碎片脱节拉大距离的缺陷 (`lib/game/jigsaw_puzzle_game.dart`, `lib/game/puzzle_piece_component.dart`)**：
   - **建立【归一化抓取锚点模型 (Normalized Grab Anchor Model)】**：
     - 点击拾起碎片时，计算光标在碎片逻辑尺寸中的归一化比例 $anchorX = \frac{x - pos.x}{size.x \cdot scale.x},\; anchorY = \frac{y - pos.y}{size.y \cdot scale.y}$；
     - 碎片在托盘与棋盘之间平滑插值缩放（从 $0.35$ 到 $1.0$ 或 $3.0$ Zoom）时，碎片的左上角位置实时由 $pos = cursor - anchor \cdot size \cdot scale_{\text{current}}$ 严格计算；
     - **视觉效果**：无论碎片怎么放大或缩小，鼠标光标永远牢牢锁定在最初点击的那个相对纹理点上，彻底杜绝距离漂移与拉大！
3. **Flutter 顶层与 Flame 画布高频坐标同步 (`lib/pages/game_page.dart`)**：
   - 在 `Listener` 挂载 `onPointerHover` 与 `onPointerMove`，统一使用排除 AppBar 等外层偏移的局部像素坐标（`event.localPosition`）；
   - 在鼠标松开移动（Hover）与按住移动（Move）全生命周期中高频驱动 `updateHoldingPiecePosition`，实现极致平滑丝滑的跟手体验。
4. **自动化测试套件更新 (`test/game_layout_test.dart`)**：
   - 编写托盘内小尺寸与棋盘放大后光标锚点重合精度的专项数学断言测试；
   - 全量 54 项测试用例全部通过，`flutter analyze` 0 警告 0 错误。

## [2026-08-25 17:55 GMT+8] 拼图交互全面升级：Windows 鼠标单击吸附抓取/光标跟随 & 归位碎片底层锁定与优先级层级管控

### 变更内容
1. **Windows 鼠标单击吸附抓取与光标跟随 (`lib/game/jigsaw_puzzle_game.dart`, `lib/game/puzzle_piece_component.dart`)**：
   - **双模手势兼容**：在 `PuzzlePieceComponent` 中智能区分短按单击与长按拖拽：
     - 若为快速单击（位移 $< 8\text{px}$ 且时间 $< 350\text{ms}$）：进入 **光标吸附跟随模式**（`holdingPiece`），鼠标无需长按即可带动碎片（及其已拼接集群）丝滑跟随移动；
     - 若为长按拖拽（位移 $\ge 8\text{px}$）：松手直接放置，与原有触屏/拖动手感 100% 兼容；
   - **再次单击放置**：混入 `TapCallbacks`，光标吸附状态下在棋盘或托盘任意位置再次单击，立即释放并触发吸附或收纳；
   - **取消抓取保护**：支持右键单击（`kSecondaryMouseButton`）与键盘 `ESC` 键平滑取消抓取，碎片原位平滑飞回。
2. **吸附归位碎片底层锁定与严密层级优先级 (`lib/game/jigsaw_puzzle_game.dart`, `lib/game/puzzle_piece_component.dart`)**：
   - **四阶层级优先级 (Priority Hierarchy)**：
     - `_activeDragBasePriority = 1000+`：当前拖拽中/光标抓取的碎片集群（绝对顶层）；
     - `_boardUnsolvedPriority = 20`：散落在棋盘上的未归位碎片（浮于已归位底板碎片上方）；
     - `_trayPiecePriority = 10`：托盘待选碎片；
     - `_solvedPiecePriority = 5`：已吸附归位的正确碎片（锁定底层，绝不遮挡任何浮动碎片）；
   - **归位锁定 (Locking)**：已吸附归位的碎片标记 `isLocked = true`，禁用一切拖拽与拾取手势，彻底杜绝误触与移位；
   - **命中测试天然上层优先**：Flame 的 Hit-testing 遵循 Priority 倒序遍历，当未归位碎片与底层已归位碎片重叠时，光标/触摸点击 100% 优先拾取上层的未归位碎片。
3. **自动化测试与静态检查 (`test/game_layout_test.dart`)**：
   - 新增已归位碎片 Priority=5 锁定拦截、未归位碎片 Priority=20、单击吸附抓取/移动/放置、取消抓取复位等全套单元测试；
   - 全量 54 项测试用例全部通过，`flutter analyze` 0 警告 0 错误。

## [2026-08-25 17:10 GMT+8] 全面接入 assets/bg 10 款无缝平铺桌板背景 & 新增通用无缝平铺处理与 SSIM/TQI 质检工具

### 变更内容
1. **静态资源注册与数据仓库重构 (`pubspec.yaml`, `lib/data/game_repository.dart`)**：
   - 在 `pubspec.yaml` 中注册了 `- assets/bg/` 资源目录；
   - 将 `GameRepository.kBackgroundAssets` 全面重构为 10 款 `assets/bg/tile_000.webp` ~ `assets/bg/tile_009.webp`（实木、纸质、毛毡、亚麻等无缝材质，单张仅 2KB ~ 64KB）；
   - 默认背景初始化为第一款无缝桌板 `assets/bg/tile_000.webp`。
2. **对局背景渲染与选择器无缝平铺改造 (`lib/pages/game_page.dart`, `lib/widgets/choose_background_sheet.dart`)**：
   - **`GamePage`**：全屏底层壁纸由原本的单张强行拉伸（`BoxFit.cover`）升级为 **`ImageRepeat.repeat` 无缝平铺模式**，完美支持手机竖屏、Windows 窗口任意缩放及 Web 4K 巨屏，永远 1:1 像素级清晰且零拉伸模糊；
   - **`ChooseBackgroundSheet`**：弹窗中的壁纸预览卡片长宽比调整为 `1.0`（正方形），展示各款材质的自然平铺效果，标签更新为「桌板 1」~「桌板 10」。
3. **新增通用无缝平铺处理与客观评测工具 (`scripts/make_seamless_tiles.py`)**：
   - 支持对任意正方形材质进行【高通去暗角光照均衡 + 环形余弦羽化融合】一键无缝化处理；
   - 引入国际公认 IEEE SSIM（结构相似性）与 TQI 质检模型，自动生成 4×6 拼接大图预览与总览画板。
4. **自动化测试套件同步 (`test/widget_test.dart`)**：
   - 同步更新背景数量、首尾路径与选择器点击测试断言；
   - 全量 50 项测试 100% 全部通过，`flutter analyze` 0 警告 0 报错。

### 变更内容
1. **顶部卡片扁平紧凑化 (`lib/pages/tabs/my_puzzles_tab_view.dart`)**：
   - 将原先占据较大垂直空间的 2 个大 Card（高 98px）重构为 **3 个扁平紧凑的并排小 Card**（高 66px）：
     - **「相册选图」**：绿色调，点击直接唤起系统本地相册选图与拼图制作；
     - **「已下载」**：橙色调，通过 `ValueListenableBuilder` 实时联动 `DownloadManager` 缓存张数，点击滑出已下载图片管理抽屉；
     - **「在线搜图」**：蓝色调，点击进入全屏 Pixabay / Unsplash / Pexels 免版权图库浏览器；
   - 采用精致轻量级横向排列（图标 + 加粗主标题 + 辅助副标题），在移动端与宽屏端均具有极佳的视觉紧凑感与触控体验。
2. **精简自制合辑标题栏 (`lib/pages/tabs/my_puzzles_tab_view.dart`)**：
   - 移除标题右侧多余的「已下载图片」文字按钮（已提升至顶部主卡片）；
   - 标题栏仅保留「我的自制合辑」与关卡总数展示，视觉更加简洁有序。
3. **测试用例同步升级 (`test/new_features_test.dart`)**：
   - 更新 `MyPuzzlesTabView` 组件测试，验证「相册选图」、「已下载」、「在线搜图」正常渲染，并验证旧文字按钮已彻底移除；
   - 全部 50 项测试 100% 通过，`flutter analyze` 0 警告 0 错误。

## [2026-08-25 15:32 GMT+8] 修复拼图界面横屏状态条遮挡棋盘 & 托盘碎片未打乱两大问题

### 变更内容
1. **修复横屏时顶部计时/进度状态条遮挡拼图区域 (`lib/pages/game_page.dart`)**：
   - 原实现中 Flame 游戏画布以 `Positioned.fill` 铺满全屏，而引擎内部仅预留固定 `_topToolbarHeight = 52.0` 的顶部空间，实际 Flutter 头部（AppBar 48 + 状态子栏 + 进度条）约 100px，导致横屏下棋盘被压在头部之下；
   - 重构为 `Column(头部 + Expanded(GameWidget))` 结构：头部（导航栏、计时/进度工具栏、细进度条）与游戏画布纵向排布、互不重叠，任意方向均不再遮挡；
   - 头部三栏提取为独立方法 `_buildHeader()`，逻辑不变仅调整挂载位置；缩放悬浮徽章与通关横幅保持浮层不动。
2. **修复托盘碎片顺序未打乱、可直接按序拼成原图 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - 根因：初始化按 `id = r*cols+c` 顺序将碎片依次放入托盘，且 `_realignTrayPieces` 也按 id 插入顺序重排，导致托盘排列与原图完全一致；
   - 新增 `_trayOrder` 字段：`onLoad` 时用 `Random(seed)` 对全部碎片 id 洗牌后决定托盘从左到右的摆放顺序；
   - `resetCurrentGame()` 重开本局时用新的随机种子重新洗牌，保证每次重置排列都不同；
   - `_realignTrayPieces()` / 托盘滚动等重排逻辑改为严格遵循 `_trayOrder` 顺序。
3. **Flame 内部顶部预留高度** `_topToolbarHeight` 由 52.0 调整为 8.0（画布已位于头部下方，仅需保留少量上边距）。

### 验证
- `flutter analyze`：无问题；
- `flutter test`：50 项测试全部通过（托盘初始顺序输出确认已打散）。

## [2026-08-25 15:23 GMT+8] 精简主页 AppBar、合并 Star 星星统计至全屏成就页，并将设置与玩法帮助重构为独立全屏页面

### 变更内容
1. **主页 AppBar 极致精简 (`lib/pages/main_screen.dart`)**：
   - 移除了 AppBar 右侧的 Stars 星星统计胶囊与问号帮助按钮；
   - AppBar 仅保留「🏆 成就与统计」和「⚙️ 设置」两个精美操作图标，界面更加纯粹清爽；
   - 点击成就或设置时，均通过标准全屏路由（`Navigator.push`）进入，返回时自动刷新主页状态。
2. **全屏成就与统计中心 (`lib/pages/achievements_page.dart`)**：
   - 从原先局促的 Dialog 浮层重构为独立全屏页面 `AchievementsPage`；
   - **整合 Stars 星星数据**：在数据看板首位加入「关卡累积星星」黄金统计卡片，与「完成局数」、「已拼碎片」、「总游玩时长」、「平均局时」、「成就解锁率」共同构成 6 大全景数据看板；
   - 采用响应式 Material 3 现代卡片与勋章墙布局，舒展展示 12 枚成就勋章、进度条与达成标识。
3. **全屏游戏设置中心 (`lib/pages/settings_page.dart`)**：
   - 从 Dialog 浮层重构为独立全屏页面 `SettingsPage`；
   - 采用现代化分组卡片设计（音效与交互、外观与背景、玩法与帮助、数据管理、版本信息）；
   - **整合玩法帮助入口**：在「玩法与帮助」分组中内置「玩法技巧与操作指引」入口，点击平滑跳转至全屏玩法手册。
4. **全屏玩法与操作技巧手册 (`lib/pages/how_to_play_page.dart`, `lib/pages/game_page.dart`)**：
   - 新建独立全屏页面 `HowToPlayPage`，替代原 `HowToPlayDialog`；
   - 图文卡片式展示 6 大核心操作技巧（拖拽磁吸、碎片组队、双指缩放平移、底图透视、边缘筛选、一键整理托盘）；
   - 对局页面（`GamePage`）顶部菜单中的“玩法技巧与说明”同步升级为跳转至全屏玩法手册。
5. **自动化测试与静态检查 (`test/new_features_test.dart`)**：
   - 完善针对 `MainScreen` 精简 AppBar、`AchievementsPage`、`SettingsPage`、`HowToPlayPage` 的 Widget 测试；
   - 全部 50 项测试 100% 通过，`flutter analyze` 0 警告 0 错误，Windows 桌面端编译调试构建成功。

## [2026-08-25 15:05 GMT+8] 优化大图提取按钮布局（移动至 AppBar 下方右侧，彻底杜绝遮挡网页）

### 变更内容
1. **大图提取按钮移至顶部右侧 (`lib/pages/online_image_picker_page.dart`)**：
   - 彻底移除右下角悬浮大按钮（原先会遮挡 Pixabay 等图库底部的图片下载规格与尺寸选择浮层）；
   - 将按钮移至 **AppBar 正下方右侧**（`top: 8, right: 10`），样式精简为半透明墨绿毛玻璃胶囊「🧩 提取本页大图」；
   - 整个网页中下方交互区 100% 毫无遮挡，规格选择与下载弹窗操作流畅无阻。

## [2026-08-25 15:00 GMT+8] 在线图库浏览增加网页内部前进/后退、显式关闭(X)与防误退 PopScope 保护

### 变更内容
1. **左上角显式关闭按钮 (`lib/pages/online_image_picker_page.dart`)**：
   - 将原先容易产生误解的返回箭头改为标准的「✕ 关闭」图标，点击才退出在线选图界面，防止用户误退丢失搜索历史。
2. **AppBar 右侧新增网页级前进与后退按钮 (`lib/pages/online_image_picker_page.dart`)**：
   - 新增 `◀` 后退与 `▶` 前进按钮，动态感知 WebView 历史堆栈（`canGoBack` / `canGoForward`），无历史时自动置灰禁用，有历史时支持平滑回退与前进。
3. **系统物理/手势返回智能拦截 (`lib/pages/online_image_picker_page.dart`)**：
   - 接入 `PopScope` 拦截机制：当按物理返回键或手势滑动返回时，若网页内部有浏览历史，优先触发网页内后退（`goBack`），仅在无历史时才退出页面，提供双重防误退保护。

### 变更内容
1. **细腻平滑无级滚轮缩放 (`lib/pages/crop_puzzle_page.dart`)**：
   - 解决桌面端鼠标滚轮单格缩放过大（2倍跳跃）的问题，采用以鼠标焦点为中心的无级微调算法（单格 4% 平滑递增/递减）；
   - 配合严格的视口边界 clamping 约束，实现丝滑流畅的缩放体验且永不露出黑底。
2. **实时缩放比例徽标与一键复位 (`lib/pages/crop_puzzle_page.dart`)**：
   - 在裁切视口右上角增加精致小巧的毛玻璃缩放胶囊（如 `100%`, `120%`, `200%`），实时响应用户缩放；
   - 缩放大于 100% 时高亮显示，支持点击一键复位回 100% 原始居中对齐状态。

### 变更内容
1. **精准拦截 Pexels 下载直链 (`lib/pages/online_image_picker_page.dart`)**：
   - 优化 `shouldOverrideUrlLoading` 路由识别，支持识别带有 `&dl=`、`?dl=`、`images.pexels.com/` 等参数的下载跳转；
   - 针对包含 `dl=` 的原始下载直链，保留其指定的分辨率与原始参数（如 4000x2311），确保完整拦截并存入下载箱，不再误触发系统 Edge 外部下载。

## [2026-08-25 14:52 GMT+8] 解锁 Unsplash 5000+ 原始超高分辨率直链与自制拼图裁切边界安全约束

### 变更内容
1. **Unsplash 原始超清大图直连 (`lib/pages/online_image_picker_page.dart`)**：
   - 优化链接升级策略，针对 Unsplash `/download` 官方直链保留原始下载地址，不再强加 `w=2400` 限制，完整享受 5000+ 原始超高像素无损下载；
   - 网页预览图升级默认提升至 4K+ 级（`w=3840&q=90`）。
2. **自制拼图裁切边界安全约束与黑边杜绝 (`lib/pages/crop_puzzle_page.dart`)**：
   - 将 `InteractiveViewer` 的 `boundaryMargin` 设为 `EdgeInsets.zero`，最小缩放 `minScale: 1.0`，完全禁止将图片拖离视口范围；
   - 优化 Canvas 导出矩阵变换与 BoxFit 几何对齐，实现屏幕预览与最终拼图 100% 像素级对齐，彻底杜绝裁切黑边。

## [2026-08-25 14:42 GMT+8] 优化下载结果浮动通知条（5s 自动消失+关闭按钮）与严格防破损图片校验

### 变更内容
1. **下载成功通知条优化 (`lib/pages/online_image_picker_page.dart`)**：
   - 彻底移除右下角常驻常遮挡的已下载胶囊，改为仅在下载成功后浮现 5 秒自动隐藏的精致通知条；
   - 通知条右侧新增显式 `X` 关闭按钮，支持用户随时主动关闭；
   - 页面右上角 AppBar 的「📥 打开下载箱」图标完全无遮挡，随时可点击查看。
2. **多层严格图片合法性校验与防破损防御 (`lib/logic/download_manager.dart`, `lib/pages/online_image_picker_page.dart`)**：
   - **二进制合法性解码拦截**：保存前必须通过 `ui.instantiateImageCodec` 验证，若遇到 HTML 错误页/防盗链损坏数据，自动删除临时文件并拒绝入库，从根源杜绝下载箱图裂；
   - **最小分辨率过滤**：小于 200×200 的图标、头像或占位图自动拒绝并清理；
   - **嗅探器过滤优化**：JS 嗅探器过滤 `.svg`、`placeholder`、`data:image` 与自然宽高小于 250px 的无关杂质图，确保仅提取真实高清素材。

## [2026-08-25 12:45 GMT+8] 内置全屏在线免版权图库选图、批量下载箱与关卡来源追踪系统

### 变更内容
1. **【我的自制】Tab 顶部双通道入口重构 (`lib/pages/tabs/my_puzzles_tab_view.dart`)**：
   - 顶部单一大卡片改造为左右对称紧凑双大卡片：左侧「📷 相册选图」（保持原有本地相册导入流水线）、右侧「🌐 在线选图」（直通全屏免版权图库浏览器）；
   - 采用安全布局约束与自适应空间分配，彻底杜绝小屏幕或紧凑视口下的布局溢出。
2. **全平台图库浏览器架构与 Windows 桌面端适配 (`lib/logic/desktop_webview_picker.dart`, `lib/pages/online_image_picker_page.dart`)**：
   - **移动端 (Android/iOS)**：单 WebView 极简内存架构，统一维护 1 个 `WebViewController`，顶部药丸栏平滑切换 Pixabay、Unsplash、Pexels 三大免版权图库；
   - **桌面端 (Windows/macOS/Linux)**：引入 `desktop_webview_window: ^0.2.3`（基于 Microsoft Edge WebView2），在独立大窗口中流畅浏览图库，自动处理 `main(args)` 多进程 titlebar 初始化与下载拦截；
   - **三层下载拦截与智能嗅探机制**：URL 路由拦截大图直链，右下角悬浮「🧩 提取当前页大图」通过 JS 智能嗅探最高清大图与网页内 Base64 解码，支持自动注入 CSS 净化网页端推广条；
   - **双模下载与反爬 Cookie 处理 (`lib/logic/download_manager.dart`)**：通过 `Dio`（附带 WebView 上下文 Cookies、Referer、UA）与 WebView JS Fetch 兜底下载，杜绝 403 Forbidden；自动解析真实像素尺寸（4K/FHD/HD）与文件体积，提供防重复下载与一键清空机制。
3. **批量已下载管理抽屉 (`lib/widgets/downloaded_drawer_sheet.dart`)**：
   - 右上角与右下角常驻悬浮「📥 下载箱 (N)」胶囊，支持批量搜索连续挑选下载，不打断用户浏览心流；
   - 底部抽屉展示已下载图片网格、画质角标（如 4K 超清）、分辨率与来源平台标签，点击直接唤起拼图制作。
4. **关卡全链路来源追踪与向下兼容 (`lib/data/models/custom_puzzle_item.dart`, `lib/widgets/choose_difficulty_sheet.dart`, `lib/pages/crop_puzzle_page.dart`)**：
   - `CustomPuzzleItem` 新增 `sourceType`（'gallery' | 'online' | 'preset'）、`sourcePlatform` 与 `sourceUrl` 字段，并在自制关卡卡片与难度选择弹窗中呈现专属平台徽标；
   - 序列化机制具备向下完全兼容，历史旧关卡存档平滑升级不崩溃。
5. **设计文档与自动化测试**：
   - 新增完整架构设计方案文档 `docs/online-image-picker-and-source-tracking-design.md`；
   - 新增专属单元测试 `test/download_and_source_tracking_test.dart`，全部 49 个单元/Widget 测试 100% 通过，`flutter analyze` 0 警告 0 错误。

## [2026-08-25 12:44 GMT+8] 新增免费分类图库批量下载方案与自动化脚本

### 变更内容
1. **新增完整方案文档 (`docs/image-source-and-batch-download-guide.md`)**：
   - 遴选 5 大免费可商用分类图库：Pixabay(首选)/Pexels/Unsplash/Wikimedia Commons/Openverse，明确许可、分类覆盖与 API 批量能力；
   - 建立本项目 5 分类关键词映射（animals/plants/architecture/landscape/anime）与 `assets/images/levels/` 目录对应关系；
   - 提供 3 种批量方法：A. API+Python脚本（推荐） B. 浏览器插件 C. gallery-dl/aria2 命令行，并给出与 `puzzle_quality_analyzer.py` + `export_selected_puzzles.py` 打通的完整工作流与一键命令。
2. **新增自动化下载脚本 (`scripts/batch_image_downloader.py`)**：
   - 支持 4 源（pixabay/pexels/unsplash/openverse）、5 分类、多关键词轮询、分辨率/方向过滤、去重、限流重试；
   - 并发下载（默认 6 线程）+ 断点续传（已存在跳过）+ `tqdm` 进度条；
   - 自动创建 `assets/images/levels/{category}/` 并生成 `download_manifest.json` 记录 `sourceUrl/license/author`，满足溯源与合规需求；
   - 支持 `--dry-run` 预览、`--per-category`、`--min-width`、`--orientation` 等参数，已验证 `openverse --dry-run` 命中与真实下载落盘。
3. **验证**：
   - `python scripts/batch_image_downloader.py --help` 正常；
   - `openverse --per-category 2 --dry-run` 命中 4 张；
   - `openverse --per-category 1` 真实下载 1 张至 `temp/test_download/` 并生成 manifest，验证通过后已清理。

## [2026-08-25 11:09 GMT+8] 吸收 myphoto 项目经验：升级 EXIF 拍摄方向自动转正与鲁棒性加固

### 变更内容
1. **EXIF 拍摄方向自动校正 (`scripts/puzzle_quality_analyzer.py`, `scripts/export_selected_puzzles.py`)**：
   - 吸收 `myphoto` 项目图片预处理经验，在 `read_image_safely` 中增加 `ImageOps.exif_transpose` 自动转正，彻底避免数码相机/手机拍摄的竖屏照片因 EXIF Orientation 标记被横置颠倒而误判死区与长宽比。
2. **多模态与性能避坑对齐**：
   - 保持图片输入尺寸 $\le 512\text{px}$ 限制（缩短 Prefill 时间）；
   - 保持 FP16 原生精度与 Batch 4 并发；
   - 完善 JSON 正则容错解析。

## [2026-08-25 11:07 GMT+8] 修复 Qwen-VL 多模态思考模式冲突与 JSON 正则解析 Bug

### 变更内容
1. **Ollama Thinking 模式与 JSON 语法约束冲突修复 (`scripts/puzzle_quality_analyzer.py`)**：
   - 深入定位到 `qwen3-vl:4b` 模型在启用 Ollama 思考链时，与原有的 `"format": "json"` 约束器发生冲突，导致模型内容在 thinking 阶段被强行截断返回空字符串；
   - 移除了强硬的 `format="json"` 约束，改为提示词强约束 + 鲁棒的 `re.search` 正则自动提取 ```json ... ``` 和 `{...}` 结构体；
   - 补齐了丢失的 `import re` 模块导入，避免被内部异常静默捕获。
2. **端到端验证通过**：
   - AI 视觉策展成功识别真实主题（如 `🏔️ 自然风光`、`✨ 动漫插画`、`🐾 萌宠生灵`）、画面主体描述与策展人点评，并正确注入到 HTML 与 JSON 报告中。

## [2026-08-25 10:52 GMT+8] 完善素材归档管线可选参数并禁用自动 featured 分类

### 变更内容
1. **纯净可选参数控制 (`scripts/export_selected_puzzles.py`)**：
   - 智能裁切 (`--auto-crop`)、WebP压缩转码 (`--webp`) 与生成索引清单 (`--manifest`) 均重构为显式可选开关（默认关闭，保持原图格式与原样直拷）；
   - 严格禁用并注释了自动 `featured` 映射，所有素材归入其实体主题子目录（anime/nature/animals/architecture 等），避免污染人工精选池。
2. **HTML 交互报告与命令生成升级 (`scripts/puzzle_quality_analyzer.py`)**：
   - 更新了常驻底部浮动操作栏中的一键复制命令与可选参数提示。

## [2026-08-25 10:37 GMT+8] 拼图质检管线增加四周边缘智能裁切评估与自适应最大网格难度分级

### 变更内容
1. **四周边缘死区容忍与内部核心加权 (`scripts/puzzle_quality_analyzer.py`)**：
   - 区分「四周边框格 (Border)」与「内部核心格 (Core)」；降低四周边框死区惩罚（边框玩家易拼且具备大图裁切余量），重点严惩内部盲盒死区。
2. **智能裁剪最佳窗口评估 (Smart Auto-Cropping ROI Search)**：
   - 对 4K/8K 超高分素材自动分析单侧死区（如顶部纯色天空、底部暗部），输出裁切提分建议与潜在适玩度预测。
3. **自适应最大网格难度上限 (Adaptive Max Grid Tier)**：
   - 结合有效分辨率与细节密度，为每张图片智能推荐最适合的最大切片规格（16~25 块入门 / 36~64 块标准 / 100~225+ 块大师宗师）。
4. **多模态 Batch 4 并发与 HTML 报告升级**：
   - 实测 50 张大图（物理初筛 + 20 张 Qwen-VL Batch 4 深度策展）仅耗时 39.15 秒；
   - HTML 报告增加核心死区 vs 边框死区对比、裁剪建议标签与最大上限徽章。
