# 变更记录 — 2026-09-08

## 自制/UGC 导出长边统一收敛为 1920（与 studio 对齐）（2026-09-08 16:08）

- **改动** `lib/logic/puzzle_model.dart`：新增公共常量 `kMaxExportLongSidePixels = 1920`（与 studio 侧 `image_proc.DEFAULT_LONG_TARGET = 1920` 对齐，仅缩小不放大）。
- **改动** `lib/pages/crop_puzzle_page.dart`：`_saveAndCreate` 落盘逻辑由「短边上限 2160」改为「长边收敛至 1920」（保留长宽比）。crop 交互过程不再限制最大选区，仅在保存时统一缩放，避免超大图显存暴涨；自制/UGC 输出尺寸口径与网络关卡（studio 导出长边 ≤1920）一致。
- **未做**：缩略图（360/720）保持客户端按需生成，是否由 studio 一并产出缩略图档留作后续独立立项设计；`downloaded_image_item.dart` 的 2160 仅用于 4K 画质标签阈值，与导出尺寸口径无关，未改动。

## jigsaw-data 多平台 Assets 发布方案落地（2026-09-08 12:30）

- **目标**：内容产物平台无关（相对 canonical key），平台差异收敛到唯一通道表 `channels.json`；
  支持 GitHub/Gitee/ModelScope/Cloudflare R2 多平台发布 + 国内外分流 + 主备自动切换。
- **新增脚本**（`temp/publish/`，未改动 lib/ 与 studio 代码）：
  - `channels.json`：6 通道定义（gitee/r2cdn/modelscope/jsdelivr/github/r2pub）、`releaseTag=assets`、zip 路径规则（preserve/flatten）。
  - `assetmap.py` / `normalize.py`：canonical key 解算 + 把 studio `out/` 归一化为 `stage/`（注入 `zipKey`、下沉 `.gitattributes` 关闭 LFS）。
  - `publish.py`：normalize / r2 / github / gitee / modelscope / all / verify 子命令；R2 走 rclone，git 平台走 worktree 投影+Release，发布顺序 blob→index→manifest。
  - `gitee_publish.py`：Gitee API 建 tag + 上传 Release 资产（需 GITEE_TOKEN）。
  - `verify_channels.py`：跨通道可达性 + zip sha256 抽检巡检（CI 门禁）。
  - `app_reference/`：`channel_config.dart`（通道表+解算）、`content_selector.dart`（竞速选路+粘性+熔断）、`integrations.dart`（最小改动清单）。
- **发布实测**：
  - R2（`jigsawdata.umao.top`）全量发布并通过巡检（56/56 key 可达，zip sha256 一致），作为国内外统一兜底主力。
  - GitHub 已推送 master 与 `assets` 标签（同指向含 zipKey 的 commit 1abf441），11 个 zip 已上传 Release（海外可用）。
  - Gitee 已用 Windows 凭据推送 master（含 zipKey）；Release 资产上传需 GITEE_TOKEN（API 401，待用户令牌）。
  - ModelScope 推送需 MODELSCOPE_TOKEN（当前鉴权失败，待用户令牌）。
- **设计要点**：`releaseTag` 固定为 `assets`（不随版本变，通道表永久稳定）；zip 文件名含 `-r{rev}` 不可变支持 CDN 长缓存；app 端用 `zipKey`+通道表合成多镜像、SHA256 校验兜底，旧版 app 仍走 `zipUrl` 兼容。
- **关联文档**：`docs/multi-platform-assets-publish-design-20260908.md`（完整方案与路径规则表）。

## 图集、活动与每日挑战卡片视觉与排序优化 (2026-09-08 10:58)

- **变更背景**：
  - 用户反馈 4 项视觉与交互体验优化：
    1. 图集（Collection）和活动（Event）内部卡片无需显示 `Level X` / `第 X 关` 等关卡编号，保持画面干净整洁；
    2. 图集网格卡片原标题栏右侧的 `10 levels` 在英文环境下占宽过大导致标题挤压截断，需将关卡数独立置于标题上方一行；
    3. 每日挑战（Daily Tab）每月挑战关卡倒序排列，最新日期排在首位；
    4. 图集卡片、活动卡片以及其内部关卡卡片根据 `updatedAt`（最近 7 天发布且未完成）统一展示与首页（Home）完全一致的 `New` 斜体金褐色徽章。
- **修改文件**：
  - `lib/logic/content/models/puzzle_collection_item.dart`:
    - 增加 `updatedAt`（`DateTime?`）字段并在 `fromJson` 中解析 `json['updatedAt'] ?? json['startTime']`；
    - 提供 `bool get isNew`（7天内判定）；同步补齐 `copyWith` 与 `toJson`。
  - `lib/logic/content/models/puzzle_event_item.dart`:
    - 增加 `updatedAt`（`DateTime?`）字段并在 `fromJson` 中解析 `json['updatedAt'] ?? json['startTime']`；
    - 提供 `bool get isNew`（7天内判定）；同步补齐 `copyWith` 与 `toJson`。
  - `lib/logic/content/pipelines/collections_content_pipeline.dart`:
    - 在 `getLevelsForCollection` 中将图集的 `updatedAt ?? startTime` 透传给生成的 `PuzzleLevelItem.addedAt`。
  - `lib/logic/content/pipelines/events_content_pipeline.dart`:
    - 在 `getLevelsForEvent` 中将活动的 `updatedAt ?? startTime` 透传给生成的 `PuzzleLevelItem.addedAt`。
  - `lib/logic/catalog_index.dart`:
    - 统一全局索引中 Event 与 Collection 关卡的 `addedAt` 来源为 `updatedAt ?? startTime`。
  - `lib/pages/collection_levels_page.dart`:
    - 移除卡片左上角 `t.game.titleLevel` 关卡编号角标；
    - 新增 `level.isNew && !isCompleted` 判定，在左上角展示与首页统一规格的贴边 `New` 角标。
  - `lib/pages/event_levels_page.dart`:
    - 移除卡片左上角 `t.game.titleLevel` 关卡编号角标；
    - 新增 `level.isNew && !prog.isCompleted` 判定与 `New` 角标，并补齐完成/进度角标。
  - `lib/pages/tabs/collections_tab_view.dart`:
    - 图集卡片底部重构为 `Column`：第 1 行显示 `10 levels` 关卡数，第 2 行标题独占全宽，彻底消除英文长标题截断问题；
    - 图集卡片新增 `col.isNew` 判定并在左上角展示统一样式的 `New` 角标；
    - 顶部活动焦点 Banner 若 `ev.isNew` 优先展示 `NEW` 标签。
  - `lib/pages/tabs/daily_tab_view.dart`:
    - 每月关卡列表按 `dailyDate` 倒序排序，使最新日期的卡片排在网格首位。
  - `lib/pages/tabs/events_tab_view.dart`:
    - 活动卡片若 `event.isNew` 优先展示 `NEW` 状态角标。
  - `lib/logic/content/app_content.dart`:
    - 增加 `@visibleForTesting void setManagerForTest` 便于独立单测隔离。
  - `test/widgets/card_new_and_order_test.dart`:
    - 新增 5 项单元与 Widget 测试，全链路覆盖 `isNew` 时间计算、管线透传、关卡无编号与 New 角标展示、每日倒序排列。
- **验证**：
  - `dart format`：改动文件格式化全通过；
  - `flutter analyze`：0 error，无新增 warning/info；
  - `flutter test`：299 / 299 测试全量通过；
  - `flutter build windows --debug`：Windows 编译无错误。
- **备份**：
  - 改动前文件均已备份至 `temp/backups/20260908_cards_update/`。

## 全量补齐残留未接入 slang 的硬编码 UI 字符串 (2026-09-08 10:27:16)

- **UI 界面与弹窗**:
  - `lib/pages/collection_levels_page.dart`: 清理缓存弹窗标题/说明、取消/确认按钮、清理 Toast、释放空间 Tooltip、关卡标题 (`t.levels.titleOf`)、空状态及重试按钮、关卡计数全量替换为 `t.collections.*`、`t.levels.*`、`t.common.*`。
  - `lib/pages/pack_levels_page.dart`: 缺失图片 Toast (`t.pack.imageMissing`)、续玩标题 (`t.levels.titleOf`)、删除图包 Tooltip (`t.pack.deleteTooltip`)、空状态提示 (`t.pack.emptyLevels`) 接入 slang。
  - `lib/pages/tabs/events_tab_view.dart`: 空状态标题/提示/同步刷新按钮 (`t.events.emptyTitle/emptyHint` / `t.common.sync`)、活动状态角标 (`t.events.badgeActive/badgePast`)、图包类型角标 (`t.events.badgeZip/badgeOnline`)、默认描述文案 (`t.events.descFallback`) 接入 slang。
  - `lib/pages/tabs/daily_tab_view.dart`: 时间锁 SnackBar 提示替换为 `t.daily.notUnlocked`。
  - `lib/widgets/share_card_generator.dart`: 水印应用名替换为 `'\u{1F98A} ${t.app.title}'`。
- **数据展示模型与本地化**:
  - `lib/logic/content/models/puzzle_pack_item.dart`: `displaySource` 返回本地化的 `source.album` / `source.online`。
  - `lib/logic/content/models/puzzle_collection_item.dart`: `displayTypeLabel` 返回本地化的 `collections.typeEvent` / `collections.typeOfficial`。
  - `lib/data/game_repository.dart`: 主线关卡生成默认标题接入 `LocaleSettings.instance.currentTranslations.game.titleLevel(index: i)`。
  - `lib/data/models/custom_puzzle_item.dart` & `lib/data/models/level_item.dart` & `lib/data/resume_helper.dart`: fallback 规格文本使用 `LocaleSettings.instance.currentTranslations.difficulty.pieceCount(cols: cols, rows: rows, count: rows * cols)`。
- **验证**:
  - 针对改动文件运行 `dart format`。
  - 运行 `dart analyze`: 0 error, 0 warning。
  - 运行 `flutter test`: 294 / 294 全部通过（已同步修复 `pack_content_pipeline_test.dart` 中对 `displaySource` 的本地化断言）。
  - 运行 `flutter build windows --debug`: 编译成功。
  - 运行 `flutter test .\integration_test\app_test.dart -d windows`: 页面集成测试全通。

## Studio 日志可观测性补齐（B1+B4：导出链路落盘 + 关键 catch 改 warning）

> 范围：后端优先。前端（Web 浮动日志按钮 + console 落盘）为后续步骤，本次未实施。

### 后端
- `studio/server.py` — `_handle_export` 的 `log_fn` 现在在写入 JobStore 的同时调用
  `logger.log(...)`（带 `[EXPORT]` 前缀），使导出全过程（逐张转码、index.json 写入、
  账本更新、各阶段 catch）落盘到 `temp/studio.log`，任务结束后仍可回溯。
- 为以下模块新增 `logger = logging.getLogger(__name__)`，并把静默 `except: pass` /
  `except Exception: return False` 改为 `logger.warning/error`（含文件路径与根因）：
  - `core/scanner.py`：单图元数据提取失败、哈希计算失败（原静默返回 None/空串）。
  - `core/workspace.py`：旧版 tags.json / .studio.db 迁移失败、操作/导出审计流水写入失败。
  - `core/cache_db.py`：质检详情 JSON 解析失败；补充模块 logger。
  - `core/exports_ledger.py`：账本解析损坏（含损坏副本备份）、旧版 exported.json 迁移失败、order 解析异常。
  - `core/quality_evaluator.py`：win32 标准流编码重配置失败（debug 级）。
  - `core/tags_manager.py`：tags.json 读取/保存失败（保存失败升级为 error）。
  - `core/image_proc.py`：缩略图缓存写入失败；原 `sys.stderr.write` 改为 `_logger.warning`。
  - `taxonomy.py`：relative_to 回退（debug 级）。
- 验证：`python -m py_compile` 全绿；`import studio.server` 触发 setup_logger 正常；
  启动服务实测 `/api/health`、`/api/export` 缺参均按 `文件名:行号` 格式落盘 `temp/studio.log`。

### 待办（后续步骤，未实施）
- B2/B3：在扫描/质检/标签合并/导出各阶段补充 `info/debug` 进度日志。
- B5：`FileHandler` 换 `RotatingFileHandler`（约 5MB×5）防止日志无限增长。
- 前端：新增 `static/js/logger.js`（内存环形缓冲 + 浮动按钮日志面板），并把
  `api.js`/`app.js` 的 catch 接入 `console.error` 与前端日志缓冲。

## Studio 导出「进度感知」落地（同步执行 + 状态轮询，向后兼容）

> 方案：`studio/docs/export-progress-perception-design-20260908.md`（评审后实施）
> 核心取舍：**不引入后台线程/SSE/任务生命周期**——导出仍在原 POST 请求线程内同步执行
> （执行模型零改动），利用 ThreadingHTTPServer 多线程，前端并发轮询只读状态接口拿实时进度。
> 可靠性：无 `clientTaskId` 的请求（含既有单测直调）走原代码路径，行为零变化。

### 后端

- `studio/server.py` — 新增模块级 `ExportJobStore`（`_JOBS` + `_JOB_LOCK`，全部读写持锁、快照返回拷贝；
  惰性清理：终态超过 5 分钟剔除、最多保留 50 条，无定时器）；新增 `GET /api/export/status?task=xxx`
  （未知任务返回 `found:false`，HTTP 200）；`_handle_export` 收到可选 `clientTaskId` 即注册任务，
  `log_fn` 双写 JobStore、`execute` 全程由 `progress_fn` 上报、成功/异常路径各自落 `done/error` 终态。
- `studio/core/image_proc.py` — `convert_images_parallel` 增加可选 `on_progress(done,total,current,ok)`：
  并行 `as_completed` 与串行兜底两分支均逐张回调（`current` 含 src/dst/label，`ok` 为成败）；
  回调异常吞掉不影响转码；默认 None 行为不变。
- `studio/exporters/base.py` — `BaseExporter` 增加可选 `progress_fn` 与 `report_progress()`；
  每张完成输出一条 `图片 源名 ==> 目标名 (n/total)` 日志（失败为 warn），无订阅者时仍输出。
- `studio/exporters/registry.py` — `get_exporter` 透传 `progress_fn`（可选，缺省兼容）。
- `studio/exporters/main_exporter.py` / `pack_exporter_base.py` / `daily_exporter.py` — 转码处接
  `on_progress=self.report_progress`。

### 前端

- `studio/static/js/api.js` — 新增 `fetchExportStatus(taskId)`（task 不存在返回 `found:false`，不抛错）。
- `studio/static/js/app.js` — `runExport` 生成 `clientTaskId` 塞入 payload；POST 与状态轮询（首查 150ms、
  间隔 700ms，`setTimeout` 递归不叠加速率）并行；任一通道先到终态即经幂等 `finalize()` 收尾；
  status 404/网络错误静默停止轮询、POST 结果兜底（最坏=回到旧行为）。
- `studio/static/index.html` — 执行中按钮显示 `⏳ 正在导出 (n/total)...`；执行中实时滚动日志面板。

### 转码期逐张处理日志（评审补充）

- 实机反馈：进度在按钮正常，但转码期（55 张 ≈ 49s）日志面板无任何条目、时间戳呈跳跃感。
- `studio/exporters/main_exporter.py` / `pack_exporter_base.py` / `daily_exporter.py` — 转码前先打一条
  `开始转码 N 张图片 → fmt (quality=q) ...` 阶段日志；pack/daily 的 zip 任务带 `label=arc_name`，
  逐张日志显示归档名而非临时文件名。
- 验证：workers=1 与 workers=8 两分支各 3 张 → 逐张日志行数 = 图片数、计数 (n/3) 递增，断言通过。

### 完成态 UI：预览视图保持 + 底部只留「关闭」（评审补充）

- 诉求：导出完成后统计/预览面板不要刷新消失（现状会跳到第④步结果页导致统计消失）；界面其余保持原样，
  完成态操作只留一个「关闭」。
- `app.js` — 新增 `exportDone` ref；`finalize` 不再切 `exportStep=4`，改为置 `exportDone` 并**停留第③步预览视图**；
  `openExport`/`restartExport`/`runExport` 重置 `exportDone`；`goExportStep` 在完成态点击步骤条时视为开启新一轮配置并清空上次结果。
- `index.html` — 日志/结果区显示条件改为 `(exportDone || isExporting)`；底部按钮组拆两态：
  完成态仅渲染「关闭」，非完成态保留 复制日志/上一步/下一步/确认导出；移除第④步「🔄 再导一次」按钮分支
  （`restartExport` 函数保留注释标注防回归，重导=关闭后重新打开）。
- 验证：46 单测（含前端 headless mount）全部通过。

### 验证

- 新增单测 5 个（JobStore：注册/快照拷贝/终态日志冻结/清理上限；status 端点真实起服端到端），
  **全套 46 个测试通过**（含既有 41 个无 taskId 兼容路径）+ 前端 smoke。
- 真实链路端到端（起服 + POST + 并发轮询，成功/失败两路径）：
  - 成功：77 次采样观测 progress 0→6/6、日志随执行递增（终态 11 条含「图片处理完成」）、终态 `done` + summary；
  - 失败：损坏图安全中止 → 终态 `error` + 错误信息，POST 返回 ok=false 携带 error。
- 备份：`temp/backups/studio-export-progress-20260908-090926/`

---

## 自制关卡移除虚假 title 与无 title 关卡规范适配

- **背景与根因**：拼图游戏关卡本身均无独立的图片标题（主关卡为关卡编号、每日关卡为日期、图包/活动为包名+序号）。原实现中自制关卡硬编码了 `'我的自制拼图'`，预置样例硬编码了 `'巴黎埃菲尔铁塔晨曦'` 等虚构标题，在选择难度等页面暴露出现，造成概念混淆与违和。
- **改动范围**：
  1. `lib/data/models/custom_puzzle_item.dart`：
     - `CustomPuzzleItem` 的 `title` 改为可选且默认空字符串 `''`。
     - 增加 `CustomPuzzleItem.isFakeTitle` 校验，在 `fromJson` 反序列化时自动识别并清洗历史遗留的虚假 title（`'我的自制拼图'`、`'巴黎埃菲尔铁塔晨曦'`、`'午后阳光与香浓拿铁'`、`'草地上奔跑的小柴犬'` 等）为空字符串。
  2. `lib/pages/crop_puzzle_page.dart`：裁剪保存自制拼图时不再填充硬编码的虚假 title。
  3. `lib/data/game_repository.dart`：移除 3 个预置自制样例中硬编码的虚假 title；清理日志中的 title 占位。
  4. `lib/widgets/choose_difficulty_sheet.dart`：
     - 难度选择页 AppBar 过滤虚假/空 title，当关卡无 title 时仅居中展示大标题「选择难度」，不显示多余副标题。
     - 删除确认弹窗适配无 title 情况，新增通用文案 `deleteDescGeneric`（如「确定要永久删除此自制拼图吗？删除后不可恢复。」）。
  5. `lib/l10n/`：`zh.i18n.json` 与 `en.i18n.json` 新增 `deleteDescGeneric` 并重新生成 slang 资源。
  6. `lib/pages/tabs/my_center_tab_view.dart`：个人中心卡片在 `card.title` 为空时不渲染空白占位行，自制拼图优雅呈现副标题（如「自制拼图 · 相册」）。
- **验证**：
  - 静态分析 `flutter analyze` 0 error / 0 warning。
  - 新增及全量测试 `flutter test` 294 个用例全部通过。
  - Windows Debug 编译 `flutter build windows --debug` 构建成功。

## Studio 导出转码并行化提速（图片级多进程，约 3.4~4.6x）

> 现象：导出 16 张图耗时约 1 分钟且全程无中间日志/进度。定位：单张 libwebp
> `method=6` 编码为耗时主体（6000×4000 大图实测 4~7s/张），换库（vips/NConvert/
> pyvips）单张仅快 20~50%；数量级加速来自「多图并行」——XnConvert 50 张/14s 同为此原理。

### 实测基准（本机 8 核 16 线程，webp q70）

```
16 张大图(共309MB, F: 盘源)         耗时      相对加速
Pillow m6 顺序(原实现)              86.9s      1x
Pillow m6 进程池 x8                 19.0s      4.6x
vips effort=6 进程池 x8             14.2s      6.1x   (vips 编码略快 ~25%，引入外部依赖)
NConvert 单进程                     ~2.9s/张    ~2x
pyvips 顺序(内部线程)               69.2s      1.3x
```

### 方案

`studio/core/image_proc.py` 新增：
- `sha256_file()` — 文件哈希（worker 内自足）
- `_convert_one_parallel(job)` — 模块级进程池 worker（Windows spawn 可 pickle）
- `_default_export_workers()` — worker 数：环境变量 `STUDIO_EXPORT_WORKERS` 优先，否则 `min(8, CPU核数)`
- `convert_images_parallel(tasks)` — 并行批量转码；进程池不可用时自动回退串行，绝不中断导出

三个导出器的图片转码循环改为「父进程备元数据 → 进程池并行转码 → 按序组装」，
产物（文件名/顺序/标签/hash）语义与串行完全一致：
- `studio/exporters/main_exporter.py` — 主线图片转码
- `studio/exporters/pack_exporter_base.py` — event/collection ZIP 内图片转码（临时文件名加 pack_id+idx 防冲突）
- `studio/exporters/daily_exporter.py` — daily ZIP 内图片转码（同上）

### 验证

- studio 既有 41 个单测全部通过
- 端到端（隔离临时源目录，12 张混合大图含 3 张 30MB+ PNG）：串行 ~69s → 并行(8) 20.3s，
  **3.4x**；串/并行产物（images/batches/index.json/manifest）去除时间戳字段后逐字节一致
- 非主线程（模拟 ThreadingHTTPServer worker）内 spawn 进程池实测正常（6 张 6000×4000 图 9.5s）
- 真实批次（F: 盘 16 张）预期 47s → 约 10~15s

---

## 1. 标签规则统一：无标签素材一律落成规范兜底标签 [Others]

> 用户要求「把没有 tag 的当作 Others tag，统一排序规则」。落地为一条**标签不变量**：
> 素材无标签 / 标签无法归类时，`tags` 一律为 `["Others"]`（taxonomy 规范形式，大写 O），
> 绝不出现空数组或小写 `"others"`。于是「是否未分类」在任何位置都等价于 `OTHERS_TAG in tags`，
> 筛选 / 统计 / 待复核判定不再需要分散的特判分支。

### 涉及文件

- `studio/taxonomy.py` — 新增唯一规范常量 `OTHERS_TAG = "Others"`
- `studio/core/tags_manager.py` — **修复潜在 bug**：`("others" in tags)` 对规范形式 `"Others"` 永远为假，导致高 confidence 的未分类素材不会被自动标记待复核；改为 `OTHERS_TAG in tags`（实测：conf=1.0 未打标 → `review_required=True`，修复前为 False）
- `studio/server.py` — 导出预检的兜底标签由小写 `["others"]` 改为 `[OTHERS_TAG]`（实测预览 `stats.tags` 键由 `others` 统一为 `Others`）
- `studio/exporters/main_exporter.py` — 双写兼容分支收窄，改用常量
- `studio/static/js/app.js` — 前端引入标签不变量三件套：
  - `normalizeTags(tags)`：去空去重、小写 others 一律规范为 `"Others"`，空则落 `[OTHERS]`；
  - `isOthers(r)` / `isOthersTag(t)`：统一的「未分类」判定；
  - `applyTags(r, tags)`：写入 tags 时同步 `catalogs` 与 `review_required`，保证不变量恒成立。
  - 入口收口：扫描加载（`doScan`）与导出成功后的重拉均做归一化；`batchSetTag` / `batchAddTag` / `batchRemoveTag` / `batchClearTags` / `toggleViewerTag` 全部改走 `applyTags`。
  - 判定坍缩：`tagCounts` 直接平铺计数（Others 不再单独虚拟累加）、`unreviewedCount` / Others 过滤 / 「选待复核」从「空数组或全 others」的多条件特判统一为 `isOthers(r)`。

### 验证（Playwright 实机，40 张全未打标样本）

```
1) Others(其他) 侧栏计数 = 40 ✅   2) 全部记录 tags === ['Others']，无空数组 ✅
3) 点「其他」过滤 = 40 张 ✅       4) 全选→覆盖 Landscapes → Others=0 / Landscapes=40 ✅
5) Others 过滤 = 0 张 ✅           6) 移除 Landscapes → 退回 Others=40 ✅
导出预检 stats.tags 键 = Others（原为小写 others）✅   41 个单测全部通过 ✅
```

---

## 2. Studio 导出页面（排序/预览三步流）问题排查与修复

> 详见 `studio/docs/studio-export-page-issue-report-and-fix-20260907.md`
> 排查方式：Playwright(Edge) 实机驱动 UI + `/api/export/preview` 契约探针 + 产物落盘核验。
> 回归：自研探针全绿 + `studio` 既有 41 个单测全部通过。

### 修复的功能性 Bug

1. **重复导出**：`index.html` 导出导航按钮的 `v-else` 兜底分支使第④步（结果页）仍显示「✅ 确认导出」，可再次点击重复导出。现改为第④步仅显示「🔄 再导一次」（重置回第①步），新增 `restartExport()`。
2. **Main 默认改名规则**：`rename` 由 `none` 改为 `sequence`，手动拖拽的顺序在产物文件名上直接可见（`101.webp…110.webp`）。此前顺序只存在于 `main.json`，磁盘不可见（与设计文档 §4.1 相悖）。
3. **首次版本号**：`main_exporter.py` 无历史版本时 `version` 由 `101` 改为 `1`，与前端「下版本 1」提示一致（101 是起始序号不是版本号）。
4. **排序口径统一**：`server.py` `_handle_export_preview` 的 `root` 由 `Path(src)` 改为 `Path(src).resolve()`，与 `/api/scan`、`build_manual_order` 一致。此前 srcDir 为相对路径/含 `..`/盘符大小写差异时，manual 排序键匹配失败会**静默退化**为扫描字典序且无告警。
5. **第二步补 ↑/↓/✕ 按钮**：激活此前从未被模板引用的死代码 `moveExportItem` / `removeFromPreview`；`✕` 剔除单张后即时重算统计（新增 `recalcPreviewStats()`），保证 KPI/分布与清单同口径。
   - **配套新契约 `excludedPaths`**：`base.py` 新增 `resolve_excluded()`，`main/daily/pack` 三个导出器与 `/api/export/preview` 统一按小写 posix 相对路径过滤。否则被 ✕ 的图片会拿到兜底 rank 排到清单**末尾照样导出**（预览 8 张、实际导出 10 张且序号错位）。前端新增 `exportExcluded` 集合并随 payload 提交，切换导出范围 / 新开导出会话时自动作废。

### 性能 / 规模

6. **预检请求体降 72.6%**：3000 张实测 2.1 MB → 580 KB。
   - `api.js` 的 `jsonStringifySafe` 去掉 `JSON.stringify(obj, null, 2)` 缩进；
   - `app.js` 新增 `buildSlimRecords()`，`tagsRecords` 只回传 `path/file/hash/tags/exported`（此前回传全量 records 含 width/height/quality 等）。
7. **大列表渲染**：`.og-card` 加 `content-visibility: auto` + `contain-intrinsic-size`，视口外卡片跳过布局与绘制。
8. **原生图片拖拽冲突**：`.og-thumb` 加 `draggable="false"` + `-webkit-user-drag:none` + `pointer-events:none`，避免与 SortableJS 抢拖拽事件。

### 其他

9. **体积预估联动质量**：`server.py` 新增 `_estimate_ratio(fmt, quality)`，`estWebpBytes` 由恒定 `sourceBytes*0.2` 改为随 format + quality 折算（webp 基准 0.20@q70，jpg 0.35，png/original 1.0），并回传 `estRatio` 供前端剔除后重算。同时删除第一步中只有说明文字、没有任何控件的「WebP 压缩率」死字段。
10. 标签分布 / 目录分布改为**各自独立归一化刻度**（`distMaxTags` / `distMaxDirs`），避免互相挤压。
11. 第一步排序下拉补齐 `mtime_dsc`，与第二步选项集一致，且改动即触发重新预检；第三步「起始序号」仅在 main 类型显示。
12. **导出视图加宽**：`.export-view` `max-width` 880px → **1920px**（对齐设计文档 §3「全屏 Exporter View」）。1920 屏实测宽度 1872px，顺序网格 16 列（原 880px 仅约 7 列）；1440/1280 小屏由 overlay padding 自然收敛为 1392/1232px。`.export-body` 去掉 `max-height: calc(100vh - 210px)` 改用 `flex:1 + min-height:0` 精确填满剩余高度，消除底部大片留白。
13. **澄清虚拟滚动适用范围**：主界面浏览网格（无拖拽，标准窗口化即可，收益最大）与导出顺序列表（SortableJS 需要真实 DOM，虚拟化与拖拽冲突）是两个问题。顺序列表的 `↑/↓` 按钮操作的是 `previewOrdered` 数组下标、与 DOM 无关，天然兼容任何虚拟化方案；真正受限的只有"跨屏拖拽"。详见报告 §7。

---

## 3. 导出工作台收尾三项（2026-09-08 晚段）

### 3.1 主界面输出字段下沉

输出目录 (Output Directory) / HTTP 根地址 (HTTP Base) 不再是主界面配置栏的字段——它们属于"导出确认前才需要决定"的产物输出设置。
- `studio/static/index.html` `config-bar` 移除 outDir/httpBase 两个 field-group，源目录仍由主界面填写。
- 两者作为「📦 输出设置」卡移入导出流程第③步（导出前预览），以 `*` 标注输出目录为必填；填写即记忆 `localStorage`，改动会重跑预检让「建议起始序号/版本」基于真实产物目录。
- 旧的 `persistConfig` 之前**没有暴露到 return 块**（仅 main bar `@change` 静默失效），本次顺带修正。

### 3.2 空输出目录点导出有明确报错

之前 `runExport` 校验只在 toast 闪一条就 return，导出大屏下一闪即逝，给操作者"没反应"的感觉。
- 新增 `exportError` ref：`runExport` 入口先清空，再分别校验「源目录 / 输出目录 / 标题（event+collection）」，不通过则把具体原因写到 `exportError` 并 toast 提示，`exportStep` 保持 3，不误入导出。
- 第③步 body 顶部加 `<div v-if="exportError" class="export-error-banner">` 红条，**持久展示**直到用户在输出设置或第①步修正（`goExportStep` / `openExport` / `onOutputChange` 都会清空）。
- 文案按"少了什么 → 在哪里补"两句式说清，例如：`输出目录为空：导出前请先在上方填写 输出目录 (Output Directory)。`

### 3.3 一键随机排序（tag 均分）

第②步新增「🎲 随机排序 (tag 均分)」按钮，与「↕ 整体反转」并列。
- 算法：从每张图的 tags 里取首个真实标签（无则归 OTHERS 桶），组内 Fisher-Yates 洗牌；组间按 **taxonomy 顺序**（其他=17 在末位，未知名排在 Other 之前）排序后做**欠账优先贪心**：
  - 每个 tag 的「欠账」= `(pos+1)/N × count − 已出数`，欠得最多的下一格出列（同分按 taxonomy 顺序破平）。
  - 若最优组与上一格相同且存在别的可选组，则改取次优组（避免同 tag 紧邻）。
  - 强制头部为 taxonomy 顺序的第一个非空组，保证序列观感从真实分类起。
- 该算法在常见分布下**理论最优**：同 tag 不会出现连续，除非某组数量大到无解（如某组占 30+、总 40，理论最小邻接 19 也已达成）；数量较均衡时（4×10、5×8 等）邻接为 0。
- 随机后 `sortBy` 自动切到 `manual`，仍可继续手动拖拽 / ↑↓ 调整。

### 3.4 验证

```
1) 主界面配置栏输入框数 = 1 ✅  （无「输出目录」「HTTP 根地址」字样）
2) 顺序列表张数 40
3) 一键随机：顺序变化 ✅   sortBy = manual ✅   相邻同 tag = 0
   头 12 个示例: [Landscapes, Others, Animals, Others, Landscapes, Animals, Others,
                 Landscapes, Animals, Others, Landscapes, Others]
   分布: {'Landscapes': 12, 'Animals': 12, 'Others': 16}
4) 第③步「📦 输出设置」面板出现 ✅
   清空 outDir → 点「确认导出」→ 红条 ⛔ "输出目录为空：导出前请先在上方填写 输出目录 (Output Directory)。"
   exportStep 保持 3 ✅
   填写 outDir → 红条消失 ✅
   确认导出 → 第④步日志 11 行 ✅  落盘 40 张 ✅
```

`studio` 既有 41 个单测全部通过；drag_probe 等老探针按 outDir 移到第③步的改动同步修正（`__STUDIO_VM__.outDir = v`）。回归脚本：
- `temp/studio_debug/verify_step3_output_shuffle.py` —— 三项需求合并验证
- `temp/studio_debug/shot_step3_states.py` —— 红条态 / 正常态截图

### 涉及文件

- `studio/static/index.html`
- `studio/static/js/app.js`
- `studio/static/js/api.js`
- `studio/static/css/studio.css`
- `studio/server.py`
- `studio/exporters/base.py`
- `studio/exporters/main_exporter.py`
- `studio/exporters/daily_exporter.py`
- `studio/exporters/pack_exporter_base.py`

### 未处理（需单独拍板）

- 顺序列表**虚拟滚动**（25k 张仍会生成 2.5 万 DOM 节点，`content-visibility` 只缓解渲染不减少节点）。
- `.export-view` 是否提升为真全屏（当前 `max-width: 880px`，与设计文档 §3「全屏 Exporter View」不符）。
- `og-card` 渲染 `it.tags` 徽章、利用 `prevTarget` 做历史批次提示。
- 预检结果缓存（sortBy/scope 未变时复用，避免切步重复请求）。

### 测试脚本（回归可复跑）

`temp/studio_debug/`：`probe_preview.py` / `ui_probe.py` / `drag_probe.py` / `scale_probe.py` / `scale_drag.py` / `verify_fixes.py` / `gen_images.py`
