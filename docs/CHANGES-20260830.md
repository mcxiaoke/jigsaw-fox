# 变更记录 2026-08-30

## [2026-08-30 17:27 GMT+8] 性能调优与难度选择交互体验优化（Memory Caching, Zero-IO on Drag/Snap, UX Polish）

**修改文件**：`lib/services/achievement_store.dart`、`lib/services/achievement_service.dart`、`lib/pages/game_page.dart`、`lib/data/progress_store.dart`、`lib/services/unlock_service.dart`、`lib/services/sound_service.dart`、`lib/widgets/choose_difficulty_sheet.dart`、`test/services/achievement_service_test.dart`、`test/new_features_test.dart`

**改动内容**：
- **⚡ 拼图性能根治与零 I/O 吸附**：
  - `AchievementStore` 升级为 100% 内存缓存（`_countersCache`, `_unlockedCache`, `_claimedCache`），彻底消除每次读写的 `jsonDecode` 与磁盘 I/O 阻塞；
  - `GamePage._onPieceSnapped()` 彻底移除局内实时成就评估与反序列化，吸附碎片数直接在内存累加并在关卡完成（`_handleSolved`）时批量提交一次，彻底根除 Release 移动端拼图掉帧与卡死。
- **🎯 难度选择器与解锁状态体验修复**：
  - `ProgressStore` 内存缓存 3 星图统计值，`UnlockService` 提供同步判定接口，`ChooseDifficultySheet` 打开时直接同步计算锁定状态，彻底消除了按钮先绿后灰的视觉闪烁；
  - 底部按钮在未解锁时文案精简为简洁有力的 **`未解锁`**，去除超长说明字符串，杜绝文字溢出；
  - 修复解锁说明文本中的 Dart 字符串插值，清晰准确显示目标张数与当前进度（如 `🔒 需要获得 3 星的不同拼图达到 2 张（当前 0/2）`）。
- **🎨 UI 呼吸感与排版优化**：
  - 预计耗时单独抽离为居中小行：`⏱️ 预计耗时：12~18分钟`；
  - 底部弹窗增加内边距与留白，包裹 `SingleChildScrollView` 确保各尺寸设备自适应且绝不溢出。
- **🎵 音效防堆积节流**：
  - `SoundService` 为 `Sfx.tap`、`Sfx.lock`、`Sfx.switchToggle` 增加 70ms 节流，杜绝快速连点或低端机卡顿时的音频连发爆音。

**校验**：`flutter analyze` 0 issues；全量 `flutter test` 139/139 全部通过；`flutter build windows --debug` 成功；Windows 冒烟运行极速顺畅。

---

**修改文件**：`lib/widgets/choose_difficulty_sheet.dart`、`lib/pages/achievements_page.dart`、`lib/data/game_repository.dart`、`test/new_features_test.dart`

**改动内容**：
- **ChooseDifficultySheet 7 档难度阶梯与解锁交互**：接入 `UnlockService` 实时判定档位锁定状态，锁定时卡片呈现灰暗锁头图标并在顶部提示解锁条件（如需 3 星图 X 张），禁用开始按钮；针对 2:3/3:2 矩形 L2（54块）断层弹窗二次确认防受挫，支持“不再提示”记忆。
- **AchievementsPage 成就墙数据驱动落地**：彻底摆脱原有 12 项假成就，升级为 25 项官方成就数据驱动；顶部实时展示总星数、3 星图数、通关局数、拥有金币、吸附碎片与时长看板；支持“领 +X 💰”即时领奖交互并播放 `coinsFly` 音效；监听成就解锁通知流实时刷新。
- **主线 100 关阶梯难度对齐**：按 v3.3.1 设计将 100 关主线平滑分为 6 个阶段（1~10: L1 25块，11~35: L1.5 36块，36~60: L2 64块，61~80: L3 100块，81~93: L4 144块，94~100: L5 225块），主线不设 L6 极限档。
- **回归测试全绿**：更新测试断言适配数据驱动结构，全项目 139 个单元与组件测试 100% 通过。

**校验**：`flutter analyze` 0 warning / 0 error；`flutter test` 139/139 全部通过；`flutter build windows --debug` 编译成功；Windows 冒烟运行正常。

---

**新增文件**：`lib/services/economy_service.dart`、`lib/services/unlock_service.dart`、`lib/services/achievement_store.dart`、`lib/services/achievement_service.dart`、`test/services/economy_service_test.dart`、`test/services/unlock_service_test.dart`、`test/services/achievement_service_test.dart`

**修改文件**：`lib/pages/game_page.dart`、`lib/logic/puzzle_model.dart`

**改动内容**：
- **经济系统服务落地（EconomyService）**：管理金币与提示券资产，实现增量通关发奖（Base + StarBonus，重复通关增量发奖与 20% Base 保底防死锁）；实现单日 200 金币硬上限控制（按本地日历日自动重置）；实现提示扣费优先级（免费券优先 → 金币扣费 → 余额不足拦截）。
- **内容解锁规则引擎（UnlockService）**：SSOT 架构直接读取底层 ProgressStore 与 GameRepository 真实数据；实现 7 档难度阶梯解锁判定（L1~L3 默认开，L4/L5/L6 按 3 星图数 2/5/10 张解锁）；实现每日挑战与活动主题包通关门槛判定。
- **数据驱动成就系统（AchievementService & AchievementStore）**：25 项 Dart const 静态定义（局数/3星/连胜/自制/条件型/吸附/时长/全通/大满贯）；持久化计数器与解锁时间戳；即时反馈通知流与 coinsFly 音效；支持成就奖励领取。
- **GamePage 经济与成就闭环**：通关结算触发金币发放与成就评估；胜利弹窗展示获得金币数与解锁成就徽章；提示按钮接入经济校验。
- **单测全面覆盖**：编写 11 项专属服务单测，全项目 139 个单测 100% 通过。

**校验**：`flutter analyze` 0 warning / 0 error；`flutter test` 139/139 全部通过；`flutter build windows --debug` 编译成功；Windows 冒烟运行正常。

---

**修改文件**：`lib/pages/game_page.dart`

**改动内容**：
- **提示 1.5s 时停机制**：在 `GamePage` 中引入 `_hintPauseUntil`，点击智能提示时触发 1500ms 时停，期间 `_startTimer` 暂停累加游戏用时，确保提示动画观察时间不压缩玩家评星成绩。
- **结算链路原子化与 StarCalculator 接入**：重构 `_handleSolved` 结算序列，立即取消定时器并原子执行：① `StarCalculator.calcStars` 双轴打分；② `ProgressStore.instance.recordDifficultyCompletion` 写入嵌套档位记录并获取 `deltaStars`/`minHintsUsed`；③ `GameRepository` 关卡进度同步；④ 清理快照；⑤ 弹出带 `earnedStars` 的 `_showVictoryDialog`。
- **防止异步重入与并发冲突**：确保结算过程严格按顺序 await，避免网络/存储与后续导航竞争。

**校验**：`flutter analyze` 0 warning / 0 error；`flutter test` 128/128 全部通过；`flutter build windows --debug` 编译成功；Windows 冒烟运行正常。

---

**新增文件**：`test/data/progress_store_test.dart`、`test/data/migration_service_test.dart`

**修改文件**：`lib/data/progress_store.dart`、`lib/data/migration_service.dart`

**改动内容**：
- **嵌套档位存储模型**：新增 `DifficultyRecord`（包含 `bestStars`, `bestTimeSeconds`, `isCompleted`, `playCount`, `minHintsUsed`, `extra`）；`LevelProgress` 新增 `records: Map<String, DifficultyRecord>`，彻底消除扁平遍历 350 次 SP 查询。
- **minHintsUsed 状态机与原子更新**：`ProgressStore.recordDifficultyCompletion` 实现 `minHintsUsed` 状态机（初始 -1，单调递减更新，首次降为 0 时返回 `isFirstNoHintWin` 支撑成就触发），记录单档最佳用时与星数增量 `deltaStars`。
- **全局资产聚合统计**：`ProgressStore` 新增 `getDistinctImagesWith3Star`（按 canonicalId 去重 3 星图数，支撑解锁判定）、`getTotalStars`（全图全档位星数求和）与 `getTotalSolved`。
- **存量数据迁移与幽灵快照清理**：`MigrationService` 增加 v3.3.1 迁移链路，将旧 16 块（`4x4`）记录自动与 25 块（`5x5`）合并（取最佳星级与最佳用时并累加游玩次数），并安全清理已作废的 3:4/4:3 与 4x4 未通关幽灵快照。
- **单测覆盖**：编写 100% 覆盖率的 `progress_store_test.dart` 与 `migration_service_test.dart`。

**校验**：`flutter analyze` 0 warning / 0 error；`flutter test` 128/128 全部通过；`flutter build windows --debug` 编译成功；Windows 冒烟运行迁移日志正常。

---

**新增文件**：`lib/logic/star_calculator.dart`、`test/logic/star_calculator_test.dart`

**修改文件**：`lib/logic/puzzle_model.dart`、`lib/pages/crop_puzzle_page.dart`、`lib/widgets/choose_difficulty_sheet.dart`、`test/crop_puzzle_test.dart`、`test/widget_test.dart`、`test/new_features_test.dart`、`test/snapshot_store_test.dart`

**改动内容**：
- **规格收敛与最小面积损失裁剪**：`PuzzleAspectRatio` 枚举收敛为 `square1x1` (1:1), `portrait2x3` (2:3), `landscape3x2` (3:2) 3 种方向；实现 `CropLoss(r, target) = 1 - min(r / target, target / r)` 最小面积损失法选取算法；`CropRatio` 同步收敛。
- **7 档难度网格对齐**：`PuzzleDifficulty` 与 `tiers` 全面对齐 v3.3.1 定稿（1:1 含 L1.5 6×6=36 块，删 4×4=16 块，增加 20×20=400 块及 16×24/24×16=384 块）；同步各档位 `estimatedMinutes` 与非线性 `secPerPiece` 属性。
- **双轴多梯度评星计算器**：新建 `StarCalculator`，实现 `hintAllowance`（提示免罚额度随片数缩放 `(pieces/50).ceil().clamp(2,6)`）与双轴打分（TimeScore × HintScore 取 min，全程 int 毫秒防抖动比对，保底 1 星）。
- **单元测试与回归**：新增 `star_calculator_test.dart` 覆盖全部临界边界；修复已有测试中旧档位与旧比例断言；修复 `ChooseDifficultySheet` 在新标签下的尺寸防溢出优化。

**校验**：`flutter analyze` 0 warning / 0 error；`flutter test` 123/123 全部通过；`flutter build windows --debug` 编译成功；Windows 冒烟运行启动正常。

---

**修改文件**：`docs/jigsaw-difficulty-scoring-achievements-design.md`（v3.3 → v3.3.1）

基于两份实现就绪度评审（`temp/reviews/jigsaw-v33-design-vs-code-gap-review-20260830.md`、`jigsaw-design-v3.3-implementation-complexity-and-gaps-report-20260830.md`），开工前冻结全部实现歧义：

- §2.2 裁剪管线按图源分叉：UGC 在 CropPuzzlePage 导出时裁剪；内置/下载图经 ImageCacheManager 派生缓存（键 `${assetPath}_${targetRatio}`）
- §3 新增主线 100 关默认难度阶梯（1~10 关 L1 / 11~35 L1.5 / 36~60 L2 / 61~80 L3 / 81~93 L4 / 94~100 L5，主线不设 L6）；L2 二次确认在点"开始"时触发 + "不再提示"存 Prefs
- §4.1 修正"Base=estimatedMinutes 区间下限"表述错误（实为落于区间内，7 档均核算）
- §5 补 minHintsUsed 结算状态机（初始 -1、单调递减更新、首次归零发 noHintWin 事件）；3 星图数统计范围=ProgressStore 全部 canonicalId（main/daily/custom/generic/UGC/在线包）
- §6.1 补 bestReward 由 bestStars 推导、无需新存储；金币/提示两表口径注记
- §6.2 补提示扣费优先级：免费券 → 金币 → 轻量引导弹窗
- §7.3 补跨天归属判定（每日挑战按 dateStr、签到/日上限按动作时本地时间）；结算原子性与 _saveDebounce 并存说明；isUnlocked 双来源治理（旧持久化字段迁移后废弃）
- §8.1 speed_10min 保持 ≥100 片（用户决策：荣誉性宽松、日上限 200 兜底，不提高到 144 片）
- §8.2 补成就达成即时反馈（顶部胶囊横幅 + coinsFly 音效）
- §9 新增分阶段实施顺序 Phase 1~5（地基 → 结算原子化 → 三大 Service → UI → L6 实测）

再次清理文档格式化污染 47 行（`~~`/`\~`/`\_`、`<br />`，污染形式较上次新增 `~~` 双写）。

纯文档变更，未改代码。

---

## [2026-08-30 15:52 GMT+8] v3.3 追加 3 处进码前微调 + 清理格式化污染

**修改文件**：`docs/jigsaw-difficulty-scoring-achievements-design.md`

- §3 跨规格偏差补口径注记：`bias = |sq - rect| / sq`（方形片数为分母；max 口径 L2 为 18.5%）
- §3 新增 `estimatedMinutes` 显式同步对照表（7 档：1~3/2~4/5~8/12~18/25~35/50~75/90~180 分钟，与 §4.1 UI 预估列一致），消除 `puzzle_model.dart` tiers 同步遗漏风险
- §9 Roadmap L6 性能实测任务补两项前置渲染优化：`RepaintBoundary` 隔离动态/静态碎片 + 组合体（Group）合并使渲染节点随拼接递减
- 清理文档被自动格式化工具污染的 40 行（`\~`/`\_` 转义、空表格单元格 `<br />`，跳过代码块；与 commit 8c4e276 后的同类问题同源）

纯文档变更，未改代码。

---

## [2026-08-30 15:23 GMT+8] v3.3 评审修订：评分/经济/存储/解锁全面收紧

**修改文件**：`docs/jigsaw-difficulty-scoring-achievements-design.md`（v3.2 → v3.3）

**用户三项决策**：① 矩形 24→54 断层接受不补档；② 解锁门槛改用 3 星图数口径（L4/L5/L6 = 2/5/10 张）；③ SecPerPiece 采用非线性表 + 上线后埋点校准。**总体原则（荣誉性）**：休闲游戏难度与解锁全宽，日常内容 L1~L3 拿满奖励，L5/L6 纯荣誉挑战。

**主要修订**（基于 4 份外部评审 `temp/reviews/`）：
- §2.2 修正裁剪示例方向错误（4:3→3:2 应裁高）+ 比例选取改最小面积损失法 + UGC 图片必须可视裁剪 UI（禁止静默中心裁剪）
- §3 声明矩形断层为已知妥协（附 UI 缓解：二次确认弹窗）
- §4.1 SecPerPiece 线性 2.5~5.0 改非线性 3.0~25.0（与 estimatedMinutes 对齐，标注竞速基准语义 + 埋点校准机制）
- §4.2 评星删冗余区间（140%/5 次无效分支）、提示免罚额度随片数缩放 `hintAllowance`、全程 int 毫秒比较、提示时停 1~2s、StarCalculator 替换 `_calculateStars` 须配单测
- §5 存储改嵌套 `LevelProgress.records: Map<difficultyKey, DifficultyRecord>`（避免扁平键 350 次 SP 查询）+ 新增 `minHintsUsed` 字段（支撑 noHintWin 防刷）+ 16→25 合并规则（max/min）+ 双总星口径（累加=展示 / 3 星图数=解锁）+ §5.1 幽灵难度清理逻辑
- §6 金币增量制（防同图重复刷金）+ 复玩保底 `Base×20%`（防金币死锁）+ 日上限 200 + 提示定价以显式表为准 [5,6,10,15,20,25,35]（不变量 hintPrice ≤ 2 星收益）+ 删星币兑换改星数里程碑奖励
- §7 解锁门槛全改 3 星图数口径 + 荣誉性原则（高难无独占奖励）+ UnlockService 数据源改 ProgressStore/GameRepository（SSOT）+ 结算原子性质量门 + 本地日历日口径 + 签到无门槛化
- §8 成就模型显式分三类（accumulative/conditional/derived）+ masterAll 移出枚举改 dependsOn + 计数器档位分区（`solved:10x10`）+ speed_10min 限 ≥100 片 + three_star_diff 50→30 + playSeconds 生命周期增量口径
- §9 Roadmap 补 3 组单测（star_calculator/migration_service/economy_service）、UGC 裁剪 UI、L6 降档预案阈值（>500ms 或 <45fps）

纯文档变更，未改代码。原稿备份：`temp/backups/jigsaw-difficulty-scoring-achievements-design.md.bak_20260830_152309`。

---

## [2026-08-30 15:03 GMT+8] v3.2 新增内容解锁体系章节

**修改文件**：`docs/jigsaw-difficulty-scoring-achievements-design.md`（v3.1 → v3.2）

**新增**：
- §7 内容解锁体系（Progression / Unlock，原成就系统顺延为 §8）：6 种解锁维度（顺序/星数/通关数/档位/活跃天数/时间窗口）+ 各内容源分层解锁策略表 + `UnlockService` 规则引擎（复用 AchievementStore 计数器，零重复存储）
- Roadmap 新增 P1 `UnlockService` 落地条目（解锁判定收敛 + 每日/活动门槛 + 锁定 UI 组件）

**休闲宽松原则**（按用户要求）：所有门槛按"正常玩家 1-2 天自然达标"校准——L4 星≥5、L5 星≥15、L6 星≥30；每日挑战仅需通关 1 局（防空号）、活动需通关 5 局；锁定项必显条件与进度文案，防弃坑。

纯文档变更，未改代码。

---

## [2026-08-30 14:58 GMT+8] v3 定稿补 7 处设计缺口（外部评审修订）

**修改文件**：`docs/jigsaw-difficulty-scoring-achievements-design.md`（v3 → v3.1）

**新增/修订**：
- §2.3 4×4=16 档位删除策略（推荐档改 L1.5/L1；存量 bestStars 并入 L1、快照作废）
- §5 术语与代码对齐（Difficulty→PuzzleDifficulty、PuzzleId→canonicalId、复合键 canonicalId+difficultyKey）
- §5.1 旧 3:4/4:3 存档兼容策略（未通关作废 / 已通关保留记录，migration_service 执行）
- §6.2 提示定价显式表（L1=15 … L6=60）
- §7.1 metric 枚举扩充 playSeconds / speedWin / masterAll（派生成就），补齐 speed_10min / time_2h / master_all 覆盖
- §7.3 新增 AchievementStore 存储设计（计数器按 metric 存储、threeStar 按 canonicalId 去重、签到字段）
- §8 Roadmap 补全 P0 文件清单（crop_puzzle_page.dart 删 3:4/4:3 入口、裁剪缓存管线、L6=400 性能实测前置、LevelItem.isUnlocked 口径确认、签到存储依赖顺序）

代码核对一致项：daily/custom/generic 确实未存 stars、`_calculateStars` 单轴、2:3 现网缺 L6、totalStars 仅算 main levels。

纯文档变更，未改代码。

---

## [2026-08-30 14:48 GMT+8] 难度·评分·成就·经济系统设计合并为 v3 定稿

**新增文件**：`docs/jigsaw-difficulty-scoring-achievements-design.md`

**归档**：三份旧稿移入 `temp/docs/`：`jigsaw-difficulty-level-design.md`（v1）、`jigsaw-scoring-stars-achievements-design.md`（v1）、`jigsaw-optimal-difficulty-scoring-design.md`（v2）

**v3 核心决策**：
- **移除 3:4/4:3 图片规格**：收敛为 1:1 + 2:3/3:2 双规格，配套居中裁剪适配（4:3→3:2 损失 12.5%）；依据：内置图库 63 张实测 3:4 占比 0% + 12k² 序列稀疏致 L4/L5 偏差超标
- **1:1 保留 6×6=36 过渡档**（L1.5 仅正方形图），平滑 25→64 跃升；跨规格偏差全部 ≤16%
- 采纳 v2 全部优化：双轴多梯度评星（Time×Hint 取 min）、实际片数评分基准、金币 DifficultyBase+StarBonus（倍率 16→5.5×）、成绩绑定 PuzzleId、AchievementMetric 枚举成就（25 项里程碑式）

纯文档变更，未改代码。

---

## [2026-08-30 14:10 GMT+8] 新增评分·评星·成就与奖励系统设计文档

**新增文件**：`docs/jigsaw-scoring-stars-achievements-design.md`

**内容**：基于 6 档难度分级，设计 1-3 星评星规则（提示+用时双维度、按档位参数化）、星星/金币双货币经济、29 项 Dart const 表驱动成就、道具池与落地改造点。已确认决策：v1 不做误放惩罚机制（v2 扩展位）、成就用 Dart const 表、双货币分离。纯文档变更，不动架构文档。

---

## [2026-08-30 14:02 GMT+8] 修复存档续玩 P1 & P2 缺陷与全面优化

**新增文件**：`lib/data/migration_service.dart`、`test/snapshot_store_test.dart`

**修改文件**：`lib/data/snapshot_store.dart`、`lib/data/progress_store.dart`、`lib/data/resume_helper.dart`、`lib/data/game_repository.dart`、`lib/logic/models/puzzle_state.dart`、`lib/pages/game_page.dart`、`lib/pages/pack_levels_page.dart`、`lib/pages/tabs/home_tab_view.dart`、`lib/pages/tabs/daily_tab_view.dart`、`lib/pages/tabs/my_puzzles_tab_view.dart`、`test/logic/snapshot_test.dart`

**改动内容**：
- **P1-4（单关卡单残局收敛）**：`SnapshotStore.save` 写入新难度快照时自动扫描并清理同关旧快照文件；`ProgressStore` 保持单难度同步，彻底杜绝孤儿快照。
- **P1-5（自由摆放残局续玩）**：`LevelProgress.canResume` 放宽为 `hasSnapshot && !isCompleted`，结合 `isTrivial`（5s内无操作过滤），使有效自由摆放残局正常支持“继续游玩”。
- **P1-6（收敛双写停写大 JSON）**：`GameRepository` 彻底停写 SharedPreferences 中的 `savedSnapshotJson` 大字段，快照全量交由文件系统管理，消灭冗余 IO。
- **P1-7（平滑迁移服务）**：引入 `MigrationService`，在 `GameRepository.init()` 中自动检测并将老版 Prefs 快照平滑无感迁移至新体系并标记完成。
- **P1-8（索引与文件自愈对账）**：`ResumeHelper.fetchResume` 在检测到快照文件缺失或损坏时，自动清理失效索引；`ProgressStore` 新增 `reconcile` 对账机制。
- **P2 优化**：
  - **P2-9**：`SnapshotStore` 原子写引入 `.bak` 备份回滚保护，消除 Windows 覆盖重命名失败与零文件窗口风险。
  - **P2-10 & P2-11**：清理 `listDifficultyKeys` 死代码，全链路改用非阻塞异步目录扫描。
  - **P2-12**：文件名安全化追加基于原始 `canonicalId` 的 8 字符短哈希（FNV-1a），彻底防止特殊字符替换产生的重名串档。
  - **P2-14**：`PuzzleBoardState.fromJson` 增加 `pieces.length == rows * cols` 与版本合法性校验，损坏时安全自愈。
  - **P2-15**：启动时自动异步清理残留 `.tmp`/`.bak` 临时文件；`resetAllData` 调用 `SnapshotStore.clearAll` 彻底清空快照目录。
  - **P2-16 & P2-17**：消除 `fetchResume` 重复 load 开销；优化 `loadJsonString` 避免二次编解码。
  - **P2-18**：修复所有 9 处 `use_build_context_synchronously`，`flutter analyze` 达成 0 issues。

**校验**：`flutter analyze` 0 issues；`flutter test` 118/118 全部通过；`flutter build windows --debug` 编译成功。

---

## [2026-08-30 13:58 GMT+8] 新增拼图难度分级设计文档

**新增文件**：`docs/jigsaw-difficulty-level-design.md`

**内容**：确认跨规格统一的 6 档难度分级方案（新手/简单/普通/进阶/困难/大师），1:1、2:3、3:4 三种图片规格按总片数对齐；删除原方案中 1:1 的 6×6=36 错位档，修正 12×12 漏写与 2:3 跳档问题。纯文档变更，不改动原架构文档（`jigsaw-puzzle-game-architecture.md` / PRD 暂不同步）。

---

## [2026-08-30 13:45 GMT+8] 修复“重新开始直接进局”与“点进去就有记录”

**修改文件**：`lib/data/resume_helper.dart`、`lib/pages/game_page.dart`、`lib/pages/tabs/home_tab_view.dart`、`lib/pages/tabs/daily_tab_view.dart`、`lib/pages/tabs/my_puzzles_tab_view.dart`

**背景**：用户反馈“只要点进去就有记录，点重新开始也是直接进，永远不能选难度；点重新开始应删进度回到无记录的选难度页”。

**改动**：
- **重新开始**：`ResumeHelper.handleResumeResult` 的 `restart` 分支改为仅 `clearResume+onClearRepo` 后返回 `false`，不再直接 `onPushGame`，由调用方 `tryHandleResumeFlow` 返回 `false` 后继续弹 `ChooseDifficultySheet`（无 `savedProgressPercent`），实现“删档后回到选难度”
- **点进去就有记录**：`ResumeHelper.fetchResume` 与 `GamePage._doSave/_flushSync` 增加“无意义残局”过滤：`percent==0 && hints==0 && elapsed<5s && pieces.every(clusterId==id)` 视为初始未玩，不落盘也不视为可续玩，避免空局占位

**校验**：`flutter analyze` 9 提示；`flutter test` 113 通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 13:38 GMT+8] 修复已通关重玩无“继续”及 P0 级存档缺陷

**修改文件**：`lib/data/resume_helper.dart`、`lib/data/game_repository.dart`、`lib/pages/game_page.dart`、`lib/game/jigsaw_puzzle_game.dart`、`lib/data/snapshot_store.dart`、`lib/pages/tabs/home_tab_view.dart`、`lib/pages/tabs/daily_tab_view.dart`、`lib/pages/tabs/my_puzzles_tab_view.dart`

**背景**：用户反馈“某关卡某难度已通关后重玩，玩一半退出再进只有重玩无继续”；同步复盘 `docs/code-review-20260830-save-resume.md`（8040f70）。

**Code Review 甄别**：
- **真实 P0 必改**：P0-1（难度不匹配静默丢弃，`adaptiveForSize` 覆盖快照 `rows/cols` 导致 `_applyBoardState` 直接 return）、P0-2（`pieceCount->diff` 横竖歧义 `6x4/4x6` 删错档）、P0-3（`_doSave` fire-and-forget，`dispose` 逃逸 Timer，`paused` 无法保证落盘）—— 均已修复
- **真实 P1 必改**：P1-5（`percent==0` 自由摆放不弹继续，实为残局却成孤儿）—— 已修复；P1-8（索引/文件双真相源）在本次“只留最新残局”简化下以 `hasSnapshot` 为准，文件为准已在 `fetchResume` 兜底遍历
- **按需简化**：P1-4 多难度并存按用户最新要求“只留最新残局”，保留单 `activeDifficultyKey`，`ContinueDialog` 多难度 Tab 保持但仅传单值；P1-6 双写未收敛、P1-7 无迁移因“未发布可直接改”暂不处理
- **P2 优化**：仅修 P2-9 原子写零文件窗口（`rename` 直接覆盖，回退删后重命名）与 P2-13/P2-14 的日志，其余（`listSync` 同步、canonical 哈希、`.tmp` 清理等）暂缓

**改动**：
- **续玩阻断**：`ResumeHelper.fetchResume` 去掉 `isCompleted` 首行阻断，改为仅 `hasSnapshot && percent<100 && !isSolved`，`percent==0` 亦可续玩；`displayProgress` 改为 `hasSnapshot` 优先于 `isCompleted`；三 Tab 的 `ChooseDifficultySheet.savedProgressPercent` 改为 `dp==0?null:dp`，重玩残局亦显示
- **P0-2**：`GameRepository.updateLevel/Daily/Custom/Generic` 新增 `difficultyKey` 显式参数，通关/清档优先按 `dkey` 精确删，`pieceCount` 仅作兼容兜底；`GamePage._handleSolved` 显式 `dkey=SnapshotStore.difficultyKeyFor(_effectiveDifficulty)` 传入
- **P0-3**：`SnapshotStore.save` 改为 `tmp.rename` 直接覆盖（无零窗口），新增 `saveSync` 供同步兜底；`GamePage` 新增 `_flushSync/_canonicalIdForSave`，`didChangeAppLifecycleState` 与 `dispose` 改调 `_flushSync`（取消 debounce 且 `_isSaving` 时不建新 Timer，避免逃逸），`_doSave` 保留异步路径供常态
- **P0-1**：`GamePage._loadImage` 若有快照则直接用快照 `rows/cols` 构造 `effectiveDiff` 并跳过 `adaptiveForSize`，避免 `_applyBoardState` 因尺寸 mismatch 静默丢弃；`JigsawPuzzleGame._applyBoardState` 增加 `warning` 日志带 `dkey/canonicalId`
- **P1-5**：`fetchResume` 去掉 `percent<=0` 阻断，已通关重玩的自由摆放残局可继续

**校验**：`flutter analyze` 9 提示；`flutter test` 113 通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 13:00 GMT+8] 进一步抽取续玩分支 Helper，消除 Continue/Restart 分支重复

**修改文件**：`lib/data/resume_helper.dart`、`lib/pages/tabs/home_tab_view.dart`、`lib/pages/tabs/daily_tab_view.dart`、`lib/pages/tabs/my_puzzles_tab_view.dart`、`lib/pages/pack_levels_page.dart`

**背景**：首轮抽取后，`final resumeResult = await ResumeHelper.maybeShowResumeDialog(...); if (resumeResult != null) { if (cancelled) ... if (continue) ... else if (restart) ... }` 约 20 行分支仍在 4 个入口重复。

**改动**：
- `ResumeHelper:26` 新增 `diffForKey`、`handleResumeResult`、`tryHandleResumeFlow`：`maybeShow` 后统一处理 `cancelled/continue/restart` 三分支，`continue` 自动 `diffForKey+loadJsonString`，`restart` 自动 `clearResume+onClearRepo`，均带 `context.mounted` 守卫。
- 四入口精简为一行：`if (await ResumeHelper.tryHandleResumeFlow(context:..., canonicalId:..., fallbackDifficulty:..., isCompleted:..., title:..., imageBytes:..., onClearRepo:..., onPushGame:..., onCancelled:...)) return;`，`ChooseDifficultySheet` 的 `savedProgressPercent` 统一走 `ResumeHelper.displayProgress/loadProgress`。

**校验**：`flutter analyze` 9 个 `use_build_context_synchronously` 提示；`flutter test` 113 通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 12:50 GMT+8] 抽取存档续玩复用 Helper，消除 Home/Daily/My/Pack 重复代码

**新增/修改文件**：`lib/data/resume_helper.dart`、`lib/pages/tabs/home_tab_view.dart`、`lib/pages/tabs/daily_tab_view.dart`、`lib/pages/tabs/my_puzzles_tab_view.dart`、`lib/pages/pack_levels_page.dart`

**背景**：用户反馈 `final canonicalId = level.id; final progress = await ProgressStore.instance.load(canonicalId); PuzzleBoardState? fileSnapshot;` 等约 20 行续玩检查逻辑在 4 个入口重复。

**改动**：
- 新增 `ResumeHelper:11` 统一封装：`fetchResume(canonicalId, fallback, isCompleted)`（按 `activeDifficultyKey` 优先、失败遍历兜底，仅 `0<percent<100` 视为可续玩）、`maybeShowResumeDialog(context, canonicalId, fallback, isCompleted, title, imageBytes)`（有存档即弹 `ContinueDialog`，返回 `null` 无存档 / `'cancelled'` 取消 / `'continue:<k>'` / `'restart:<k>'`）、`clearResume`、`loadProgress`、`displayProgress`。
- 四入口精简至 5-8 行调用：`home_tab_view.dart:85`、`daily_tab_view.dart:23`、`my_puzzles_tab_view.dart:98`、`pack_levels_page.dart:81` 均改为 `await ResumeHelper.maybeShowResumeDialog(...)` 分支处理 `continue/restart/cancelled`，`ChooseDifficultySheet` 的 `savedProgressPercent` 改为 `ResumeHelper.displayProgress`。

**校验**：`flutter analyze` 仅 10 个 `use_build_context_synchronously` 提示；`flutter test` 113 项全通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 12:40 GMT+8] 落地“存档与续玩”长期最优方案（文件级快照 + 显式继续/重开）

**新增/修改文件**：`lib/logic/models/puzzle_state.dart`、`lib/data/snapshot_store.dart`、`lib/data/progress_store.dart`、`lib/data/game_repository.dart`、`lib/pages/game_page.dart`、`lib/widgets/continue_dialog.dart`、`lib/pages/tabs/home_tab_view.dart`、`lib/pages/tabs/daily_tab_view.dart`、`lib/pages/tabs/my_puzzles_tab_view.dart`、`lib/pages/pack_levels_page.dart`、`test/logic/snapshot_test.dart`

**背景**：按 `docs/save-resume-continue-design-20260830.md` 长期最优设计分步实施，游戏未发布故直接改格式、无需兼容旧存档；补充要求“存档格式前瞻性”。

**改动**：
- **快照格式 v3 前瞻兼容**：`PuzzleBoardState:114` 升级到 `currentVersion=3`，新增 `canonicalId/difficultyKey/aspectLabel/createdAt/updatedAt/extra`，`PieceState` 新增 `extra`；`fromJson` 未知键进入 `extra` 并在 `toJson` 原样回写，`copyWith` 支持新字段，`effectiveCanonicalId/effectiveDifficultyKey` 兜底；`test/logic/snapshot_test.dart` 更新到 v3 并新增“v2 仍可读”与“extra 透传”回归。
- **文件级存储**：新增 `SnapshotStore:18`（`path_provider` `AppSupport/snapshots/<safeId>__<RxC>.snapshot` 原子写 `tmp->rename`、损坏自愈、按 `canonicalId::difficultyKey` 多难度隔离）与 `ProgressStore:18`（`SharedPreferences jigsaw_progress_v3_<safe>` 轻量索引：`hasSnapshot/activeDifficultyKey/snapshotKeys/progressPercent` 等，`extra` 透传）。
- **GameRepository 门面**：`init` 同时初始化两新 Store；`updateLevel/Daily/CustomProgress` 修复 `copyWith clearSnapshot` 失效、同步写文件与索引、通关/放弃时删文件；新增 `canonicalForLevel/Daily/Custom/Pack`、`loadXxxSnapshot/hasXxx`、`updateGenericProgress` 供扩展包；`resetAllData` 清空 `snapshots` 目录；`deleteCustomPuzzle` 清对应快照。
- **可靠保存**：`GamePage:41` 混入 `WidgetsBindingObserver`，`didChangeAppLifecycleState(paused/hidden/detached/inactive)` 立即 `flush`；`PopScope(canPop:false)` 拦截系统返回；`dispose` 移除观察者并 `flush`；`onProgressChanged/onStateUpdated` 均走 `_scheduleSave(800ms debounce)`，`_doSave` 基于 `solvedCount` 实时计算 `percent` 并经 `GameRepository` 落文件，覆盖自由摆放未吸附位移；`_handleSolved` 走通用 `canonicalId` 分支并取消 debounce；`_playNextLevel` 优先从文件读快照。
- **显式二选一**：新增 `ContinueDialog:9`（缩略图/进度条/用时/规格/`SegmentedButton` 多难度、二次确认重开）；`Home/Daily/My/Pack` 四入口进入前先查 `SnapshotStore`/`ProgressStore`，有 `hasFile(0<percent<100)` 则先弹 `ContinueDialog`，`继续`载入对应 `difficulty` 的 `initialSnapshotJson`，`重开`删文件清索引后新局；无存档则走 `ChooseDifficultySheet`（其 `savedProgressPercent`/`onResetProgress`/`onStart` 均改为读文件优先、清文件兜底）。
- **扩展包覆盖**：`PackLevelsPage:81 _openLevel` 改用 `canonicalId=level.id`（`pack:xxx`）、`ProgressStore` 索引与 `SnapshotStore` 文件，支持继续/重开与多难度。
- **验证**：`flutter analyze` 仅剩 11 个 `use_build_context_synchronously` 提示（已加 `mounted` 守卫）；`flutter test` 113 项全通过（含新增 2 项 v3 回归）；`flutter build windows --debug` 成功。

**前瞻性说明**：新增字段均为可选带默认值，未知键 `extra` 双向透传，旧版本读新快照不丢不崩，新版本读旧快照自动补 `canonicalId/difficultyKey`；后续新增如 `boardTheme/pieceSkin/timeLimit` 只需在 `extra` 中增量扩展或加可选字段，无需破环。

---

## [2026-08-30 12:09 GMT+8] 清除 STUCK-DEBUG 调试日志；修正 maxZoom 下限逻辑（碎片尺寸 + minZoom 下限）

**修改文件**：`lib/game/jigsaw_puzzle_game.dart`、`lib/logic/engine/puzzle_engine.dart`、`test/game_layout_test.dart`

**改动**：
- 全量移除 `[STUCK-DEBUG]` 调试日志（保留正常的 `dragEnd/BoardSlot snap/organizeTray` 等业务日志），并清理随之不再使用的 `package:logging` 导入。
- `_maxZoom` 修正为 **`max(minZoom, maxPieceZoomPx / max(pieceSize.x, pieceSize.y))`**：新增公开常量 `minZoom = 2.0`（下限，保证大块/稀疏拼图也能放大贴边）、`maxPieceZoomPx = 72`（碎片尺寸目标）。修复“72px 目标小于原始格子 → maxZoom<1 完全无法放大”的缺陷。
- 同步更新 maxZoom 派生测试，改为引用 `JigsawPuzzleGame.minZoom / maxPieceZoomPx`，不再写死数值。

**校验**：`flutter analyze` 无 Error；`flutter test` 111 项全通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 11:28 GMT+8] 缩放上限改为按“碎片最大尺寸”动态推导；吸附参数手调

**修改文件**：`lib/game/jigsaw_puzzle_game.dart`、`lib/logic/engine/puzzle_engine.dart`、`test/game_layout_test.dart`

**改动**：
- 移除写死的 `_maxZoom = 3.0`，改为按“放大后最大碎片边长 ≤ `_maxPieceZoomPx`(200px) 屏幕像素”反推：`_maxZoom = max(1.0, _maxPieceZoomPx / max(pieceSize.x, pieceSize.y))`。碎片越小(密集拼图)能放大越多，越大(大块)自然收紧，任何碎片不会被放大到超过 200px。
- 吸附参数手感手调：`PuzzleEngine.defaultSnapRatio` `0.48 → 0.40`、`effectiveSnapDistance().maxScreenPx` `44 → 48`（配合“锁定即贴槽”一致性）。
- 同步更新测试：原“缩放最大倍数严格限制 300%”改为“按碎片最大尺寸动态推导并 clamp 在 maxZoom”；各依赖写死 3.0/2.0/2.5 的缩放测试改为以 `game.maxZoom` 为准；吸附容差断言上限由 44px 改为 48px。

**校验**：`flutter analyze` 无 Error；`flutter test` 111 项全通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 10:38 GMT+8] 新增存档与续玩长期最优设计（继续 / 重新开始）

**新增文档**：`docs/save-resume-continue-design-20260830.md`

**背景**：调研发现“玩到一半退出再进需有继续/重开”现有仅部分可用：写时机仅绑吸附、后台杀进程丢档、清档 `copyWith` 失效、难度切换静默丢弃、扩展包无存档、单大JSON膨胀等问题（详见设计 §2）。

**设计要点**：
- 主键 `CanonicalId + difficultyKey(RxC)` 隔离多难度并存，统一覆盖 `main/daily/event/pack/ugc`。
- `PuzzleBoardState v3`（`canonicalId/difficultyKey/checksum/gzip` 可选）落文件 `<AppSupport>/snapshots/<safeId>_<RxC>.snapshot` 原子写，`ProgressStore` 轻量索引落 `SharedPreferences`。
- `GamePage SaveLifecycleObserver`：吸附/空闲/切后台/PopScope/dispose 全覆盖，800ms debounce + 后台立即 flush。
- 显式 `ContinueDialog` 二选一（缩略图/进度/用时/多难度Tab），重开二次确认；列表仅读轻量索引。
- `MigrationService` 从旧 `jigsaw_level_*/jigsaw_daily_*/jigsaw_custom_list` 自动迁移，文件损坏 checksum 自愈，预留 `SnapshotBackend` 云同步接口。
- 分五阶段落地（底座→可靠保存→UI→全来源→加固），详见文档 §14。

**校验**：仅新增设计文档，未改代码；`flutter analyze` / `flutter test` 无需执行。

---

## [2026-08-30 10:29 GMT+8] 修复“锁定但未被吸附/视觉未贴边”的状态·视觉脱节（isSolved 与吸附阈值不一致）

**修改文件**：`lib/game/jigsaw_puzzle_game.dart`

**背景**：独立边缘碎片落在离正确槽位有一段距离处，被 `isLocked` 锁定却**没有自动吸过去、没有绿色边框、视觉未贴边**。根因：锁定原用 `PieceState.isSolved`（逐轴误差 ≤0.035），而吸附/位移用欧氏距离 ≤ `effectiveSnapDistance()`——两者阈值不一致，导致碎片可在“逐轴接近槽位(<0.035)但欧氏距离超出吸附阈值”的区带被锁定，但没真正吸附（无位移方向、无绿框），造成状态与视觉脱节。

**改动**：
- `updatePiecesStateAndPriorities` 锁定判定由 `isSolved && planted` 改为 **`planted && 到槽位欧氏距离 ≤ effectiveSnapDistance()`**（与 `resolveSnap` 使用同一阈值），实现“锁定 ⟺ 真正被吸附就位”。
- 锁定时**强制把视觉对齐到槽位**（`comp.position.setFrom(_normalizedToScreen(nx,ny))` + `scale=_zoom`），并在锁定转场补触发绿色吸附边框（`triggerSnapGlow`），保证“锁定即已贴槽、有绿框”。
- `game_layout_test.dart` 新增“边缘碎片逐像素逼近槽位”回归测试：从正确槽位左右各 80px 逐像素扫描，断言**任何时刻 `isLocked` ⟹ 视觉已贴槽**，且远离槽位不锁定、可被扫把回收；旧代码会在“逐轴 <0.035 但欧氏超阈值”的窄带区触发失败。
- `game_layout_test.dart` 新增 5 类典型场景覆盖：「#1#2 中间碎片及其中间组合可自由拖动(不锁定)」「#3#4#5 边缘/边缘组吸附锁定不可动、零散碎片补全边缘组合被吸附锁定」。

**校验**：`flutter analyze` 无 Error（仅既有 `_showPauseMenu` 未使用警告）；`flutter test` 111 项全通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 10:24 GMT+8] 恢复“缩放无关、带硬上限”的吸附容差 effectiveSnapDistance()

**修改文件**：`lib/game/jigsaw_puzzle_game.dart`、`test/game_layout_test.dart`

**背景**：放大棋盘后，碎片在离正确槽位仍有较大距离时就被吸附并锁定（“离槽位还有距离却被吸附/锁定”），或被锁定但视觉未跟上。根因：原吸附容差是归一化常量 `0.48 * min(1/cols,1/rows)`，在屏幕像素上随 `_zoom` 等比放大，放大后吸附半径暴涨、吸得太远。

**改动**：
- 恢复 `JigsawPuzzleGame.effectiveSnapDistance()`：屏幕像素吸附半径恒定并设硬上限 44px，缩放越大归一化阈值越小；1× 且单元格像素×0.48<44px 时与原始 0.48 行为一致，无手感回退。
- `handlePieceDragEnd` 与 `hint` 两处 `resolveSnap` 均改传 `customSnapDistance: effectiveSnapDistance()`。
- `game_layout_test.dart` 新增：放大后归一化阈值随放大收紧、吸附半径屏幕像素不超过 44px 的回归测试。

**校验**：`flutter analyze` 无 Error；`flutter test` 108 项全通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 10:13 GMT+8] SoundService 播放埋点升级（排查“棋盘自己响声音”）

**修改文件**：`lib/services/sound_service.dart`

**改动**：
- `play()` 每次实际播放日志由 `fine` 升为 `info`（release 默认可见），输出 `file / vol / sfx / caller`。
- 新增 `_caller()`：从 `StackTrace.current` 提取顶层 UI 调用方（`lib/xxx.dart:行号`），把“音效”和“触发操作”对上，便于定位自响来源；可用 `SoundService._logCaller=false` 关闭。
- 被 80ms 节流丢弃的 `snap` 记 `fine` 级日志，便于发现高频 snap 重复请求。

**校验**：`flutter analyze` 无 Error；`flutter test`（snap/game_layout）全通过。

---

## [2026-08-30 10:12 GMT+8] 吸附/锁定改为“连通到边缘装配体”规则（参考 WebJigex 设计）

**修改文件**：`lib/logic/engine/puzzle_engine.dart`、`lib/game/jigsaw_puzzle_game.dart`、`test/logic/snap_algorithm_test.dart`

**背景**：上一版“局部锚定（边缘或邻接已就位片）”仍允许“两片内部碎片互认资质”形成小悬浮岛。经与成熟网页版 WebJigex（`temp/code/jigex-prog.pretty.js`）比对确认其设计：碎片永不锁死、吸附须有邻居，`isEdge` 只用于边缘筛选/装配识别。据此将判定升级为**连通性**规则：只有“已就位且 4-连通分量内含边缘碎片”的碎片（即从边框长出的装配体）才可锁定不可移动；未连通边缘的内部岛屿（单块或多块拼合）始终可自由拖动。

**改动**：
- `puzzle_engine.dart`：去掉 `isPlacementAnchored`，新增 `computePlantedPieceIds`（对 isSolved 碎片做 4-连通分量 BFS，分量含边缘片即视为“已植入”集合）与 `canSnapCluster`（集群含边缘片，或成员目标正交邻格已被植入装配体占据，才允许吸附）。
- `resolveSnap` 阶段一改用 `canSnapCluster` 门控：孤立内部碎片/岛屿不吸附到槽位、保持自由可拖动（对应 WebJigex“无邻居不吸附”）。
- `jigsaw_puzzle_game.dart` `updatePiecesStateAndPriorities` 锁定判定改为 `isPieceSolved && planted.contains(id)`：内部岛屿即便拼合成自由集群也绝不 `isLocked`。
- `snap_algorithm_test.dart` 新增/改写：「孤立内部碎片…不吸附锁定（连通到边缘规则）」「内部两片岛屿即使都位正且相邻，不连通边缘也不吸附锁定（4×4 真·内部岛）」「边缘碎片可独立吸附；与已就位装配体相邻的内部碎片也可吸附」。

**校验**：`flutter analyze` 无 Error；`flutter test` 107 项全通过；`flutter build windows --debug` 成功。

---

## [2026-08-30 10:00 GMT+8] 修复“孤立中间碎片在正确位置被锁死/粘死”及“扫把无法归位”

**修改文件**：`lib/logic/engine/puzzle_engine.dart`、`lib/game/jigsaw_puzzle_game.dart`、`test/logic/snap_algorithm_test.dart`、`test/game_layout_test.dart`

**背景**：托盘模式下放大棋盘后，玩家会发现个别碎片“异常吸附在棋盘上”（既不在边缘、也无任何邻居却单独被锁定），导致无法移动、扫把也归不了位。日志（`temp/buglog.txt`，6×8 棋盘 id=34, r=4,c=2）证实：该碎片恰好落在自己的正确槽位 `(0.25, 0.6667)`，被 `resolveSnap` 阶段一按“靠近自身正确槽位即吸附”规则单独锁定成“空中孤岛”。这不符合现实拼图规则——碎片只有在“边缘”或“与已就位碎片相邻”时才应落地。

**改动**：
- `puzzle_engine.dart` 新增锚定判定 `isPlacementAnchored`（含 `isBorderPiece` 边界判断）：/碎片若处于棋盘边缘，或与某个已就位（正确槽位）的正交相邻碎片相接，才算“锚定”；同集群成员之间不互认（避免孤立岛两两互认资质）。
- `resolveSnap` 阶段一（棋盘槽位吸附）增加锚定门控：整个待吸附集群若未锚定，则拒绝吸附到槽位、保持游离可移动（仍可在阶段二与邻居试合并）。
- `jigsaw_puzzle_game.dart` `updatePiecesStateAndPriorities` 的锁定判定改为 `位置正确 && 锚定`：即便某碎片恰好位于正确槽位，只要无锚定就不得 `isLocked`，从而可被拖动/被扫把回收。
- 新增复现/回归测试：
  - `snap_algorithm_test.dart`：「孤立内部碎片即使位置正确也不吸附锁定」「边缘碎片可独立吸附；与已就位碎片相邻的内部碎片也可吸附」。
  - `game_layout_test.dart`：「棋盘上网格相邻的两块自由碎片松手即被自动合并成集群，且扫把无法归位」，记录既有“合并成集群后扫把只回收 clusterSize==1”行为基线。

**校验**：`flutter analyze` 无 Error；`flutter test` 106 项全通过。