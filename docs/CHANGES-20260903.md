# CHANGES-20260903

## Pets / Animals / Birds / Food 新增广角特写场景与非纯色背景纹理约束（2026-09-03 09:24 GMT+8）

- **背景与原因**：
  - 用户提出需求：为 `Pets`、`Animals`、`Birds`、`Food` 四个标签增加大比例特写场景，要求主体占据绝大部分画面空间（宛如近距离广角特写），同时背景不需事无巨细、但绝对不能出现大面积纯色/死色块；保留原有经典构图（采用策略 A 混合多样性）。
- **改动清单**：
  - **摄影提示词库 [`scripts/my_prompt_library_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_v5.json)**：
    1. **`Pets`**：保留原 3 条饱满中景模板，新增 2 条广角大特写模板（`filling 85 percent of the frame, expressive face and nose close to the lens... surrounded by textured patterns of {env_fg} with rich non-solid color depth...`）；配套追加 `wide-angle close-up perspective, small aperture f/11, deep focus...` 光圈镜头词。
    2. **`Animals`**：保留原 3 条模板，新增 2 条沉浸式广角近摄模板（`powerful face and eyes close to the camera... strictly no flat monochrome background...`）；配套追加 `wide-angle close-up lens, small aperture f/11, deep focus from nose to background...` 光圈镜头词。
    3. **`Birds`**：保留原 3 条模板，新增 2 条近距离羽毛与栖息特写模板（`intricate plumage and sharp beak filling the frame... textured foliage patterns of {env_fg}... rich non-solid color depth...`）；配套追加 `wide-angle close-up with deep depth of field, small aperture f/11...`。
    4. **`Food`**：从全局通用对象模板中独立出专属模板，保留 2 条满幅盘面布局，新增 2 条满屏溢出与俯瞰微距特写模板（`overflowing the dish and filling 85 percent of the frame... no flat solid background...`）；引入精简版 `quality_prefix/tail`（提示词缩减 105 字符），追加 `wide-angle close-up photography, small aperture f/11...` 光学词。
  - **插画提示词库 [`scripts/my_prompt_library_illust_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_illust_v5.json)**：
    - 执行 `scripts/build_illust_library.py` 同步 4 大标签的特写构图模板与非纯色纹理约束，保持插画风格池统一。
- **验证**：
  - Dry-run 抽测 4 个标签，特写场景出图率约 52%~70%，完美契合“策略 A 混合”设定；
  - 提示词平均长度保持在 625~661 字符健康区间，长词告警率大幅下降；
  - 经语义检查，所有特写场景均显式要求边缘具备自然质感与丰富色彩变化，彻底规避了纯单色与虚化死区。

## v5 素材批量生图脚本与提示词库全方位优化与缺陷修复（2026-09-03 09:15 GMT+8）

- **背景与原因**：
  - 用户要求审查并改进 `scripts/` 下的 batch v5 脚本及相关数据。
  - 经全面排查，发现存在 CLI 参数 `--resume` 崩溃报错、错把极乐鸟当花卉、动物气候组覆盖不全（活火山错配）、摄影库部分标签缺失镜头光学词导致虚化死区、构建脚本历史脏数据残留等关键问题。
- **改动清单**：
  - **调度脚本 [`scripts/my_comfyui_batch_gen_v5.py`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_comfyui_batch_gen_v5.py)**：
    1. **CLI 参数兼容修复**：显式增加 `--resume`（默认开启）与 `--no-resume`，彻底消除用户传入 `--resume` 时的 `unrecognized arguments` 崩溃报错。
    2. **新增 `--prune-missing` 联动补生成机制**：断点续跑时支持自动检测磁盘已被删除的残次图片，将其从进度中剔除并自动重新加入生成队列，打通“人工选图淘汰 -> 自动补齐配额”闭环。
    3. **磁盘容量预检**：启动前通过 `shutil.disk_usage` 检测输出目录剩余空间，低于 5.0 GB 时打印醒目警告。
    4. **增强 Metadata 记录**：`Job` 与 `_metadata/*.jsonl` 中新增 `subject` 独立字段，方便下游分析；多图场景下支持逐图记录 metadata，解决 `jigsaw_qa_report.py` 匹配落空问题。
    5. **Windows 文件操作容错**：`save_progress` 增加 3 次 `OSError` 争用重试机制。
  - **摄影提示词库 [`scripts/my_prompt_library_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_v5.json)**：
    1. **常识性错词与重复清洗**：修复 `Flowers` 中将天堂鸟花误填为 `male bird-of-paradise...` 的严重问题，更正为 `"vibrant bird-of-paradise flower (Strelitzia) in full bloom"`；将 `Flowers` 中与 `Seasons` 重复的 `lavender field` / `tulip field` 优化为特写花束。
    2. **剔除强拖分风格词**：从 `style_pools.illust` 中移除 Test2 物理实测负收益最高的 `"intricate layered digital illustration with dense ornamental detail"`（$\Delta_{adj} = -21.1$ 分）。
    3. **补全 Animals 气候与环境分组**：新增 `farm`（家禽家畜）、`garden_micro`（微距昆虫）、`australia`（澳洲群落）、`wetland`（水生两栖）4 组环境，全量覆盖全部 134 个动物主体（未映射数由 53 降为 0）；移除 `cat_env` 中的 `smoking active volcano`，防止家畜配火山。
    4. **补充 Optics 防虚化约束**：为写实摄影分类 `Flowers` 与 `Others` 补全 `small aperture f/8, deep focus` 等镜头景深约束，抑制大光圈散焦死区。
    5. **环境词去方位化**：清洗 `catalog_env_partners` 中自带的 `foreground` / `background` 前缀，消除模板组装时的方位语义倒错。
    6. **模板优化**：移除 Animals 自定义模板中的 `"in the distance"` 留白词；修复全局 `object[0]` 缺少 `{env_bg}` 的问题。
  - **插画提示词库与构建脚本**：
    1. **[`scripts/my_prompt_library_illust_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_illust_v5.json)**：同步 Flowers 错词修正、跨标签去重、Animals 环境分组与环境词清洗。
    2. **[`scripts/build_illust_library.py`](file:///c:/Home/Projects/jigsawpuzzle/scripts/build_illust_library.py)**：重构为纯净无污染的 Direct Clean Transformation 逻辑，彻底清除过时脏数据和历史商标，确保词库构建 100% 可重复、可复现。
  - **使用文档 [`docs/comfyui-batch-generation-guide-v5.md`](file:///c:/Home/Projects/jigsawpuzzle/docs/comfyui-batch-generation-guide-v5.md)**：
    - 更新参数表，补充 `--resume`、`--no-resume` 与 `--prune-missing` 说明，修正 `--clean-every` 默认值为 100。
- **验证**：
  - 备份先行：原文件全部备份至 `temp/backups/*_20260903_091200.*`。
  - 语法编译：`py_compile` 0 语法错误。
  - 完整 Dry-run 验证：
    - `python scripts/my_comfyui_batch_gen_v5.py --resume --dry-run-count 5` 正常通过，0 报错；
    - `python scripts/my_comfyui_batch_gen_v5.py --no-resume --dry-run-count 1` 正常通过；
    - `python scripts/my_comfyui_batch_gen_v5.py --library scripts/my_prompt_library_illust_v5.json --dry-run-count 3` 正常通过；
    - 抽检 `Flowers` 与 `Animals` 生成提示词，天堂鸟花与农场/微距动物生态景深完全正确；
    - 风险词排查与重复项检测 0 命中，Animals 未映射主体数降至 0。
