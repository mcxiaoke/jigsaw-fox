# CHANGES-20260903

## 画幅单数据源收敛、单例内存优化与大跨度弹窗防打扰优化（2026-09-03 18:07 GMT+8）

- **背景**：
  - 针对代码 Review 指出的 3 项 P1 级与 P2 级细节缺陷（数据源分叉、每帧对象重建、大跨度过度触发及展示标签反序）进行精准修复与收敛。

- **改动清单**：
  1. **【P1-1 / 单数据源彻底收敛】`image_crop.dart` 代理 `PuzzleAspectRatio`**：
     - `kStandardRatios` 消除静态硬编码数组，直接改为代理 `PuzzleAspectRatio.values.map((a) => a.ratio).toList()`；
     - `nearestStandardRatio` 直接调用 `PuzzleAspectRatio.fromSize(width.toDouble(), height.toDouble()).ratio`，从根源消除平局分界点（如 1.15）遍历顺序不一致带来的裁切/切片判定分叉风险；
     - `cropLossFor` 直接代理 `PuzzleAspectRatio.cropLoss`。
  2. **【P1-2 / 内存与引用稳定性】`crop_puzzle_page.dart` 单例化**：
     - 将 `supportedCropOptions` 由每帧调用的 getter 改为全局不可变常量单例：`final List<CropRatioOption> supportedCropOptions = List.unmodifiable(...)`；
     - 彻底消除每帧构建 5 个新对象的 GC 开销与指针不一致风险。
  3. **【P1-3 / UX 防打扰】`choose_difficulty_sheet.dart` 大跨度弹窗判定收敛**：
     - 将原宽泛的 `>= basePieces * 2.0` 判定优化为：必须同时满足 **跨度 >= 2.5x** 且 **跨越至少 2 个难度层级**（`diff.tierIndex - baseTier.difficulty.tierIndex >= 2`）；
     - 消除正常玩家常规选择 L3/L4（如 36→100、24→96）时的频繁打扰弹窗，恢复防劝退提示原设计初衷。
  4. **【P2-1 / 存量展示文本对齐】统一宽×高展示标签**：
     - 修正 `level_item.dart:118`、`custom_puzzle_item.dart:133` 与 `game_page.dart:229` 中兜底难度的反序标签，由 `'$rows × $cols'` 统一纠正为 `'$cols × $rows'`（如 `4 × 6`）。

- **验证**：
  - 代码格式：对改动 Dart 文件执行 `dart format`；
  - 静态检查：`flutter analyze` 0 警告 0 错误；
  - 单元测试：`flutter test`（227/227 单元测试全部通过）；
  - 编译构建：`flutter build windows --debug` 成功构建。


## 画幅扩展与网格重构四方审查缺陷闭环修复（2026-09-03 17:34 GMT+8）

- **背景**：
  - 针对四份针对新代码的深度审查报告（`gmf`、`bdm`、`ds`、`msf`），对指出的即插即用解耦、排序稳定性、每日推荐一致性、逻辑复用及容错细节完成代码级排查与闭环修复。

- **修复与加固清单**：
  1. **【即插即用彻底解耦】`crop_puzzle_page.dart` 重构为纯动态 `CropRatioOption`**：
     - 彻底废除手写独立的 `enum CropRatio` 及未使用的 `targetWidth`/`targetHeight` 死字段；
     - 引入 `class CropRatioOption`，由 `supportedCropOptions = PuzzleAspectRatio.values.map(CropRatioOption.new).toList()` 纯动态派生；
     - 彻底兑现承诺：增删或注释任意画幅比例，裁切页 0 行改动，UI 选项自动联动。
  2. **【单文件解耦加固】`puzzle_model.dart` 将 `multipliers` 内化为枚举实例字段**：
     - 将静态 Map `_multipliers` 废除，直接作为 `PuzzleAspectRatio` 构造函数字段 `final List<int> multipliers;`；
     - `tiers` 访问直接遍历 `multipliers`，单文件内注释任意比例 100% 零编译报错；
     - `fromSize` 初始化 `minLoss = double.infinity`，消除平局时的首项隐式偏置。
  3. **【全序确定性与 Hash 优化】`PuzzleDifficulty` 全序排序与哈希**：
     - `_buildPresets` 在 `pieceCount` 升序基础上增加 `cols` 与 `rows` 二级、三级比较键，消除 Dart 非稳定排序在跨平台/跨 VM 时的同片数顺序抖动；
     - `PuzzleDifficulty.hashCode` 优化为 `Object.hash(rows, cols)`，消除转置尺寸（如 4×6 与 6×4）的哈希碰撞。
  4. **【业务语义精准对齐】`daily_tab_view.dart` 与 `my_center_tab_view.dart` 兜底精准化**：
     - 每日挑战入口回退难度显式获取 `PuzzleAspectRatio.square1x1.tiers.firstWhere((t) => t.difficulty.recommended).difficulty`，精准命中 1:1 正方形 36 块推荐档，消除混合 presets 升序后命中 24 块的朝向与片数偏差；
     - 个人中心 tab 活跃难度损坏/缺失时的 `presets[0]` 兜底同步改为显式正方形推荐档。
  5. **【消除重复代码】`pack_levels_page.dart` 统一复用 `ResumeHelper.diffForKey`**：
     - 删除残留的私有 `_diffForKey` 方法，统一调用公共 `ResumeHelper.diffForKey`，享受 `rows x cols` 动态字符串解析兜底。
  6. **【健壮性与容错增强】`ResumeHelper.diffForKey` 字符串解析容错清洗**：
     - 在 split 前进行 `k.trim().toLowerCase().replaceAll('×', 'x').replaceAll(' ', '')` 规范化清洗，防止外部/历史脏数据丢失精确尺寸。
  7. **【注释与文档全面同步】**：
     - 更新 `unlock_service.dart` 档位注释为 `(0~7)`；
     - 更新 `image_crop.dart`、`thumbnail_generator.dart` 头注为 5 大画幅；
     - 同步更新定稿方案 `docs/aspect-ratio-expansion-and-grid-design-20260903.md`、选图标准 `docs/puzzle-image-selection-standard.md` 与评分系统文档 `docs/jigsaw-difficulty-scoring-achievements-design.md`。

- **验证**：
  - 静态检查：`flutter analyze` **0 警告 0 错误（No issues found! 7.2s）**；
  - 单元测试：`flutter test` **全量 227/227 项测试 100% 全部通过**；
  - 平台编译：`flutter build windows --debug` **成功构建 `JigsawFox.exe`（耗时 23.5s）**。


## 画幅比例扩展方案深度审查报告落地（2026-09-03 19:00 GMT+8）

- **背景**：对未提交的 `docs/aspect-ratio-expansion-and-grid-design-20260903.md` v4.1 方案代码实现进行全量深度审查（`flutter analyze` 0 issue / `flutter test` 226 pass）。
- **产物**：新增审查报告 [`docs/aspect-ratio-expansion-and-grid-design-20260903-msf.md`](./aspect-ratio-expansion-and-grid-design-20260903-msf.md)（MSF 评级 B+，3 项 P1 需修复后合入、6 项 P2 建议）。
- **核心结论**：几何/管线/经济零 P0 事故，风险集中于每日推荐错位（`daily_tab_view:171` 全局 `presets.firstWhere(recommended)` 误命中 24）、排序不稳定（`List.sort` 非保证稳定）及非标尺寸评星阈值漂移，均已给出 5 行内修复方案。
- **验证**：`flutter analyze` 0 告警；`flutter test` 失败 2 项为依赖内网测试服务器的 `content_manager_test` 集成用例（`git stash` 回退前同样失败，确认为环境噪音）。

## 画幅比例扩展与自适应纯正方形切片网格重构落地（2026-09-03 17:07 GMT+8）

- **背景与原因**：
  - 依照定稿方案 [`docs/aspect-ratio-expansion-and-grid-design-20260903.md`](file:///c:/Home/Projects/jigsawpuzzle/docs/aspect-ratio-expansion-and-grid-design-20260903.md)，扩展原生画幅支持（三大黄金比例家族：1:1, 2:3/3:2, 3:4/4:3），统一管线标准，消除纯几何切片变形；
  - 重构自适应网格模型，严格遵循“纯正方形碎片（$W=H$）约束”，构建 L1~L7 阶梯阶数与难度推荐；
  - 彻底解耦硬编码，实现即插即用（Plug & Play）架构；
  - 底层全链贯通 L7 宗师档（576~600 块），常规 UI 隔离不展现，自制/已有快照放行防吞；
  - UGC 裁切页全面极简化（去除 icon，纯数字比例文本）。

- **改动清单**：
  - **[`lib/logic/puzzle_model.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/puzzle_model.dart)**：
    1. `PuzzleAspectRatio` 新增 `portrait3x4('3:4 竖屏', 3, 4)` 与 `landscape4x3('4:3 横屏', 4, 3)`，并为每个比例配置原生 `recommendedK`；
    2. 引入 `_multipliers` 纯函数乘数表驱动动态生成 `tiers`，彻底消除硬编码 switch 分支；
    3. `PuzzleDifficulty` 原生重写 `tierLevel`, `tierTag`, `tierIndex`, `estimatedMinutes`, `secPerPiece`，前 7 档数值 100% 保持现网原值不变，安全追加 L7 宗师档（28.0s / 3~5小时）；
    4. 原生重写 `operator ==` 与 `hashCode`，保留 `adaptiveForSize`；
    5. `presets` 静态单例缓存并严格按 `pieceCount` 升序稳定排序（首项 24 块，末项 600 块）。
  - **[`lib/logic/image_crop.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/image_crop.dart)**：
    1. `kStandardRatios` 扩充收录 5 种标准几何画幅比例（`1.0, 1.5, 2/3, 4/3, 3/4`），实现最小面积损失法单一数据源。
  - **[`lib/data/resume_helper.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/data/resume_helper.dart)**：
    1. `diffForKey` 增加 `rows x cols` 字符串格式解析兜底，保证即使比例或档位下线，历史快照亦能 100% 还原游玩。
  - **[`lib/logic/unified_puzzle_resolver.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/unified_puzzle_resolver.dart)**：
    1. 孤儿卡比例恢复消除三目分支，改用 `PuzzleAspectRatio.values.firstWhere` 优雅动态匹配与兜底。
  - **[`lib/services/unlock_service.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/services/unlock_service.dart)**：
    1. `kDifficultyStarImageRequirements` 前 7 档门槛严格维持不变（前 4 档为 0，L4=2, L5=5, L6=10），末尾追加第 8 项（L7 门槛 15 张 3 星）。
  - **[`lib/services/economy_service.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/services/economy_service.dart)**：
    1. `kDifficultyBaseCoins` 前 7 档原样保持，追加第 8 项 32 币；
    2. `kHintPrices` 前 7 档原样保持，追加第 8 项 45 币；
    3. `kStarBonusTable` 前 7 行原样保持，追加第 8 行 `[0, 20, 40]`（3 星总收益 72 币）。
  - **[`lib/widgets/choose_difficulty_sheet.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/widgets/choose_difficulty_sheet.dart)**：
    1. 增加 `enableL7` 可选参数（默认 `false`）；
    2. 引入 `_playableTiers` 过滤 L7（但对 `initialDifficulty.tierLevel == 'L7'` 放行防吞），`_currentTiers` 重定向至 `_playableTiers`；
    3. 解锁预加载与异步检查遍历完整覆盖 `kDifficultyStarImageRequirements.length`（8 档）；
    4. 消除大跨度弹窗中 `pieceCount == 54` 硬编码，改由基于推荐起步档的 $\ge 2.0\times$ 动态判定，弹窗文案完全动态插值。
  - **[`lib/pages/crop_puzzle_page.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/crop_puzzle_page.dart)**：
    1. `CropRatio` 支持 5 种标准画幅，彻底移除 `icon` 字段，标签改为简洁数字文本（`1:1`, `2:3`, `3:2`, `3:4`, `4:3`）；
    2. `ChoiceChip` 移除 `avatar: Icon(...)`，视觉清爽简约；保留载入时 `fromSize` 自动匹配默认选项。
  - **[`lib/pages/tabs/daily_tab_view.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/daily_tab_view.dart)**：
    1. `fallbackDifficulty` 消除 `presets[2]` 魔法索引依赖，优化为具名安全查找 `presets.firstWhere((p) => p.recommended)`。
  - **测试断言适配**：
    - `test/crop_puzzle_test.dart`：更新选项数量断言为 5，更新简洁数字比例文本断言；
    - `test/widget_test.dart`：更新 `presets.last.pieceCount == 600`，更新 3:4 与 4:3 命中断言；
    - `test/logic/star_calculator_test.dart`：更新 4:3 和 3:4 原生比例精确命中断言；
    - `test/logic/image_crop_test.dart`：更新 4:3、3:4 和 4:5 目标比例断言；
    - `test/services/economy_service_test.dart` & `unlock_service_test.dart`：循环覆盖上限扩至 8，全面验证 L7 收益与解锁门槛。

- **验证**：
  - 代码格式：对 15 个改动的 Dart 文件运行 `dart format`；
  - 静态检查：`flutter analyze` 0 警告 0 报错；
  - 全量单元测试：`flutter test`（227/227 单元测试全部通过）；
  - 平台编译验证：`flutter build windows --debug` 成功构建可执行文件（耗时 17.8s）。

## Dart 自动裁切流程引入主体感知智能裁切算法 (findSmartCropRect)（2026-09-03 14:46 GMT+8）

- **背景与原因**：
  - 此前 Dart 端在 ZIP 图包导入时仅使用纯几何居中裁切（`centerCropRect`），遇到画面偏侧主体（如三分法构图的人像、偏侧动物等）容易裁掉主体关键部位；
  - 依照需求，将 Python 端验证成熟的主体显著性能量寻优算法移植到 Dart 运行时中，作为独立函数调用，并在自动裁切流水线中默认启用。
- **改动清单**：
  - **[`lib/logic/image_crop.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/image_crop.dart)**：
    1. **新增独立函数 `findSmartCropRect(img.Image image, {required double targetRatio, int analysisLongSide = 512})`**：
       - 采用降采样分析（最长边 512，耗时仅 ~10ms），内存分配基于平铺连续 `Float32List` / `Float64List`；
       - 融合 Sobel 边缘细节梯度、HSV 色彩饱和度与暖色/肤色加权；
       - 施加弱中心先验（边缘衰减 15%），当画面全平坦纯色时自动平滑回退至 `centerCropRect`；
       - 基于 2D 积分图在 $O(1)$ 复杂度内滑动搜索主体能量最大的窗口。
  - **[`lib/logic/cache/thumbnail_generator.dart`](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/cache/thumbnail_generator.dart)**：
    1. `CropTaskParams` 与 `generateCroppedBytesFromBytes` 新增可选参数 `bool smartCrop = true`，**默认启用**；
    2. 后台 Isolate 中当 `params.smartCrop == true` 时优先调用 `findSmartCropRect`，关闭时无缝回退纯几何居中。
  - **[`test/logic/image_crop_test.dart`](file:///c:/Home/Projects/jigsawpuzzle/test/logic/image_crop_test.dart)**：
    1. 增加针对 `findSmartCropRect` 的测试组：覆盖纯色图平滑回退居中、偏左主体自动靠左、偏右主体自动靠右、偏上主体自动靠上、已匹配目标比例直接返回全图等 5 种典型场景；
    2. 增加对 `ThumbnailGenerator.generateCroppedBytesFromBytes` 在 `smartCrop: true` 和 `smartCrop: false` 两种模式下的端到端验证。
- **验证**：
  - `flutter test test/logic/image_crop_test.dart`（24/24 测试通过）；
  - `flutter analyze` 0 警告 0 报错；
  - 全量单元测试 `flutter test`（227/227 全部通过）；
  - 编译验证 `flutter build windows --debug` 顺利通过（耗时 20.4s）。

## Hive 存储改造 v4.7 全量落地（准备 + Phase1/2/3 + 清理，2026-09-03 10:05 GMT+8）

按 [docs/hive-migration-design.md](file:///c:/Home/Projects/jigsawpuzzle/docs/hive-migration-design.md) 分阶段完成业务数据从 SharedPreferences → Hive（hive_ce 2.19.3）迁移，无迁移/无双写（无存量用户）。

- **依赖**：pubspec 新增 `hive_ce: ^2.19.3`（不引入 hive_ce_flutter / 代码生成）。
- **基建（准备阶段）**：新建 `lib/data/storage_manager.dart`（3 box：`game-progress-v1` / `game-collections-v1` / `app-state-v1`；统一 `openAll` 打开点 + 损坏检测 `safeOpenBox`/`isCorruption`/`BoxCorruptException` + 两阶段备份恢复 `_quarantineBox`/`_restoreBoxFile`/`_fallbackRecreate` + 原子目录备份 `backupNow`（保留 5 份）+ `flushPendingWrites`/`closeAll`（close 前 flush）+ 内存兜底 `openAllWithMemoryFallback`）；`test/test_helper.dart`（每测独立临时目录 + mock 单例 + 备份开启变体）；`main.dart` 接入 openAll → 启动备份 A（`_recreatedBoxes` 守卫）→ 组1 init，并新增顶层持有 `AppLifecycleListener`（onHide/onInactive/onExitRequested：flush + 会话备份 B + closeAll，5 分钟节流）。
- **Phase 1（progress）**：ProgressStore 改为 Hive JSON String 单条 + 全量内存索引 `_index[cid]`；`getTotalPlayCount` 等聚合走内存；删除 `MigrationService`（调用 + `migration_service.dart` + `migration_service_test.dart` + `snapshot_store_test` 内对应用例）；**删除主线 `jigsaw_level_{i}` 整条读写路径**（`_initLevels` 读取分支 + `updateLevelProgress` 两处 setString → 统一 ProgressStore 单路 + 静态生成水合）。
- **Phase 2（collections）**：FavoriteStore/DownloadManager/自制拼图改为 `favorite:{cid}`/`material:{id}`/`custom:{id}` 逐条存储；init 一次性前缀读入内存缓存，素材按 downloadedAt、收藏按 favoritedAt 倒序；`custom:presetsInitialized` 标志（app-state 跨 box）+ 自制 ugc:{id} 进度水合 + `deleteCustomPuzzle` 级联删进度；DownloadManager 失效过滤先收集后批量删；`custom:{id}` 只落元数据（新增 `toMetadataJson`，§5.2）。
- **Phase 3（app-state）**：EconomyService/AchievementStore/GameRepository 统计改原生类型 `econ:*`/`ach:*`/`stat:*`（AchievementStore 显式前缀匹配扫描，禁 split）；新建 5 个 `reset()`（ProgressStore/EconomyService/AchievementStore/FavoriteStore/DownloadManager）+ `ProgressStore.reconcileSnapshots()`；`resetAllData` 按 §7.6 七步重写（清 box → 清快照 → 内存重置 → starter 重发 100/5 → 重建 → 保留设置 → 对账）。
- **清理**：移除 UI 层 savedSnapshotJson fallback 死代码（game_page / home_tab_view / my_puzzles_tab_view，原 3 处）；`grep jigsaw_ lib/` 仅剩 §2.1 保留的 5 个设置 key（sound/haptic/grid_preview/scatter/background）+ UI 态 `jigsaw_daily_fold_v1` / `skip_l2_gap_warning`。
- **行为变更（有意为之）**：`resetAllData` 不再清 SharedPreferences 设置（原 `_prefs.clear()` 连设置一起清）；重置后样例重新植入（恢复出厂，与现状一致）。
- **测试**：新增 storage_manager_test（往返/原生类型/损坏判定/备份恢复/keys 迭代安全）、game_progress_migration_test、game_collections_migration_test、game_state_migration_test；改造 10 个依赖 prefs 的旧测试文件迁移到 `initTestAppStorage`/`initTestStorage`。全量 `flutter test`（221+）+ `flutter analyze` 0 问题通过。

## Pets / Animals / Birds / Food 新增广角特写场景与非纯色背景纹理约束（2026-09-03 09:24 GMT+8）

- **背景与原因**：
  - 用户提出需求：为 `Pets`、`Animals`、`Birds`、`Food` 四个标签增加大比例特写场景，要求主体占据绝大部分画面空间（宛如近距离广角特写），同时背景不需事无巨细、但绝对不能出现大面积纯色/死色块；保留原有经典构图（采用策略 A 混合多样性）。
- **改动清单**：
  - **摄影提示词库 [`scripts/my_prompt_library_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_v5.json)**：
    1. **`Pets`**：保留原 3 条饱满中景模板，新增 2 条广角大特写模板（`filling 85 percent of the frame, expressive face and nose close to the lens... surrounded by textured patterns of {env_fg} with rich non-solid color depth...`）；配套追加 `wide-angle close-up perspective, small aperture f/11, deep focus...` 光圈镜头词。
    2. **`Animals`**：保留原 3 条模板，新增 2 条沉浸式广角近摄模板（`powerful face and eyes close to the camera... strictly no flat monochrome background...`）；配套追加 `wide-angle close-up lens, small aperture f/11, deep focus from nose to background...` 光圈镜头词。
    3. **`Birds`**：保留原 3 条模板，新增 2 条近距离羽毛与栖息特写模板（`intricate plumage and sharp beak filling the frame... textured foliage patterns of {env_fg}... rich non-solid color depth...`）；配套追加 `wide-angle close-up with deep depth of field, small aperture f/11...`。
    4. **`Food`**：从全局通用对象模板中独立出专属模板，保留 2 条满幅盘面布局，新增 2 条满屏溢出与俯瞰微距特写模板（`overflowing the dish and filling 85 percent of the frame... no flat solid background...`）；引入精简版 `quality_prefix/tail`（提示词缩减 105 字符），追加 `wide-angle close-up photography, small aperture f/11...` 光学词。
  - **插画提示词库 [`scripts/my_prompt_library_illust_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_illust_v5.json)**：
    - 执行 `scripts/build_illust_library.py` 同步 4 大标签的特写构图模板与非纯色纹理约束，保持插画风格池统一。
- **验证**：
  - Dry-run 抽测 4 个标签，特写场景出图率约 52%~70%，完美契合“策略 A 混合”设定；
  - 提示词平均长度保持在 625~661 字符健康区间，长词告警率大幅下降；
  - 经语义检查，所有特写场景均显式要求边缘具备自然质感与丰富色彩变化，彻底规避了纯单色与虚化死区。

## v5 素材批量生图脚本与提示词库全方位优化与缺陷修复（2026-09-03 09:15 GMT+8）

- **背景与原因**：
  - 用户要求审查并改进 `scripts/` 下的 batch v5 脚本及相关数据。
  - 经全面排查，发现存在 CLI 参数 `--resume` 崩溃报错、错把极乐鸟当花卉、动物气候组覆盖不全（活火山错配）、摄影库部分标签缺失镜头光学词导致虚化死区、构建脚本历史脏数据残留等关键问题。
- **改动清单**：
  - **调度脚本 [`scripts/my_comfyui_batch_gen_v5.py`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_comfyui_batch_gen_v5.py)**：
    1. **CLI 参数兼容修复**：显式增加 `--resume`（默认开启）与 `--no-resume`，彻底消除用户传入 `--resume` 时的 `unrecognized arguments` 崩溃报错。
    2. **新增 `--prune-missing` 联动补生成机制**：断点续跑时支持自动检测磁盘已被删除的残次图片，将其从进度中剔除并自动重新加入生成队列，打通“人工选图淘汰 -> 自动补齐配额”闭环。
    3. **磁盘容量预检**：启动前通过 `shutil.disk_usage` 检测输出目录剩余空间，低于 5.0 GB 时打印醒目警告。
    4. **增强 Metadata 记录**：`Job` 与 `_metadata/*.jsonl` 中新增 `subject` 独立字段，方便下游分析；多图场景下支持逐图记录 metadata，解决 `jigsaw_qa_report.py` 匹配落空问题。
    5. **Windows 文件操作容错**：`save_progress` 增加 3 次 `OSError` 争用重试机制。
  - **摄影提示词库 [`scripts/my_prompt_library_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_v5.json)**：
    1. **常识性错词与重复清洗**：修复 `Flowers` 中将天堂鸟花误填为 `male bird-of-paradise...` 的严重问题，更正为 `"vibrant bird-of-paradise flower (Strelitzia) in full bloom"`；将 `Flowers` 中与 `Seasons` 重复的 `lavender field` / `tulip field` 优化为特写花束。
    2. **剔除强拖分风格词**：从 `style_pools.illust` 中移除 Test2 物理实测负收益最高的 `"intricate layered digital illustration with dense ornamental detail"`（$\Delta_{adj} = -21.1$ 分）。
    3. **补全 Animals 气候与环境分组**：新增 `farm`（家禽家畜）、`garden_micro`（微距昆虫）、`australia`（澳洲群落）、`wetland`（水生两栖）4 组环境，全量覆盖全部 134 个动物主体（未映射数由 53 降为 0）；移除 `cat_env` 中的 `smoking active volcano`，防止家畜配火山。
    4. **补充 Optics 防虚化约束**：为写实摄影分类 `Flowers` 与 `Others` 补全 `small aperture f/8, deep focus` 等镜头景深约束，抑制大光圈散焦死区。
    5. **环境词去方位化**：清洗 `catalog_env_partners` 中自带的 `foreground` / `background` 前缀，消除模板组装时的方位语义倒错。
    6. **模板优化**：移除 Animals 自定义模板中的 `"in the distance"` 留白词；修复全局 `object[0]` 缺少 `{env_bg}` 的问题。
  - **插画提示词库与构建脚本**：
    1. **[`scripts/my_prompt_library_illust_v5.json`](file:///c:/Home/Projects/jigsawpuzzle/scripts/my_prompt_library_illust_v5.json)**：同步 Flowers 错词修正、跨标签去重、Animals 环境分组与环境词清洗。
    2. **[`scripts/build_illust_library.py`](file:///c:/Home/Projects/jigsawpuzzle/scripts/build_illust_library.py)**：重构为纯净无污染的 Direct Clean Transformation 逻辑，彻底清除过时脏数据和历史商标，确保词库构建 100% 可重复、可复现。
  - **使用文档 [`docs/comfyui-batch-generation-guide-v5.md`](file:///c:/Home/Projects/jigsawpuzzle/docs/comfyui-batch-generation-guide-v5.md)**：
    - 更新参数表，补充 `--resume`、`--no-resume` 与 `--prune-missing` 说明，修正 `--clean-every` 默认值为 100。
- **验证**：
  - 备份先行：原文件全部备份至 `temp/backups/*_20260903_091200.*`。
  - 语法编译：`py_compile` 0 语法错误。
  - 完整 Dry-run 验证：
    - `python scripts/my_comfyui_batch_gen_v5.py --resume --dry-run-count 5` 正常通过，0 报错；
    - `python scripts/my_comfyui_batch_gen_v5.py --no-resume --dry-run-count 1` 正常通过；
    - `python scripts/my_comfyui_batch_gen_v5.py --library scripts/my_prompt_library_illust_v5.json --dry-run-count 3` 正常通过；
    - 抽检 `Flowers` 与 `Animals` 生成提示词，天堂鸟花与农场/微距动物生态景深完全正确；
    - 风险词排查与重复项检测 0 命中，Animals 未映射主体数降至 0。
