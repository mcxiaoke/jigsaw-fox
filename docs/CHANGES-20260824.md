# 变更记录 2026-08-24

## [2026-08-24 10:20 GMT+8] 对标主流商业拼图游戏，UI/UX 易用性、美观度全面诊断与功能升级

### 变更内容
1. **对局操作与 Flame 引擎核心体验增强**：
   - **棋盘底图透视参考 (Board Ghost Watermark)**：新增 `BoardGhostComponent`，支持 0%（关闭）、20%（柔和轮廓）、45%（清晰辅助）三档快速循环切换，且与棋盘的 `_zoom` 和 `_panOffset` 自由缩放/平移保持 100% 同频对齐。
   - **实时对局状态栏**：整合实时计时器（`⏱️ mm:ss`）、已拼碎片胶囊（`🧩 18/64 · 28%`）与顶部细线动态进度条。
   - **撤销与重做 UI (Undo / Redo)**：界面顶部工具栏直观展示撤销/重做按钮，实时绑定 `UndoManager` 状态并根据可用性动态高亮/置灰。
   - **触觉与音效真实反馈**：吸附磁吸、成组合并时触发 `HapticFeedback.lightImpact()` / `SystemSound.play(click)`，通关时触发震动与庆典反馈。
   - **吸附光效与动效**：碎片吸附到位时触发 `triggerSnapGlow()` 瞬时光晕动效。
   - **对局内暂停菜单 (Pause Menu)**：支持即时查看用时与进度、快速切换音效与震动、更换桌面壁纸、重置本局及查阅新手技巧指引。
   - **通关流转与星级评定**：通关弹窗呈现 1~3 颗金星评定（依据耗时与提示使用次数）、战绩详情，并提供一键切入「下一关 (Next Level)」连贯通关流转体验。
2. **视觉系统与各页面重构**：
   - **主页 (HomeTabView)**：新增「全部关卡 / 新手入门 / 进阶探索 / 大师挑战 / 已通关 / 进行中」六大分类筛选器；关卡卡片升级为圆角浮雕阴影卡片，集成通关金星评定（⭐⭐⭐）、进行中进度条与高级磨砂锁关遮罩；顶部精美「每日挑战」推荐卡片。
   - **每日挑战 (DailyTabView)**：新增「TODAY 今日精选」摄影卡片、月度挑战全勤进度条与连续挑战天数统计（🔥 连胜天数）。
   - **我的自制 (MyPuzzlesTabView)**：重构「自制新拼图」渐变卡片与相册导入，完善自制拼图封面、规格、最佳用时与空状态友好指引。
   - **难度选择器 (ChooseDifficultySheet)**：增加难度梯度标签（入门/休闲/标准/进阶/探索/大师/专家/宗师/地狱）、预估用时、网格切线预览快捷开关及通关金星打勾。
   - **成就系统 (AchievementsDialog)**：扩充至 12 项成就勋章体系（初入拼界、拼图学徒、拼图大师、传奇、碎片收集者、专注达人、高阶挑战者等），支持未达成成就的实时进度条展示与多维度数据看板。
   - **新手玩法指南 (HowToPlayDialog)**：新增玩法技巧弹窗，系统介绍拖拽磁吸、双指缩放平移、组队拖动、底图透视、边缘筛选与一键理盘操作。
3. **自动化测试覆盖**：
   - 新增 `test/new_features_test.dart` 自动化测试套件，全面覆盖底图透视缩放、撤销重做状态回滚与重置、各弹窗组件与分类筛选交互；所有 36 项自动化测试全部全绿通过，静态代码分析 0 警告 0 报错。


## [2026-08-24 10:03 GMT+8] 拼图界面满屏壁纸背景切换、托盘纯色遮罩与原图全屏/棋盘切换功能

### 变更内容
1. **满屏壁纸背景与托盘纯色遮罩**：
   - 接入 `assets/images/bg_000.webp` 至 `bg_008.webp` 共 9 套高清风景/艺术壁纸。
   - `GamePage` 最底层采用 `Positioned.fill` 渲染 `Image.asset(_selectedBackground, fit: BoxFit.cover)`，根据屏幕与窗口比例自适应缩放并裁剪，满屏铺满除顶部 AppBar 以外的所有可视区域。
   - 优化 `TrayBackgroundComponent`，在壁纸之上覆盖一层 `Color(0x66000000)` 半透明纯色底板与 `Color(0x33FFFFFF)` 边框，消除壁纸纹理干扰，保证底部托盘碎片具备舒适清晰的对比度。
2. **壁纸选择弹窗与持久化存储**：
   - 在 `GameRepository` 中增加 `kBackgroundAssets` 常量列表与 `selectedBackground` 读写属性，将用户选择的背景持久化保存至 `SharedPreferences`（Key: `jigsaw_setting_selected_background`）。
   - 新增 `ChooseBackgroundSheet` 底部弹窗组件，以 3 列卡片网格展示 9 张壁纸缩略图与选中勾标，支持即时更换壁纸并持久化存储。
3. **AppBar 图标布局与原图全屏/棋盘切换**：
   - **最右侧图标（右一）**：`Icons.wallpaper`，点击弹出 `ChooseBackgroundSheet` 更换壁纸。
   - **右数第二图标（右二）**：眼睛图标（`Icons.visibility` / `Icons.visibility_outlined`），点击后无缝切换为居中高清原图全景覆盖模式，再次点击（或轻触屏幕任意处）快速切换回拼图。
4. **设计与架构文档同步**：
   - 在 `docs/jigsaw-puzzle-game-prd.md`（§3.2、§3.7）中写入满屏壁纸、托盘遮罩与眼睛原图切换设计规范。
   - 在 `docs/jigsaw-puzzle-game-architecture.md`（§3.11）中写入五层复合渲染管线（Wallpaper & Overlay Pipeline）与存储键。
5. **自动化测试**：
   - 补充壁纸预设配置测试、`ChooseBackgroundSheet` 9 套壁纸展示与点击回调测试，所有 29 项测试全绿通过。

## [2026-08-24 08:58 GMT+8] 关卡多难度独立通关机制与难度选择器差异化外观呈现

### 变更内容
1. **多难度独立通关与持久化体系**：
   - 在 `LevelItem`、`DailyChallengeItem`、`CustomPuzzleItem` 中增加 `completedPieceCounts: List<int>` 字段，用于记录每个关卡独立通关过的难度规格列表（如 9 块、16 块、36 块等）。
   - 在序列化/反序列化中添加向下兼容处理：若旧数据无该字段且 `isCompleted == true`，自动将默认难度纳入已通关集合，保证存档无损。
   - `GameRepository` 与 `GamePage` 联动：通关时记录具体通关难度，任一难度首通即可判定关卡解锁并开放下一关。
2. **难度选择弹窗 (ChooseDifficultySheet) 四态视觉呈现**：
   - **已通过 + 未选中**：采用柔和浅绿底色 (`#E8F5E9`)、翡翠绿边框 (`#81C784`)、深绿图标文字，右上角显著标注绿色打勾圆标 (`Icons.check_circle`)。
   - **已通过 + 选中**：采用深绿强调背景 (`#2E7D32`)、加粗边框、亮金色打勾角标，顶部标题右侧提示「已通关」标签，主按钮文案显示「重玩此难度」。
   - **未通过 + 未选中**：保持基础浅灰色卡片外观 (`#F0F2F5`)，无角标。
   - **未通过 + 选中**：深绿强调背景，主按钮文案显示「开始」。
3. **设计与架构文档同步**：
   - 在 `docs/jigsaw-puzzle-game-prd.md`（§2.1、§2.4、§3.1）中增加多难度通关规则、关卡解锁机制与选择器差异化视觉规范。
   - 在 `docs/jigsaw-puzzle-game-architecture.md`（§3.10、§4.2、§7）中写入四态渲染管线、数据表 Schema 及接口速查清单。
4. **自动化测试**：
   - 补充 `LevelItem` 多难度序列化与旧版本兼容测试、`ChooseDifficultySheet` 差异化外观与角标渲染的 Widget 测试，所有 27 项自动化测试全绿通过。

## [2026-08-24 08:46 GMT+8] 同步 Android 代码签名配置 (Signing Configs)

### 变更内容
1. **密钥配置同步**：
   - 从 `C:\Home\Projects\safenotes\android\key.properties` 复制签名密钥配置至 `android/key.properties`。
   - 确认 `android/.gitignore` 与根目录 `.gitignore` 均已忽略 `key.properties`，确保密钥安全不被版本控制追踪。
2. **Gradle 签名配置同步**：
   - 在 `android/app/build.gradle` 中添加读取 `key.properties` 的逻辑。
   - 配置 `signingConfigs.release` 支持 v1/v2 签名，并将 `buildTypes.release` 与 `buildTypes.profile` 的 `signingConfig` 指向 `signingConfigs.release`。

## [2026-08-24 08:34 GMT+8] 拼图通关锁定、棋盘边距最大化与全局自由缩放平移系统

### 变更内容
1. **通关碎片拖拽锁定**：
   - 在 `PuzzlePieceComponent.onDragStart` 中检测 `if (game.isSolved) return;`，拼图完成后禁止移动中间已拼好的拼图，保持完整画面。
2. **棋盘可用空间最大化**：
   - 优化 `JigsawPuzzleGame._computeLayout`，将左右冗余边距精简至 8px，精确扣除顶部工具栏与底部托盘间距，100% 占满剩余可用宽高，拼图区域大幅变大。
3. **全局缩放与平移（Zoom & Pan）系统**：
   - 支持最大放大到原图 1:1 分辨率（`max(3.0, image.width / boardSize.x)`），适配超多细小碎片的精细化操作。
   - **触屏支持**：双指捏合缩放、双指拖拽平移。
   - **桌面支持**：`Ctrl + 鼠标滚轮` 以鼠标光标为中心定点缩放、鼠标中键按下拖拽自由平移。
   - 坐标变换与托盘碎片拖出平滑过渡至当前的放大倍率；半透明底图精准贴合放大与平移；提供轻量悬浮倍率指示与一键重置按钮。
4. **测试验证**：
   - 补充通关锁定、边距最大化、缩放平移正逆坐标变换测试用例，所有 25 项测试全部通过，代码静态分析 0 警告 0 报错。

## [2026-08-24 08:25 GMT+8] 修复通关弹窗 (AlertDialog) IntrinsicWidth 布局断言异常

### 问题原因
- 在 `lib/pages/game_page.dart` 的 `_showVictoryDialog` 中，`AlertDialog` 内容区域的 `Image.memory` 设置了 `width: double.infinity`。
- Flutter 框架在对 `AlertDialog` 执行测量时会使用 `RenderIntrinsicWidth`，当子组件包含无限宽 (`double.infinity`) 时，`RenderIntrinsicWidth._applyStep` 触发 `Failed assertion: line 665 pos 12: 'input.isFinite': is not true` 异常崩溃。

### 修复方案
- 将 `AlertDialog` 内容包装在固定有限宽度的 `SizedBox(width: 300)` 中，并将图片尺寸限定为固定有限宽度 (`width: 300`)。
- 在 `test/widget_test.dart` 中添加了针对通关弹窗有限约束测量的 Widget 测试用例，确保无布局异常。
