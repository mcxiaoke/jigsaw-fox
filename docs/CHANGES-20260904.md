# 变更日志 2026-09-04

## 修复碎片吸附与短音效偶发丢失（起播后挂载计时器 + 自然归还免调 stop） 2026-09-04 20:40
- **问题根因**：
  此前 `_durationFor(Sfx.snap)` 误设为 60ms 极短保护期，且在 `await slot.player.play()` 发起前就启动了计时器。在 PlatformChannel 跨平台/线程调用耗时大于 60ms（正常为 40~80ms）时，计时器在播放尚未返回时提前触发并递增了 `playToken`；导致 `await play` 返回后双重检查判定 token 不匹配，再度补发 `player.stop()`，造成碎片吸附（`snap`）和落位（`place`）等短音效在大多数时候刚出声就被扼杀或完全静音。
- **修改文件**：
  - `lib/services/sound_service.dart`:
    - **计时器挂载时机纠正**：移至 `await slot.player.play(...)` 成功起播且通过代际校验后才启动，彻底消除跨通道调用时延吃掉音频播放时长的逻辑缺陷；
    - **自然归还免调 stop**：归还定时器与 `onPlayerComplete` 回调只释放内存占位（`slot.isBusy = false; slot.currentFile = null;`），绝对不调用 `player.stop()`；让音频自然完整流淌播完采样点和尾音；仅在被新声音抢占或 `stopAll()` 时才发底层 stop；
    - **槽位占用安全期校准**：短音效统一留足 150ms~250ms 安全期（`snap`: 200ms, `place`: 150ms, `tap/lock`: 200ms），长音效按采样留足余量（`win`: 1500ms, `winBig`: 5000ms），依靠 6 槽位与 LRU 自动流转。
  - `test/services/sound_service_test.dart`:
    - 同步更新测试套件中关于 `durationFor` 各音效安全期的断言。
- **校验**：
  - `flutter analyze`: 0 issues found (无任何警告或错误)；
  - `flutter test`: 259 passed (全部测试通过)；
  - `flutter build windows --debug`: 19.4s 编译构建成功。


- **修改文件**：
  - `lib/services/sound_service.dart`:
    - **P1 胜利音时长校准**：校准 `_durationFor(Sfx.win)` 为 `1300ms`（实盘 `win.wav` 78.9KB @32kHz 单声道 16bit 为 1.233s），彻底解决 Android SoundPool 与定时器过早掐断最后 330ms 胜利 sting 的截断缺陷；
    - **P1 播放前后代际双重检查**：在 `player.play()` 发起前与发起后增设双重校验守卫 `if (_generation != requestGen || slot.playToken != token)`；若锁释放至底层通道调用之间的微秒级窗口触发 `stopAll()`，立即取消起播并对在途调用补发 `player.stop()`，彻底消除竞态窗口；
    - **P2 池初始化异常复位重试**：在 `_ensurePoolInitialized()` 的 catch 块中增加 `_poolInitCompleter = null;`，防止初次建池失败后将错误 Completer 永久缓存导致后续会话永久静音，支持自愈重试；
    - **P2 锁范围细粒度化（选槽即释放）**：`_AsyncLock` 仅包裹微秒级内存槽位挑选与状态标记，绝不包含 `player.play()` 的 I/O 等待，彻底杜绝单次播放卡死导致后续所有声音排队死锁；
    - **P2 代际作废与 2.5s 超时熔断**：引入全局 `_generation` 代际与槽位 `playToken`；`player.play()` 增加 2.5s 超时熔断保护，超时或异常自动捕获并安全复位槽位；
    - **P2 即时 stopAll/dispose**：`stopAll()` 与 `dispose()` 立即递增 `_generation++`，同步取消归还计时与完成监听，使所有排队或在途的异步任务直接 no-op，彻底消灭退出页面后延迟补播；
    - **P3 懒建池机制（静音用户零开销）**：启动阶段若 `soundEnabled == false` 完全不创建原生播放器实例，节省 6 个 WAV 解码通道与 EventChannel；首次需要发声且开启声音时再通过并发安全的 Completer 懒加载建池；
    - **公开 SoundSlot**：将 `SoundSlot` 转为公开类型，支持单元测试白盒验证与无真实原生环境 Mock 注入。
  - `lib/pages/settings_page.dart`:
    - **P3 设置页关闭声音立即静音**：当用户在设置页切换关闭声音开关时，立即触发 `SoundService.I.stopAll()` 掐灭所有正在播放的长音与延迟音，并保留清脆的开关切换关前反馈音，实现即刻安静。
  - `lib/pages/game_page.dart`:
    - **P2 剥离 inactive 状态**：在 `didChangeAppLifecycleState` 中移除 `AppLifecycleState.inactive`，仅在 `paused / hidden / detached` 时调用 `stopAll()` 和保存，防止 Android 上通知栏下拉、权限弹窗等瞬时失焦误杀正在播放的胜利长音。
  - `test/services/sound_service_test.dart`:
    - 校准 `durationFor(Sfx.win)` 测试断言为 1300ms 且 `> 1200ms`；
    - 增加 5 项行为级深度单测：`stopAll` 触发代际递增、空闲槽位优先分配、全忙状态 LRU 优先抢占非胜利槽位（保护胜利音）、全胜利音槽位兜底抢占、`SoundSlot.resetSync` 状态清理与代际 token 递增（全套 12 项单测全部通过）。
- **校验**：
  - `flutter analyze`: 0 issues found (无任何警告或错误)；
  - `flutter test`: 259 passed (全部测试通过)；
  - `flutter build windows --debug`: 16.8s 编译构建成功。


- **修改文件**：
  - `lib/services/sound_service.dart`: 
    - 修复 Blocker 1（首声竞态与 AudioContext 缺失）：将 `_initPool` 转为严格异步流程并统一 await；为所有池内实例补全 `AudioContextConfig(focus: mixWithOthers)`；
    - 修复 Blocker 2（`stopAndReset` 状态竞态）：改用 `await slot.stopAndReset()`，并在内部增加 `if (wasBusy) await player.stop()` 守卫，避免对已空闲槽位无谓跨通道调用；
    - 引入内置轻量无依赖异步互斥锁 `_AsyncLock`，严格锁保护槽位挑选、抢占复用、计时归还与播放调用，杜绝同帧连点并发多任务抢占相同槽位覆盖回调；
    - 校准音效资产预估时长 `_durationFor`（依实盘 WAV 字节数精确测算：snap 60ms、place 50ms、win 900ms、winBig 4.9s 等），缩短 busy 窗口大幅降低抢占率；
    - 废除每声新建 `AudioPlayer` 导致的对象与平台通道句柄无节制泄漏架构，引入自主实现的固定容量复用池（`_kPoolSize = 6`，`_SoundSlot` 实体）；
    - 全面补齐细粒度独立节流机制（`snap 80ms`, `place 60ms`, `tap 70ms`, `switchToggle/lock 100ms`, `coinsFly 200ms`, `preview/edges 150ms`, `hint 300ms`, `win/winBig 1000ms`）；
    - 移除热路径 `_caller()` 与 `StackTrace.current` 昂贵字符串解析，播放日志降级为 `FINE`；
    - 增加 `stopAll()` 与 `dispose()` 生命周期管控接口；增加 `@visibleForTesting` 时长与节流查询辅助函数。
  - `lib/widgets/victory_dialog.dart`:
    - 将 `_statProgress` 动画监听器由匿名闭包改为具名方法并在 `dispose()` 中精准 `removeListener`，根除弹窗反复打开导致的 listener 闭包累积；
    - 移除星星发声的 `ignoreMute: true`，尊重用户的静音偏好。
  - `lib/pages/game_page.dart`:
    - 修复 Blocker 3（`_isPopping` 置位无回退）：在 AppBar 返回按钮与 `PopScope` 的 `_flushSave` 外部增加 `try-catch`，在非 mounted 或退出失败时安全重置 `_isPopping = false`，彻底消除返回死锁隐患；
    - 修复 Blocker 4（结算 Future 错误合并）：`_handleSolved` 改用 `Future.wait([rewardFuture, newAchievementsFuture])` 统一等待两项异步结算并强类型捕获，并在 `SnapshotStore.delete` 上增加 `catchError` 避免 unobserved exception；
    - 在 `dispose()` 及 `didChangeAppLifecycleState`（后台/暂停）时调用 `SoundService.I.stopAll()`，彻底清除退出或切后台后的残留延迟声音。
  - `test/services/sound_service_test.dart`:
    - 新增包含单例引用、全枚举安全性、便捷播放、`stopAll`、27 个 WAV 资产完整性、实盘校准时长验证以及独立节流表全项验证的单元测试套件（7 项测试全绿）。
  - `docs/sound-system.md`:
    - 同步更新系统架构图与技术说明，注明 6 实例固定池常驻机制及页面生命周期协作方式。
- **校验**：
  - `flutter analyze`: 0 issues found (无任何警告或错误)；
  - `flutter test`: 254 passed (原有 247 个测试 + 7 个新增测试全部通过)；
  - `flutter build windows --debug`: 18.0s 编译构建成功。

## 声音系统故障诊断归档 2026-09-04 19:40
- **诊断文档**：`docs/sound-system-diagnosis-20260904.md` [NEW] — 对本轮用户报障（连响/静默交替、胜利页卡顿、退主界面延迟响、左滑无响应、重进黑屏）的纯分析归档，未改代码。
- **核心结论**：`lib/services/sound_service.dart:166` `FlameAudio.play(lowLatency)` 每声新建 `AudioPlayer` 且永不 `dispose`，Windows `audioplayers_windows` 每实例持 `MFPlatform+MediaEngineWrapper` 导致原生句柄/`FramePositionUpdater` 每帧回调线性泄漏；MF 线程池占满后 `prepared` 30s 超时即“静默”，集中 `resume` 即“连响/卡顿”，叠加 `AppLogger StackTrace` 热路径与 `VictoryDialog` 延时声未取消共同致主线程饥饿。
- **次因**：`place/win/hint/preview` 等未节流洪峰、`winBig+3×snap(ignoreMute)` 并发 4 路 `IMFMediaSource`、`sound_service.dart:185 info` + `debugPrint` 同步 IO、`main.dart:137` 后台化 `init` 与 `AudioCache.loadAll` 抢文件、手势与 `PopScope` 被帧回调饿死。
- **关联**：`docs/sound-system.md` 现状仍描述“仅 FlameAudio.play”架构，已滞后；`lib/services/sound_service.dart:133 _logCaller=true` 与 `AppLogger sound INFO` 为 2026-08-30 排查遗留。

## 星级 D1 阈值修订:百分比改绝对次数 2026-09-04 19:30
- **文件**: `docs/jigsaw-star-rating-casual-two-track-design-20260904.md`(§4.2 及全文引用同步)。
- D1 修订:星级由「按片数百分比(1/3)」改为「提示次数绝对阈值、与片数/时间无关」——`hints ≤ 2 → 3 星;≤ 10 → 2 星;> 10 → 1 星`。理由:提示是新手辅助,高难玩家基本不用,百分比无意义且边界复杂。
- 同步更新:§0 决策表、§1.2 S1/S4/S5、§2 总览、§3 P4、§4.3/4.5、§6 清单、§7 校准、§9 待办。纯文档,无代码变更。

## 星级休闲化方案 v1.1 决策定稿(两轨制文档更新) 2026-09-04 19:20
- **文件**: `docs/jigsaw-star-rating-casual-two-track-design-20260904.md`(更新为决策定稿 v1.1)。
- 产品决策(D1~D6,2026-09-04 定):总则「第一期只做减法不加功能」;D1 星级改纯提示数制(0 提示=3 星,提示 ≥ 片数/3=1 星,时间/看原图/撤消不参与);D2 解锁不改(三星=0 提示后门槛自然变低);D3 金币与星级脱钩 + 提示降价(表:5/6/10/15/20/25/35/45→2/3/5/7/10/13/18/23)+ 每日免费 10 次(econ:dailyFreeHints);D4 挑战轨延期(涉服务端);D5 分享卡不动;D6 排行不做。UI 预估时间放宽(如 L1 1~3→约 1~5 分钟)。
- Phase 1 清单:star_calculator 纯提示制、economy_service 脱钩/降价/每日免费、victory_dialog 全星+用时弱化、game_page 结算适配、puzzle_model 文案放宽、achievement 三星→0提示通关、测试重写。纯文档,无代码变更。

## 星级体系休闲化两轨制方案(新文档) 2026-09-04 19:00
- **文件**: `docs/jigsaw-star-rating-casual-two-track-design-20260904.md` [NEW]。
- 背景:产品定位确认休闲 + 卖内容(非道具/金币变现),主流程须零打击。审视星级是否应删除 → 结论为「评价与目标解耦、拆两轨」:休闲轨(默认,完成即全星庆祝,解锁/金币/成就全部去三星化)+ 挑战轨(可选,严格阈值与徽章,不锁内容)。
- 代码事实核对:难度解锁硬性依赖「3 星不同图片数」(unlock_service.dart kDifficultyStarImageRequirements)、金币按星级阶梯(kStarBonusTable)、结算逐颗点亮星、成就含 starred 维度。
- 待评审;含待决策项 D1~D6 与 Phase 1~3 落地顺序。纯文档,无代码变更。

## 星级重校准设计文档 v2 甄别修订 2026-09-04 18:40
- **文件**: `docs/jigsaw-star-rating-recalibration-design-20260904.md`（重写，原版备份于 `temp/backups/`）。
- 依据代码核对修正初版错误：删除基于不可能存在片数(30/31、40/41、75/76…)的档位边界断崖伪命题(§3E)；补 2:3/3:2、3:4/4:3 真实档位片数与跨规格分析；删除无数据来源的 b≈1.30~1.40、L7≈1.7h 断言与循环论证；修正根因 A 语义表述(推翻 v3.3 需产品决策)；新增根因 D2(免费看原图停表漏洞)与 F(纯幂律缺固定成本)。
- 算法模型修正：评星改为按实际片数的连续函数 Base(N)=a₀+c·N^b（消除档位跳变、s/片仅 UI）；新增低档保护与 §8 P25/P30、P70/P85 校准口径；删除 §6.2 idle 结算补回预算(续玩可绕过)。
- 新增待产品决策项：语义翻转(§4.3)、跨规格公平 X1/X2(§4.5)、看原图口径 Y1/Y2/Y3(§5.4)、高阶收紧 A/B(§7)。纯文档改动，无代码变更，未运行测试。

## Studio 网页端新增实时服务端运行状态与心跳感知机制 2026-09-04 17:30
- **文件**:
  - `studio/static/index.html`: 
    - 在顶部导航 Brand 区域紧随“拼图内容打包工作台 v2.3”后新增**服务端状态指示胶囊 (`server-status-badge`)**，支持动态切换「🟢 服务在线」与「🔴 服务已断开（点击重试）」状态；
    - 新增全局**断网离线醒目警示条 (`server-offline-banner`)**，当服务端掉线时直观提示用户后台服务状态并提供“🔄 立即重试连接”按钮；
    - 给画廊缩略图卡片及大图预览弹窗绑定 `@error="handleImageError"` 事件，当图片请求失败时即刻触发服务端健康检测。
  - `studio/static/css/studio.css`:
    - 针对 `.server-status-badge` 定制在线绿意与离线呼吸微动动效（`pulse-offline`），圆润胶囊形态与系统主色调无缝贴合；
    - 增加 `.server-offline-banner` 红色醒目警示横条样式。
  - `studio/static/js/api.js`:
    - 封装导出 `checkHealth(timeoutMs = 2500)` 轻量健康探测函数（基于 `/api/health` 且禁用缓存并附带时间戳）。
  - `studio/static/js/app.js`:
    - 新增 `serverOnline`、`isCheckingServer`、`thumbEpoch` 等响应式状态；
    - 挂载页面即启动首检，并启动 3 秒周期的轻量无感心跳监测；
    - 具备**自愈刷新**能力：当检测到后端恢复在线（如用户重新运行 `python studio/server.py`），自动弹出 Toast 恢复通知并递增 `thumbEpoch`，自动促使浏览器重新拉取已裂开的缩略图，无需手动 F5 全局刷新。
- **实测与验证**:
  - `python -m unittest studio/test_studio.py` 8 项测试全部通过。
  - `python scripts/build_taxonomy.py --check` 校验通过。

## 分类体系单一事实源 (SSOT) 与跨语言代码生成器实施 (阶段一、二及 Studio 改造) 2026-09-04 17:18
- **文档规范同步**:
  - 更新 `docs/jigsaw-taxonomy-ssot-codegen-specification-20260904.md` 中依据规范引用为 `docs/catalog-tags-mapping-specification.md`。
- **阶段一：SSOT 数据源建立**:
  - `data/taxonomy.json` [NEW]: 固化全项目单一真实数据源（v3.0.0），收敛 14 个核心主标签（`main_tags`，包含 id/name/zh/icon/category_type/grid_row/order）、丰富正向正则模式库（`tag_patterns`）、中文别名速查映射表（`zh_alias_map`）以及历史目录映射（`legacy_map`）。
- **阶段二：代码生成器开发与自动化生成**:
  - `scripts/build_taxonomy.py` [NEW]: 实现 SSOT 严格数据完整性校验（14主标签数、必填字段、grid_row 1~5、正则 `re.compile` 合法性）与原子写入流水线；支持 `--check`（预检校验与同步性比对）和 `--dry-run`（试运行模式）。
  - `lib/data/constants/puzzle_tags.dart` [NEW]: 生成 Flutter/Dart 端强类型常量库（`PuzzleTagItem` 数据类、14 个独立静态常量 `kTag*`、14 主标签列表 `kMainTags`、3x5 黄金网格前台抽屉 15 项矩阵 `kHomeTags` 以及中英双向速查字典 `kTagZhToId` / `kTagIdToZh`）。
  - `studio/taxonomy.py`: 生成 Python 模块，预编译正则 `COMPILED_PATTERNS`，导出 `MAIN_TAGS`、`infer_tags_from_path()`、`normalize_tag()` 与全套历史向下兼容别名。
  - `studio/static/js/taxonomy.js` [NEW]: 生成 Web 前端 UMD 常量规范，供无网络或接口异常时离线降级使用。
- **阶段三：Studio 改造与端到端适配**:
  - `studio/static/index.html`: 引入生成的 `taxonomy.js`。
  - `studio/static/js/app.js`: 挂载分类初始化 fallback，当 `/api/taxonomy` 网络异常时自动平滑回退至本地 `window.TAXONOMY`。
  - 前台 `home_tab_view.dart` 暂缓改造，等待下一步指令。
- **实测与验证**:
  1. `python scripts/build_taxonomy.py --check`: 目标文件全量同步，校验 100% 成功。
  2. `dart format lib/data/constants/puzzle_tags.dart`: 代码格式 100% 符合规范。
  3. `flutter analyze lib/data/constants/puzzle_tags.dart`: No issues found (0 warnings, 0 errors)。
  4. `python -m unittest studio/test_studio.py`: 8 项测试全部通过（Ran 8 tests - OK）。

## 4K 大屏专属 Studio 界面重构：左侧垂直 Tag 列表 2026-09-04 17:10
- **文件**:
  - `studio/static/index.html` & `studio/static/css/studio.css`: 将原有顶部横向堆叠的标签栏重塑为**左侧独立垂直导航侧边栏 (`tag-sidebar`)**；页面升级为左右弹性双栏结构（`.studio-workspace` = `.tag-sidebar` + `.studio-main-content`）。
  - 侧边栏按列纵向平铺展示全部 15 个项目（🌟 全部 + 14 个核心 Tag），每项完整展示图标、中英双语名称与图片数量 Badge，在 4K 超宽/大屏下 0 滚动即可纵览所有大类；右侧画廊区域横向空间完全释放，可同时并排铺展数十张高分辨率拼图卡片。
- **验证**:
  - `python studio/test_studio.py` 8 项测试全部通过。
  - 本地 HTTP 服务测试 `GET /` 正常渲染侧边栏和主工作区结构。

- **文件**:
  - `studio/static/index.html` & `studio/static/css/studio.css`: 顶部 Tag 筛选栏由超长单行重构为**双行精致排布**（第一行 8 项：全部 + 大自然/动植物/载体；第二行 7 项：生活/美食/艺术/奇幻/图腾/节日），彻底消除横向滚动条，各分辨率下全量标签一览无遗。
  - `studio/static/js/app.js`: 新增 `mainTagsRow1` 和 `mainTagsRow2` 计算属性以支持双行精准分流。
  - `studio/core/image_proc.py`: 新增**服务端多级磁盘缓存**机制（缓存落盘至 `temp/studio_cache/thumbs/`），基于文件路径、mtime、文件大小与目标尺寸哈希寻址，同一图片命中缓存耗时由 72ms 骤降至 1.1ms（提速 65 倍）。
  - `studio/server.py`: `_handle_thumb` 增加 `ETag` 生成与 `If-None-Match` 协商缓存判断，未变动直接返回 `304 Not Modified`（0 字节传输）；增加 `Cache-Control: public, max-age=86400, immutable` 浏览器强缓存头。
  - `studio/static/js/api.js`: `getThumbUrl` 新增尺寸阶梯离散分桶（240/360/480/640px），防止微调滑块导致缓存雪崩击穿。
- **实测与验证**:
  1. 自动化测试：`python studio/test_studio.py` 8 项测试全部通过。
  2. 缓存基准实测：首图生成 72.36ms，二次请求命中磁盘缓存 1.11ms，HTTP 协商缓存返回 304，无内存泄漏与数据丢失风险。

- **文件**:
  - `docs/catalog-tags-mapping-specification.md`: 终结 Catalog-Tags 双层设计，统一拍平为 Tag 单层多选体系；规范 14 主 Tags + 1 "All" 的 3x5 黄金矩阵，整合无限细分词表 `tag_patterns`。
  - `studio/taxonomy.py`: 单一事实源重构。定义 `MAIN_TAGS` (14 项), 规范 `TAG_PATTERNS`, `infer_tags_from_path()`, `suggest_tags_for_image()`, `normalize_tag()` 与向下兼容别名；路径推断严格基于目录层级匹配，杜绝文件名杂质噪音。
  - `studio/core/tags_manager.py`: 适配 v3.0 标签结构，版本标为 `jigsaw-tag-v3.0-14`，完善扫描合并、待复核状态判断与原子保存机制。
  - `studio/server.py`: API 返回元数据新增 `main_tags` 与 `tags`。
  - `studio/static/index.html`: UI 统一为单层 14 主 Tag 筛选导航栏（含全部，共 15 项），批量打标下拉菜单与卡片状态对齐 14 标签；新增大图查看器底部快捷标签切换与复核开关。
  - `studio/static/js/app.js`: 重构筛选联动与多选打标状态管理，新增单张大图即时打标与复核操作。
  - `studio/static/css/studio.css`: 新增 `.tag-filter-bar`, `.tag-pill`, `.viewer-footer`, `.viewer-tag-badge` 等美化样式。
  - `studio/test_studio.py`: 全面更新单元测试断言，覆盖 14 分类定义、细分词归一化、路径推断、扫描管理与主导出器。
- **实测与验证**:
  1. `python studio/test_studio.py`: 8 项测试全部通过（Ran 8 tests - OK）。
  2. 真实图库实测：扫描 `C:\Home\Temp\Jigsaw_Organized` 现存全部 46 个真实目录（24,592 张图），100% 准确命中 14 主类，零遗漏零冲突（Colors: 760, Animals: 3837, Travel: 3101, Art: 1764, Fantasy: 1935, Flowers: 1449, Nature: 1624, Food: 1880, Holidays: 759, Landscapes: 2295, Cozy: 1394, Others: 1333, Pets: 1502, Vehicles: 959）。

- **文件**:
  - `lib/pages/event_levels_page.dart:99`
  - `lib/pages/game_page.dart:210`
  - `lib/pages/crop_puzzle_page.dart:352`
  - `lib/widgets/share_card_generator.dart:98`
  - `lib/logic/rendering/linen_texture_manager.dart:165`
  - `lib/data/storage_manager.dart:84,135,403`
  - `lib/main.dart:42`
  - `lib/pages/tabs/my_center_tab_view.dart:156`
  - `lib/data/progress_store.dart:271,625`
  - `lib/data/snapshot_store.dart:55,289,333`
- **改动**:
  1. **P19 活动覆盖**: `event_levels_page:99` `GamePage(levelIndex)` → `canonicalId: level.id` + `packTitle`，消除活动进度覆盖主线。
  2. **P01/P02/P03 显存**: `GamePage` 解码后 `!mounted` 立即 `dispose` + `try/catch` 坏图；`Crop` 拆 `Picture` + `try/finally dispose` + `offset/length`；`ShareCard` `image.dispose`；`Linen` `picture.dispose`。
  3. **P05 关窗丢档**: `storage_manager` 新增 `_pendingWrites` 队列、`putJson` 自动 `_trackWrite`、`waitPendingWrites`、`_doBackup` 前 `wait+flush`、`closeAll` 已 flush；`main onExitRequested` 先 `waitPendingWrites` 再 `backup/close`。
  4. **P16 HttpClient**: `my_center_tab_view` 加 8s 超时、`contentLength` 与流后双重 20MB 熔断（含 chunked）、`timeout 15/20s`、`finally close(force:true)`；补 `asUint8List(offset,length)`。
  5. **P21 一致性**: `ProgressStore.save` 失败回滚内存 + `reconcileSnapshots` 单条 try/catch；`SnapshotStore` `_cleanupTempFiles` 中 `tmp→snapshot` 恢复、`load` 区分 Format vs IO 仅前者删盘。
- **验证**: `flutter analyze` 0 issues, `flutter test` 247 passed。详见 `docs/IMPLEMENTATION-REPORT-20260904.md` 阶段一。


## Windows 桌面端关窗退出加速与 WebView2 运行防护 (11:08)
- **文件**:
  - `windows/runner/main.cpp`
  - `lib/services/webview_service.dart` [NEW]
  - `lib/main.dart`
  - `lib/pages/tabs/my_puzzles_tab_view.dart`
  - `lib/pages/online_image_picker_page.dart`
  - `test/services/webview_service_test.dart` [NEW]
  - `pubspec.yaml`
- **问题与排查**:
  1. **关窗卡顿与沙漏**: Windows 版关窗后出现 1~2 秒鼠标沙漏与磁盘卡顿；根因为 `flutter_inappwebview_windows` 插件在 UCRT `execute_onexit_table` 阶段触发 `coremessaging.dll` `0xc0000602 Fail Fast` 崩溃，导致系统 `WerFault.exe` 强行写入 35MB CrashDump。
  2. **WebView2 运行环境安全**: 部分精简版/LTSC Windows 系统可能未预装 Microsoft Edge WebView2 Runtime，若直接打开搜图将导致组件失败或页面卡死；且其他模块不应反向依赖 `main.dart`。
  3. **第三方库选型验证**: 深度调研与实测 `zikzak_inappwebview` 发现其 Windows 端实现严重缺失（`onDownloadStart`、`shouldOverrideUrlLoading`、生命周期均为空实现），因此保持官方稳定版 `flutter_inappwebview: ^6.1.5` 并彻底清理了无用的 `webview_flutter`、`desktop_webview_window`。
- **改动**:
  1. **瞬时退出防护**: `main.cpp` 在主消息循环退出并完成 COM 反初始化后，调用 `::TerminateProcess(::GetCurrentProcess(), 0);` 瞬时结束进程，跳过第三方 DLL 的 UCRT 退出析构冲突与 Dump 写入，实现 0ms 干净退出。
  2. **环境探测服务**: 新增 `WebViewService` 独立服务，解耦 `main.dart`；启动时通过官方标准接口 `WebViewEnvironment.getAvailableVersion()` 静默探测 WebView2，若缺失则标记 `isOnlineSearchAvailable = false`。
  3. **不改布局 + 拦截弹 Toast**: 「我的拼图」保持原有双卡片外观；当检测到无 WebView2 时，点击「在线搜图」直接弹出友好 `GameToast` 拦截，阻断底层组件创建，彻底杜绝崩溃与无限转圈。
  4. **专项单元测试**: 新增 `test/services/webview_service_test.dart`（5项测试全部通过），全量覆盖多平台与各种异常分支。
- **验证**:
  - `dart format` 代码格式化通过。
  - `flutter analyze` 0 issues。
  - `flutter test` 247 tests passed (全部通过)。
  - `flutter build windows --debug` 编译通过。

## 5项 UI/UX 改动

### 1. Windows桌面版启动窗口居中
- **文件**: `windows/runner/win32_window.cpp`
- **改动**: `Create()` 方法中通过 `GetMonitorInfoW` 获取显示器工作区，计算居中坐标替代原来的 `(10, 10)` 左上角定位

### 2. 设置 tab icon 改用主题色 + 设置页 appbar 去掉 icon
- **文件**: `lib/pages/main_screen.dart`
  - `_SettingsButton` icon 颜色从 `palette.secondaryText` 改为 `palette.brand`，与成就 `_TrophyButton` 一致
- **文件**: `lib/pages/settings_page.dart`
  - AppBar title 从 `Row[Icon + Text]` 简化为纯 `Text`

### 3. New 标记 7天判定（仅首页和每日）
- **文件**: `lib/logic/content/pipelines/daily_content_pipeline.dart`
  - 每日挑战 `PuzzleLevelItem` 创建时新增 `addedAt: DateTime(year, month, day)`，使每日关卡也能触发 isNew 判定
- **文件**: `lib/pages/tabs/daily_tab_view.dart`
  - `_buildDailyCard` 新增 `isNew` 计算（`item.isNew && !progress.isCompleted`）
  - 卡片左下角添加橙色 "New" 小徽章，与首页风格一致
- **说明**: `isNew` 逻辑不变（addedAt 7天内且未完成），首页 `_LevelCard` 已有 New 徽章无需改动

### 4. 去掉大跨度难度警告弹窗
- **文件**: `lib/widgets/choose_difficulty_sheet.dart`
  - `_handleStart` 方法移除整个 `isBigJump` 判定与 AlertDialog 弹窗块，直接启动游戏
  - 移除不再使用的 `shared_preferences` import

### 5. 首页关卡 card 进度胶囊
- **文件**: `lib/pages/tabs/home_tab_view.dart`
  - `_LevelCard` 新增右上角小胶囊：当 `progressPercent > 0 && !isCompleted` 时显示黑色半透明圆角胶囊 + 白色小字百分比

## 验证
- `flutter analyze`: No issues found
- `flutter test`: 242 tests passed
- `flutter build windows --debug`: 编译成功

## 阶段二 P1 稳定性修复 2026-09-04 16:00 (P06/P07/P08/P14/P15/P18/P20/P22)
- **文件**: `content_http_client.dart`, `download_manager.dart`, `image_cache_manager.dart`, `pack_content_pipeline.dart`, `daily_content_pipeline.dart`, `events_content_pipeline.dart`, `progress_store.dart`, `favorite_store.dart`, `game_repository.dart`, `achievement_store.dart`, `crop_puzzle_page.dart`, `event_levels_page.dart`, `victory_dialog.dart`, `content_manager.dart`, `manifest_router.dart`
- **改动**: 见 `IMPLEMENTATION-REPORT-20260904.md` 阶段二（网络全链路 `dio.download` 流式化、Zip `compute` Isolate + 2000 文件上限、`cast`→`whereType`、同步互斥锁 + manifest 缓存优先、生命周期 Build/catch/mounted 守卫）
- **验证**: `flutter analyze` 0, `flutter test` 247 passed
- **搁置**: P17 三重 OOM（难度感知解码）改动面大，需单独评审，已在报告中注明

## 阶段三 P2 体验与规范 2026-09-04 16:30 (P04/P11/P12/P13)
- **文件**: `lib/pages/tabs/home_tab_view.dart`, `daily_tab_view.dart`, `my_puzzles_tab_view.dart`, `event_levels_page.dart`, `game_page.dart`, `online_image_picker_page.dart`, `app_cached_image_provider.dart`, `app_cached_network_image_provider.dart`, `choose_difficulty_sheet.dart`
- **改动**: `asUint8List(offset,length)` 9处补齐；`Online` JS `jsonEncode` 转义；`TargetImageSize max(1,round)`；`ChooseDifficulty` 合并 Path 缓存。P10/P17/P23/P24~26 影响面大暂搁置，详见阶段报告。
- **验证**: `flutter analyze` 0, `flutter test` 247 passed（`build windows --debug` 末轮统一）

## 全量核对 2026-09-04 16:35
- `flutter analyze`: No issues found
- `flutter test`: 247 passed
- `flutter build windows --debug`: Built JigsawFox.exe 17.2s
- 3阶段共 3 commits（`26b6539`, `bab9552`, `cbb80b6`），搁置 7 项高风险需单独立项，详见 `IMPLEMENTATION-REPORT-20260904.md` 全量核对章
