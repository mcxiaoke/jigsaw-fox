# 变更记录 2026-08-26

## [2026-08-26 12:31 GMT+8] 修复托盘横向拖动与鼠标滚轮滑动支持

### 变更内容
1. **恢复托盘背景组件拖拽回调 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - 给 `TrayBackgroundComponent` 重新接入 `with DragCallbacks` 并实现 `onDragUpdate`，在托盘背景及碎片间隙区域按住拖动时驱动 `game.scrollTray(event.localDelta.x)`，提供平滑 1.0x 单一通道拖拽滚动；
   - 优化 `JigsawPuzzleGame.onPanUpdate`，限制画布空白平移仅在托盘上方棋盘区域（`pos.y < trayPosition.y`）生效，杜绝手势判定重叠。
2. **托盘滚轮与触摸板双指滚动打通 (`lib/pages/game_page.dart`)**：
   - 完善 `GamePage._onPointerSignal`：当光标悬停在托盘区域时，捕获鼠标滚轮与触摸板水平/垂直滚动手势并映射调用 `_game!.scrollTray(delta * 0.8)`，实现双端（鼠标滚轮+按住拖动）无缝滚动浏览托盘内全部碎片；
   - 全量 75 个测试全部通过，Windows 编译验证通过。



### 变更内容
根据 `temp/codereviews/code-review-fix-audit-report.md` 审查报告的核查建议，实施精准二次修补与单测强化：
1. **暂停状态与计时器遗漏补齐 (`lib/pages/game_page.dart`)**：
   - 在 `_showPauseMenu()` 开头增加 `setState(() => _isPaused = true);`，确保打开暂停菜单时计时器立即暂停走字，关闭后通过 `.then` 恢复；
   - 在原图全屏预览（`_showOriginalImage`）开启时同步置 `_isPaused = true`，点击遮罩关闭时恢复计时；
   - 在 `onStateUpdated` 回调中将引擎侧 `_game.isSolved` 状态实时同步至页面 State `_isSolved`，彻底解决通关后点 Undo 页面胜利横幅不消失、计时停摆问题。
2. **状态机动画竞态与提示防刷星规则加固 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - `resetCurrentGame()` 在重排赋值循环中显式调用 `comp.clearActiveEffects()`，彻底清空 `cancelPieceDrag` 遗留的 `MoveToEffect`，消除重置后碎片被拉回旧位置的动画竞态；
   - `hint()` 在入栈历史快照时保持 `hintsUsed: _boardState.hintsUsed`，确保 Undo 撤销仅回滚碎片位置，保留提示计数，杜绝通过 Undo 刷低提示次数以获取三星的漏洞。
3. **注释一致性与单测扩充 (`lib/logic/engine/puzzle_engine.dart`, `test/game_layout_test.dart`)**：
   - 更新 `createInitialState` docstring，移除过时的“防重叠校验”描述；
   - 新增针对“Hint 提示次数在 Undo 撤销后保留”的自动化单测；
   - 全量 75 项测试 100% 通过，`flutter analyze` 0 issues，Windows Debug 编译验证通过。



### 变更内容
针对 `temp/codereviews` 下 5 份代码审查报告指出的核心问题进行全量修复与单测覆盖：
1. **手势双事件管线与速度翻倍修复 (`lib/pages/game_page.dart`, `lib/game/jigsaw_puzzle_game.dart`)**：
   - 消除 `Listener.onPointerMove` 与 Flame `PanDetector.onPanUpdate`/`TrayBackgroundComponent` 之间的重复平移与托盘滚动分发，解决画布平移 2x 与托盘滑动 2x 问题；
   - 修复双指缩放捏合时 `cancelAllPieceDragging` 与 `cancelPieceDrag` 未清除 `_holdingPiece` 导致的“幽灵抓取跟随”缺陷。
2. **状态机清理与自适应难度分母修复 (`lib/pages/game_page.dart`, `lib/game/jigsaw_puzzle_game.dart`)**：
   - `resetCurrentGame()` 开头强制清理抓取状态（`cancelHoldingPiece` / `cancelAllPieceDragging` / `isDragging=false`），彻底消除重置后碎片“粘在鼠标光标”缺陷；
   - `_loadImage` 将自适应难度 `effectiveDiff` 持久保存为 State，所有进度分母、Header 显示、通关完成数与星级时间基准均统一使用自适应后的真实块数，消除非方形图进度超 100% 缺陷；
   - 修复 `_calculateStars()` 恒为 0 缺陷，正式接入 `_game.boardState.hintsUsed`；
   - 接入暂停对话框与结算对话框时计时器暂停逻辑（`_isPaused`），并支持读档时历史 `elapsedSeconds` 的还原；
   - 页面退出与销毁时妥善释放 `ui.Image` 显存资源。
3. **数据一致性、撤销与提示逻辑加固 (`lib/game/jigsaw_puzzle_game.dart`)**：
   - 修复 `UndoManager` 历史记录语义：在吸附与 Hint 前记录历史快照，使得通关后 Undo 正确双向回滚 `_isSolved` 与通关状态；
   - 修复 `exportSnapshotJson`：正在拖拽中的碎片回退读取原权威状态坐标，锁定碎片直接读取理论精确槽位，杜绝将动画中间态与临时拖拽坐标持久化；
   - 统一全库 `isTabletop` 判定（450.0 阈值），消除 400~450 区间模式分裂；
   - `hint()` 补传 `onBoardPieceIds` 白名单，严禁与托盘碎片发生意外合并；
   - 级联合并后全量同步所有碎片组件的 `comp.clusterId` 与 `comp.rot`。
4. **模型层与代码卫生优化 (`lib/logic/models/puzzle_state.dart`, `lib/logic/engine/puzzle_engine.dart`)**：
   - 移除 `puzzle_state.dart` 中重复定义的 `BoardTransitionResult` 与 `HintResult`；
   - 清理 `createInitialState` 中未使用的死循环与无效局部变量；
   - 给 `pieceById` 补充 `StateError` 明确报错与 `pieceByIdOrNull` 防御方法；
   - 清理 `handlePieceDragUpdate` 死代码与 `_boardGhostComp.opacity` 自我赋值。
5. **测试验证 (`test/game_layout_test.dart`)**：
   - 新增针对抓取取消、Reset 清理、通关 Undo 双向回滚、pieceById 防御、快照导出的自动化单测；
   - 全量 74 个单测 100% 通过（`flutter test` All passed），`flutter analyze` 0 Issue，Windows Debug 编译验证通过。



### 变更内容
依据 `docs/CHANGES-20260823~26.md` 汇总近三日功能更新，回写主设计文档并修正细分文档的过时描述：
- **PRD (`docs/jigsaw-puzzle-game-prd.md`)**：
  - 修正两侧工具栏结构（AppBar 右侧 4 个高频工具、Sub-Bar 5 大工具，智能提示上移置顶）；
  - 补充桌面散落模式、主装配体单向吸附、边缘闭环自动感知、失踪碎片自愈、全局 300% 缩放、窗口 Resize 自适应、无缝平铺桌板（12 款）等玩法特性；
  - 通关表现改为「保留冲压接缝/立体倒角/亚麻纹」（修正原「通关自动消除接缝」矛盾描述）；
  - 在线图库三入口（相册选图/已下载/在线搜图）与来源追踪、断点存档识别、成就/设置/玩法全屏化。
- **架构 (`docs/jigsaw-puzzle-game-architecture.md`)**：实体硬纸板 4 方案渲染管线、图层堆叠与 Column 头部布局、托盘隔离与主装配体、新增 §3.12 缩放平移/窗口自适应/桌面散落、仓储设置项与来源追踪字段。
- **细分文档**：logic 文档就位容差 `0.035` 与 Undo 容量 `30`、物理冲压主切缝 `1.2px`、在线选图双架构与三入口线框、explorer 路线图标记已落地项、内容存储文档标注方案状态与实现差异。

## [2026-08-26 10:36 GMT+8] 新增 MSF 全面代码审查报告

### 变更内容
1. **审查报告 (docs/jigsaw-code-review-20260826-msf.md)**：
   - 覆盖领域层/几何层/游戏引擎层/表现层/数据仓储层全链路，含拼图生命周期时序、吸附/合并/旋转/提示四算法精读、11×11 操作冲突矩阵、P0/P1/P2 三级 21 项缺陷清单、架构重构与三阶段修复路线图；
   - 结论：`flutter analyze` 0 Issue、`flutter test` 69 Passed 实测验证，领域与几何层扎实，风险集中于 `lib/game/jigsaw_puzzle_game.dart:95` 单体膨胀与 Flutter/Flame 双手势并发。

## [2026-08-26 10:18 GMT+8] 通关完整拼图带分割线展示 & 托盘一键整理多次点击缩放防抖与坐标同步修复

### 变更内容
1. **通关后查看已完成拼图带切线与分割线呈现 (lib/game/jigsaw_puzzle_game.dart)**：
   - 移除通关时原图水印置顶（priority 1000）和隐藏碎片边界（hideBorders）的操作；
   - 通关后棋盘上完整保留所有已归位碎片的冲压接缝切线、立体倒角高光与亚麻布纹压花质感，呈现真实完成的拼图实体质感。
2. **托盘收纳坐标同步与缩放防抖 (lib/game/jigsaw_puzzle_game.dart, lib/game/puzzle_piece_component.dart)**：
   - 在非散落托盘模式下执行一键整理（organizeTray）时，同步更新 _boardState 中碎片的托盘归一化坐标 
x, ny，彻底解决多次点击扫把时棋盘/托盘状态冲突导致的碎片变大/变小反复横跳缺陷；
   - 统一由 _realignTrayPieces 调度碎片位移与缩放，避免同一微秒内重复添加多个冲突的 ScaleEffect；
   - 优化 nimateTo 与 nimateScaleTo 动画重入判定与安全清理。
3. **测试覆盖与静态分析 (	est/game_layout_test.dart)**：
   - 新增通关碎片保留绘制及托盘一键整理多次连续点击状态稳定性测试用例；
   - 全量 69 项单元与集成测试 100% 全部通过，lutter analyze 0 警告 0 错误。

## [2026-08-26 09:58 GMT+8] 边缘碎片筛选系统改造：仅展示贴边外框碎片与托盘紧凑重排

### 变更内容
1. **碎片可见性与事件拦截机制 (lib/game/puzzle_piece_component.dart)**：
   - 增加 isFilteredOut 状态属性；
   - 当碎片被筛选过滤隐藏时，
ender(canvas) 跳过绘制，containsLocalPoint(point) 返回 alse，触摸点击与拖拽手势均自动拦截，实现彻底隐藏。
2. **边缘碎片过滤与托盘紧凑重排 (lib/game/jigsaw_puzzle_game.dart)**：
   - **过滤判定**：基于 dgeLayout.edgesFor(r, c).isBorder（即 
 == 0 || r == rows - 1 || c == 0 || c == cols - 1）判定边缘碎片；
   - **智能保留**：已归位碎片（isSolved）及与边缘碎片同集群的拼块始终保持可见；
   - **动态重排与滚动适配**：_realignTrayPieces 仅对当前可见碎片分配托盘槽位并执行平滑位移，scrollTray 动态适配可见碎片总宽度，彻底消除空白槽位与滚动断层；
   - **散落桌面适配**：在 Tabletop 模式下开启筛选同样精准隐藏未拼内部碎片，提供开阔清晰的边缘搜寻桌面；
   - **边缘拼完自动解除**：当一圈外框全部归位（isEdgeComplete）时，自动解除筛选模式，淡入恢复显示所有剩余内部碎片。
3. **状态栏交互与 Tooltip 适配 (lib/pages/game_page.dart)**：
   - 小状态栏筛选图标根据激活状态动态切换填充样式（PhosphorIconsFill.cornersOut / PhosphorIconsBold.cornersOut）与 Tooltip（仅显示边缘碎片 / 显示全部碎片）。
4. **测试覆盖与静态分析 (	est/game_layout_test.dart)**：
   - 新增针对边缘碎片过滤、内部碎片隐藏、托盘紧凑重排、边缘闭环自动恢复等场景的单元测试；
   - 全量 67 项单元与集成测试 100% 通过，lutter analyze 0 警告 0 错误。
