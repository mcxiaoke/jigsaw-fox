# CHANGES-20260902

## 界面视觉优化：顶栏图标/狐狸清理、设置页页脚精简与成就统计看板重构（2026-09-02 17:53 GMT+8）

- **背景与原因**：
  1. 主屏 AppBar 在「我的」Tab 显示的设置图标因使用彩色图片资源并叠加 `color` 导致遮罩渲染为纯黑实心色块；
  2. 主屏 AppBar 左侧狐狸图标多余，用户要求移除；
  3. 设置页底部关于区域包含拼图图标和长应用名，显冗余，用户要求仅保留版本号；
  4. 成就与统计页面顶部的统计卡片原采用 2x3 离散网格且高宽比较大，上下留白过多严重空旷。
- **改动**：
  - [main_screen.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/main_screen.dart)：
    - 移除 AppBar title 内的狐狸 Emoji 容器，保留纯净页面文字标题；
    - 将 `_SettingsButton` 中的 `Image.asset('assets/icons/setting.png')` 替换为标准矢量图标 `Icon(PhosphorIconsBold.gear, color: palette.secondaryText, size: 22)`，彻底解决实心黑色块渲染问题。
  - [settings_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/settings_page.dart)：
    - 移除底部关于区域的 `PhosphorIconsFill.puzzlePiece` 图标和 `异形拼图 Jigsaw Puzzle` 文字，仅保留居中纯净版本号 `Text('版本 1.0.0')`。
  - [achievements_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/achievements_page.dart)：
    - 实施**方案 C（两组整合信息看板）**：废弃原 6 个独立空旷小方块构成的 GridView，整合为两个主题大卡片容器（`通关与星级表现`、`游玩历程与资产`）；
    - 每个大卡片内部横向三等分对齐展示核心指标，采用带小图标的紧凑标签与 `FittedBox` 自适应等宽大号粗体数值，结构严谨饱满，杜绝空旷与留白。
- **验证**：
  - `dart format` 对有改动的文件独立格式化；
  - `flutter analyze` 静态分析 **No issues found**；
  - `flutter test` 全量测试 **181/181 全部 PASS**（含 `test/new_features_test.dart` 13/13 PASS）；
  - `flutter build windows --debug` 编译无任何报错。

## 单元与组件测试修复及设置页 Material / Wrap 布局优化（2026-09-02 17:43 GMT+8）

- **背景与原因**：运行 `flutter test` 时发现 7 个测试失败。经排查包括：业务重构后测试断言未同步更新（活动空状态狐狸文案、主屏更多选项菜单移除、主页 21 种标签分类重构）、Flutter 默认高 DPR（3.0）导致视口高度折算较小而无法点击或找到屏幕外元素、以及设置页卡片缺乏 `Material` 包装导致水波纹告警与资产卡片 Row 溢出 3.9 像素。
- **改动**：
  - [settings_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/settings_page.dart)：
    - `_buildCardContainer`：改用 `Material(color: ..., shape: ...)` 替代 `Container(decoration: BoxDecoration(...))`，彻底消除 `ListTile background color or ink splashes may be invisible` 断言；
    - `_buildPlayerIdentityCard`：资产 HUD（金币、星星、通关数）由不可折行的 `Row` 改用 `Wrap(spacing: 8, runSpacing: 8)` 自动换行，彻底消除窄屏或高 DPR 环境下的 3.9px RenderFlex 溢出；
  - [events_tab_view_test.dart](file:///c:/Home/Projects/jigsawpuzzle/test/events_tab_view_test.dart)：
    - 空状态断言同步小狐狸 IP 重构后的最新文案 `'小狐狸没找到正在进行的活动'`、狐狸 Emoji `🦊` 与 `PhosphorIconsRegular.arrowClockwise` 刷新图标；
  - [new_features_test.dart](file:///c:/Home/Projects/jigsawpuzzle/test/new_features_test.dart)：
    - `AchievementsPage`：设置 `devicePixelRatio = 1.0`，使 2000px 物理高度全量生效，所有成就勋章全部在可视范围内被检索；
    - `SettingsPage`：经组件优化后 0 告警 0 溢出无缝通过；
    - `MainScreen`：移除已被重构废弃的 `更多选项` 弹窗菜单断言，增加切换至「我的」Tab 时验证顶栏 `设置` 入口；
    - `HomeTabView`：断言由旧版难度分类升级为最新规范的 21 种主题标签（`全部`、`宠物`、`动物`、`鸟类` 等）；
    - `ChooseDifficultySheet`：设置 `Size(800, 1200)` 与 `devicePixelRatio = 1.0`，确保底部「放弃进度并重新开始」按钮处于视口内并精准触发重置回调。
- **验证**：
  - `dart format` 对改动文件格式化；
  - `flutter analyze` 静态分析 **No issues found**（0 error, 0 warning）；
  - `flutter test` 全量运行 **181/181 全部 PASS**；
  - `flutter build windows --debug` 编译无任何报错。

## 游戏界面专注度优化：移除 GamePage 顶栏收藏按钮（2026-09-02 17:26 GMT+8）

- **背景与原因**：玩家在关卡卡片点击打开「选择难度面板」时已具备大图完整预览和收藏心形按钮；进入 `GamePage` 游玩后玩家需沉浸于拼图，顶部操作区过多图标会产生视觉干扰与冗余。
- **改动**：
  - [game_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/game_page.dart)：移除顶栏 AppBar Action 列表中的收藏心形按钮及关联的无用导入（`FavoriteStore`、`GameToast`），使游戏操作区（提示、预览、边缘片筛选、重排、棋盘整理、换背景）更加清爽聚焦。
- **验证**：
  - `flutter test test/widget_test.dart`（9/9 全部 PASS）；
  - `flutter analyze` 静态分析 **No issues found**（0 error 0 warning）。

## 第三方代码审查缺陷修复与性能体验加固（2026-09-02 17:19 GMT+8）

- **背景与范围**：对照 `docs/my-tab-and-data-fields-review-20260902-dsf.md` 与 `docs/code-review-my-tab-and-data-fields-20260902.md`，对确认存在的 P0（阻断与一致性缺陷）、P1（性能劣化与逻辑漏洞）、P2（状态规范与按键交互）进行全链路修复。
- **P0 缺陷修复（数据完整性与异常容错）**：
  - **P0-1 收藏快照字段全链路透传补齐**：
    - 在 [choose_difficulty_sheet.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/widgets/choose_difficulty_sheet.dart) 的 `show()` 与弹窗内，以及 [game_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/game_page.dart)、[my_center_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/my_center_tab_view.dart) 中调用 `toggleFavorite()` 时，完整补齐 `title`, `image`, `sourceLabel`, `isLocalFile`, `aspectRatioLabel`, `author`, `tags`, `preferredDifficultyKey` 快照字段，确保自制/扩展包等数据源被删除后，我的画廊依然能以孤儿卡形式正常回退展示与清理；
  - **P0-2 孤儿卡清理物理删除**：
    - 将 [my_center_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/my_center_tab_view.dart) 孤儿卡清理处的 `clearAllSnapshots` 纠正为 `ProgressStore.instance.delete(card.canonicalId)`，彻底从磁盘移除进度记录，防止无进度僵尸卡永驻列表；
  - **P0-3 支持网络图片提取**：
    - 在 [my_center_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/my_center_tab_view.dart) 的 `_resolveImageBytes()` 中加入对 `http://` / `https://` 的 `HttpClient` + `consolidateHttpClientResponseBytes` 分支，杜绝网络图片自制/活动拼图被错误降级为兜底动物图。
- **P1 缺陷修复（性能优化与指标闭环）**：
  - **P1-1 剔除 autosave 双重全量扫描**：
    - 从 [progress_store.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/data/progress_store.dart) 的 `save()` 中剔除 `refreshAggregatesCache()`；并在 `updateProgress()` 中改为仅在通关或星级变化时按需刷新聚合缓存，消除 800ms 自动保存带来的 O(N) 性能毛刺；
  - **P1-2 统一目录内存索引缓存复用**：
    - 在 [catalog_index.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/catalog_index.dart) 中引入 `UnifiedCatalogIndex.current()` 静态单例缓存与 `invalidate()` 机制；仅在自制关卡变动或内容包更新时标记 dirty，避免 Tab 切换与数据重载时无谓遍历 6000+ 关卡；
  - **P1-3 Tab 外部刷新防抖处理**：
    - 在 [my_center_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/my_center_tab_view.dart) 的 `_onExternalChanged` 增加 300ms Timer 防抖，吸收连续广播事件；
  - **P1-4 minMoves 最少步数写入闭环**：
    - 在 [progress_store.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/data/progress_store.dart) 的 `recordDifficultyCompletion()` 增加 `moves` 参数并维护历史最小值；在 [game_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/game_page.dart) 通关结算时传入实际拼图碎片步数；
  - **P1-5 目录宽高比基于难度网格动态推导**：
    - 将 [catalog_index.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/catalog_index.dart) 中的硬编码 `square1x1` 改为 `PuzzleAspectRatio.fromSize(cols, rows)` 动态推导，支持非方形自制和扩展包正确适配画廊比例。
- **P2 缺陷修复（规范性与交互稳定性）**：
  - **P2-1 移出 build() 中的 State 突变**：
    - 从 [daily_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/daily_tab_view.dart) 的 `build()` 中移除对 `_expandedMonthKeys` 的 `add()` 操作，全部前置在 `initState()` 与 `_loadFoldPrefs()` 中完成，消除 Flutter 框架潜在的重绘死循环隐患；
  - **P2-2 Esc 按键冒泡监听**：
    - 将 [victory_dialog.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/widgets/victory_dialog.dart) 中的 `KeyboardListener` 替换为 `Focus(onKeyEvent: ...)`，解决子组件按钮抢夺焦点导致按 Esc 无法关闭弹窗的问题。
- **测试与验证**：
  - 在 `progress_store_test.dart` 补充 `minMoves` 最小值与 `delete` 清理的单元测试；
  - 在 `favorite_store_test.dart` 补充完整快照持久化读取单元测试；
  - 在 `catalog_and_resolver_test.dart` 补充 `current()` 缓存复用与 `invalidate()` 刷新单元测试；
  - 全量运行 6 个测试套件，**24/24 单元与组件测试全部 PASS**；
  - `flutter analyze` 静态分析 **No issues found**（0 error, 0 warning）；
  - `flutter build windows --debug` 构建成功。

## Victory 弹窗关闭/返回通道补齐与「我的」Tab 实时刷新修复（2026-09-02 16:36 GMT+8）

- **问题 1：玩拼图产生进度或通关后，切到「我的」Tab 为空，需重启进程才刷新**：
  - **根因**：`MainScreen` 采用 `IndexedStack` 渲染 5 个 Tab，`MyCenterTabView` 在 App 启动首帧即完成 `initState()`；当玩家在其他 Tab 游玩产生残局或通关后，切到底部第 5 个 Tab 时并不会触发 `initState()`，且此前未对 `ProgressStore` 建立响应式监听。
  - **修复**：
    - [progress_store.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/data/progress_store.dart)：新增全局响应式通知器 `progressNotifier`，在任何 `save()`、`updateProgress()`、`recordDifficultyCompletion()`、`delete()` 时自动自增广播，并在写入时同步刷新内存聚合缓存；
    - [my_center_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/my_center_tab_view.dart)：接入 `progressNotifier` 监听；引入 `isActive` 并在 `didUpdateWidget` 中检测切入当前 Tab 时立即执行 `_loadAllData()`；
    - [main_screen.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/main_screen.dart)：在挂载 `MyCenterTabView` 时传入 `isActive: _currentIndex == 4`。
- **问题 2：拼图完成界面 Victory 弹窗全屏且无关闭入口，不想玩下一关时无法退出**：
  - **根因**：此前在有下一关（`widget.onNextLevel != null`）时，操作行仅保留了 `[保存壁纸] [分享成绩] [下一关]`，将 `返回列表` 置于 else 分支彻底隐藏；且弹窗未提供右上角关闭（✕）按钮，不支持点击背景或 Esc 快捷键关闭，导致不想挑战下一关的玩家陷入无法退出的交互死胡同。
  - **修复**：
    - [victory_dialog.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/widgets/victory_dialog.dart)：
      - 右上角新增悬浮圆形关闭按钮（`PhosphorIconsBold.x`），点击直接关闭弹窗；
      - 增加 `KeyboardListener` 监听 `LogicalKeyboardKey.escape`，桌面端支持按 Esc 快捷键关闭；
      - 重构操作区为双层布局：核心行动栏明确并列提供 **`返回列表`**（直接退出关卡回列表）与 **`下一关`**（高亮主按钮）；辅助工具栏容纳 `保存壁纸`、`分享成绩` 与 `留在棋盘欣赏完整拼图`；
    - [choose_difficulty_sheet.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/widgets/choose_difficulty_sheet.dart)：优化 AppBar 标题同时渲染主标题“选择难度”与小字副标题展示关卡名称。
- **验证**：
  - `widget_test.dart`（9/9 全部 PASS）；
  - `favorite_store_test.dart`、`catalog_and_resolver_test.dart`、`progress_store_test.dart`、`my_center_tab_view_test.dart`、`daily_tab_fold_test.dart` 全部 PASS；
  - `flutter analyze` 全项目 0 error 0 warning；
  - `flutter build windows --debug` 编译成功。

## 「我的」个人中心、每日月份折叠与数据模型全量补齐（2026-09-02 16:25 GMT+8）

- **背景与目标**：
  - 彻底解决用户拼图体验中的核心诉求：支持拼图画廊式收藏、历史残局与已完成回顾；
  - 每日挑战月份支持手风琴折叠提升长列表滚动流畅度；
  - 彻底补齐数据模型在首玩、首通、末玩、末通、最少步数等维度的全生命周期字段，避免后续版本 schema 迁移阵痛。
- **数据层重构与缺失字段补全**：
  - [progress_store.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/data/progress_store.dart)：
    - `DifficultyRecord` 扩展补齐：`firstCompletedAt`, `lastCompletedAt`, `firstPlayedAt`, `lastPlayedAt`, `minMoves` 及 `isPerfect` 计算属性；
    - `ProgressStore` 扩展 `loadAllProgress()` 消除 N+1 读开销；在 `init()` 与 `save()` 维护单次全量聚合缓存（`_cachedTotalStars`, `_cachedTotalSolved`, `_cachedDistinct3Star`）；
    - `LevelProgress` 补齐时间戳，并暴露 `lastPlayedAt`, `totalPlayCount`, `completedDifficultyCount`, `allDifficultyStars`, `bestDifficultyKey` 等聚合 getters。
  - [favorite_store.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/data/favorite_store.dart)：
    - 新增 `FavoriteEntry`（包含 `canonicalId`, `favoritedAt`, `titleSnapshot`, `imageSnapshot`, `sourceLabelSnapshot`, `aspectRatioLabel`, `extra`）；
    - 实现 `FavoriteStore` 单例，提供 SharedPreferences 持久化、`idsNotifier` 响应式通知、`toggleFavorite()`、`favoritesSortedByTime()` 与孤儿清理 `pruneOrphans()`。
  - [catalog_index.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/catalog_index.dart)：
    - 新增 `CatalogEntry` 与 `UnifiedCatalogIndex`，构建主线、每日、自制、扩展包及活动专题五大源的统一元数据索引体系。
  - [unified_puzzle_resolver.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/logic/unified_puzzle_resolver.dart)：
    - 新增 `UnifiedPuzzleCardData` 与装配器 `UnifiedPuzzleResolver`，整合关卡进度、收藏状态与目录信息，并对原图已被删除的“孤儿卡”实施平滑降级（半透明、失效提示、长按清理）。
- **每日挑战月份折叠交互**：
  - [daily_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/daily_tab_view.dart)：
    - 支持月份头部点击折叠/展开，带 200ms `AnimatedRotation` 顺滑箭头与完成度徽章；
    - 状态持久化至 `daily_expanded_months`，默认展开当前月份；折叠月份直接跳过 Grid 构建提升渲染性能。
- **UI 表现层与触点联动**：
  - [my_center_tab_view.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/tabs/my_center_tab_view.dart)：
    - 新建独立的「我的」个人中心，支持「进行中」、「收藏」、「已完成」3 个子 Tab 切换与动态徽标计数；
    - 严格遵循“规则乙”（进行中按最近游玩倒序、已完成按完成时间倒序，两者独立互不排斥）；
    - 进行中优先唤起残局恢复（`ResumeHelper.tryHandleResumeFlow`），收藏与已完成点击唤起难度选择底板，支持孤儿卡长按清理；
    - 空状态优雅呈现小狐狸占位图并提供“去挑选拼图”快捷跳转。
  - [main_screen.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/main_screen.dart)：
    - 扩展为 5 Tab 结构（主页、每日、活动、自制、我的），标题联动匹配，设置按钮放置于我的页面顶部；原有自制 Tab（`MyPuzzlesTabView`）完整保留。
  - [choose_difficulty_sheet.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/widgets/choose_difficulty_sheet.dart) & [game_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/game_page.dart)：
    - AppBar 操作区接入响应式红心收藏按钮（监听 `FavoriteStore.idsNotifier`），支持即时收藏并弹出 Toast。
- **自动化测试与质量验证**：
  - 新增 `test/data/favorite_store_test.dart`（3/3 全部通过）；
  - 新增 `test/logic/catalog_and_resolver_test.dart`（3/3 全部通过）；
  - 新增 `test/my_center_tab_view_test.dart`（1/1 全部通过）；
  - 新增 `test/daily_tab_fold_test.dart`（1/1 全部通过）；
  - 增强 `test/data/progress_store_test.dart`（4/4 全部通过）；
  - 全局代码执行精准 `dart format`，无格式化报错；
  - `flutter analyze` 全库 **0 errors, 0 warnings, 0 infos**；
  - `flutter build windows --debug` 编译成功。

## GamePage 拼图画布白屏修复（2026-09-02 14:20 GMT+8）

- **问题**：进入拼图页后 AppBar 显示正常，但下方区域完全空白，连背景图与游戏画布都不显示。
- **根因**：此前在移除右侧小状态条时，误将 body `Stack` 内部唯一的非 Positioned 子组件 `Column` 替换成了 `Positioned.fill`。Flutter 规范规定：当 Stack 内所有组件均为 Positioned 时自身尺寸会塌陷为 `Size.zero`，导致铺底背景图与游戏画布全部受限为 0 尺寸无法显示。此外将进度条挂于 AppBar `bottom` 占用了额外高度且在部分平台上存在宽度约束风险。
- **修复**：
  - [game_page.dart](file:///c:/Home/Projects/jigsawpuzzle/lib/pages/game_page.dart)：将 body 结构恢复为带强尺寸约束的非 Positioned `Column`，将 `_buildProgressLine()` 进度线置于 `Column` 顶端（紧贴 AppBar 下沿），下接 `Expanded(child: GameWidget<JigsawPuzzleGame>(game: _game!))`，彻底恢复全屏尺寸；
  - 清理 AppBar 的 `bottom: PreferredSize`，为 6 个操作图标补充统一紧凑边距与 `minWidth: 38` 约束，适配窄屏手机；
- **验证**：`flutter analyze` 0 error 0 warning，`widget_test.dart` 9/9 包含 GamePage 完整真机异步挂载与 6 图标测试 PASS，`game_layout_test.dart` 38/38 PASS，`flutter build windows --debug` 编译成功。

## 主体占比定向修复：Pets/Animals/Birds（2026-09-02 14:10 GMT+8）

- **问题**（JigsawV5_full 6212 张人工抽检确认）：三个 tag 主体只占画面 10–20%，环境占大半；Pets_0008 货架毛巾、Animals_0005 丛林藤蔓为重复堆砌；Birds_0004 背景樱花虚化（bokeh 回潮）。
- **根因**（代码事实）：① animal/object 模板主体为光杆名词 `a single {subject}`，3 个 env 槽位词量为主体 ~3 倍，模型把环境当主角；② Pets `optics: None`（零主体占比指令），Animals/Birds optics 措辞弱压不过 env；③ `high angle view`/`slightly elevated view` 未屏蔽，俯视进一步压缩主体（Pets_0001 即 high angle）；④ Birds env `in the distance` + 鸟类浅景深先验，单条 `deep focus` 压不住。
- **修复**（`temp/build_v5_library.py`，F12 已验证模式：显式尺度 + medium shot + deep focus）：
  - 新增 `SUBJECT_SCALE_OPTICS`：Pets 注入 2 条（原为 None），Animals/Birds 覆写为强尺度措辞（`large in the frame and filling much of...`，Birds 加 `background fully sharp`）；措辞避开 close-up/macro/bokeh/shallow/soft 全部已知触发词。
  - 新增 `SUBJECT_SCALE_VP_DENY`：三 tag 屏蔽 `high angle view`/`slightly elevated view`（合并进 viewpoint_deny，无重复）。
- **验证**：重建 1207 subjects/91 env 全部翻译通过；risk word 断言 0 命中；JSON 抽查三 tag optics/vp_deny 生效、Food/Ocean/Transportation 等其它 tag 不受影响；dry-run 9 条 prompt 新 optics 全部出现、被屏蔽 viewpoint 不再抽中。
- **注意**：viewpoint 池收缩会洗掉这三 tag 的既有 seed（已知耦合特性），补生成需换输出目录；三 tag prompt >700 char 占比 84/269（最长 778），与全批基线（最长 765）相当。
- **备份**：`temp/backups/build_v5_library.py.bak-20260902-subject-scale`、`temp/backups/my_prompt_library_v5.json.bak-20260902-subject-scale`。
- **Round 2（14:25）optics 无效，升级为 per-tag 模板覆写**：JigsawV5_scale_fix 抽查显示 Pets/Animals 主体占比仍 10–20%（美洲豹几乎无变化、柴犬 15%→20%），Birds 有效（金刚鹦鹉 ~40%）。根因是结构性的：共享 animal/object 模板主体为光杆名词 + 3 个富 env 槽位，单条 optics 压不过 ~3:1 词量差。修复：① `my_comfyui_batch_gen_v5.py` build_jobs 增加 per-tag 模板覆写一行（`tag.get("prompt_templates") or tag_templates`）；② builder 新增 `SUBJECT_SCALE_TEMPLATES`（Pets/Animals 各 3 条主体前置模板 `one large {subject} filling most of the frame...`，env 降级为 background/sides/edges/backdrop），Birds 不覆写；object 组（Flowers/Food/Others 共享）与 animal 组（Birds）不受影响。dry-run 6 条验证主体尺度前置生效。备份 `*.bak-20260902-template-override`。**正在跑的批次是启动时加载的旧数据，Pets/Animals 任务需中止后换新目录重跑，Birds 可继续。**

- **Round 3（14:35）Birds 补课 + subject 语义修复**：用户反馈 scale_fix2 批次 Animals/Pets 大幅改善，但 Birds 三张问题图（蜂鸟 18% + 背景虚化 / 鹤望兰花冒充鸟 / 猫头鹰一家 5 只散落）。① Birds 加入 `SUBJECT_SCALE_TEMPLATES` 覆写（3 条 perched/dominating 主体前置模板，全部不含 `in the distance`）；② 新增 `SUBJECT_REWRITES_R3`：`bird of paradise`→`male bird-of-paradise with ornate iridescent plumage`（plumage 强制鸟类读法，避免渲染成鹤望兰）、`owl family`→`great grey owl`、`flamingo flock`→`greater flamingo`（多主体→单主体）。重建+dry-run 4 条验证通过；Birds prompt >700char 占 21/53（最长 800）。备份 `*.bak-20260902-birds-round3`。**scale_fix2 里已生成的 Birds 仍是旧模板产物，Birds 需单独再补跑。**

- **Round 4（14:45）quality 分组拆分（主体组放松）**：依据 QA 数据（死区中位 0.00/P90 0.00、Lap 中位 1118 vs 门槛 300——防死区词严重过量）与 Round 2/3 实证（带全量 detail 词时主体照样被环境压制），将 7 个主体类 tag（Animals/Pets/Birds/Food/Transportation/People/Sports）的 quality 拆出放松版：prefix → `deep depth of field with consistent sharpness, clear visual hierarchy, varied shape color and scale between regions`，tail → `medium-scale readable details, full-bleed artwork`；删除 `corner-to-corner dense detail`/`busy composition`/`rich micro texture variation into all four corners` 三条环境填充指令（保深度防虚化与可拼性约束）。场景类 13 tag 维持全局版不动，Art 专属覆写不变。机制复用既有 per-tag quality_prefix/tail 覆写（运行时脚本零改动）。验证：重建后 7 tag 覆写齐全、无意外扩散、dry-run prompt 已无三条填充指令且长度 <700char。回退护栏：下批 QA 主体组死区 >2% 或 Lap 中位 <400 → 删除 SUBJECT_QUALITY_TAGS 覆写块即回全局版。备份 `*.bak-20260902-quality-split`（builder + JSON 双份）。

- **Round 5（14:58）Sports 彩虹污染修复**：JigsawV5_full 批次 Sports 168/168 张全部出现彩虹，整体不可用。根因：Sports env 池仅 3 条，其中 2 条是彩虹源——`sunlight through water mist micro rainbow`（prompt 字面含 rainbow）与 `frozen water spray ice shards red clay dust`（水雾+阳光必出彩虹），且池大小=3 意味着每张 prompt 都 rng.sample 到全部 3 条=100% 注入。修复：① `docs/jigsaw-tag-subject-catalog.md` Sports 环境搭档改写为 5 条干净场馆（观众席/室内木地板球馆/红色跑道/自行车馆赛道/攀岩墙），并加注"运动场景一律避免水雾、喷淋、水花类环境词"；② builder CN_TO_EN 补 4 条新 env 翻译。重建后 env 池 5 条、rainbow/mist/spray 残留 0；全库扫描确认其余 tag 的 rainbow 词均为有意 subject（丹霞/双彩虹/彩虹渐变/彩虹毛线/彩虹鹦鹉），env 级水雾仅 Sports 中招。备份 `build_v5_library.py.bak-20260902-sports-rainbow`，目录文档走 git 回退。

- **Round 6（15:08）Sports 整体重设计（主体改特写场景）**：原 42 个 subject 为裸运动名词（soccer/tennis/surfing），在 special 模板下拼出 "a single soccer"/"a richly detailed tennis" 病句，且含脏数据（sunrise yoga silhouette 剪影一票否决、boxing and MMA 等多概念合并）。重设计为 40 个单主体装备/瞬间特写（球类 12/冰雪 5/水上 6/户外 7/身心竞技 10），全部 singular 可数、兼容 "a single {subject}" 语法，含用户点名的足球/羽毛球/自行车/滑雪/浆板。① 目录文档 Sports 主体元素重写 + 核心规避更新（单主体特写原则、禁水雾词）；② builder CN_TO_EN 补 38 条翻译。重建后 Sports catalog=40（总 catalog 1205），翻译全过；dry-run 5 条语法通顺。**已知局限**：Sports env 池仅 5 条通用场馆，无亲和映射（badminton 可能配到 velodrome 背景），如生成结果显示错配明显，下一步扩 env 池至 ~12 条并建 subject_env_group 分组（机制已存在于 build_jobs）。备份 `*.bak-20260902-sports-redesign`。

- **Round 7（15:13）放松组收窄为 3 tag**：实测放松组结果后，用户确认仅 Pets/Animals/Birds（主体占比修复三兄弟）保留放松版 quality；Food/Transportation/People/Sports 回退全局全量版。`SUBJECT_QUALITY_TAGS` 收窄并留注释记录决策依据。重建后全 21 tag 校验：3 RELAXED + 17 GLOBAL + 1 Art 专属，无扩散。

- **Round 8（15:20）Sports 环境亲和映射**：JigsawV5_sports 实测（26 张）证实 Round 6 预警的错配——**每个非球类 subject 全部错配**（滑雪板配红色跑道、桨板配室内球馆、浮潜面镜配自行车馆、冰壶配体育场），根因是 env 池 5 条全是场馆且无冰雪/水域场地。修复（复用 Animals 的 env_groups 机制）：① 目录文档 env 池扩至 16 条（新增草坪球场/室内网球馆/滑板场/山路护栏/滑雪场/冰封湖面/雪村/浅滩沙嘴/激流河谷/游艇码头/泳池泳道线 11 条），补注亲和映射说明；② builder 新增 `SPORTS_ENV_GROUPS`（court 4/track 5/snow 3/water 4，各组 ≥3 满足 rng.sample）与 `SPORTS_SUBJECT_ENV_GROUP`（40 subjects 全映射：球类+身心竞技→court、公路极限→track、冰雪→snow、水上→water），main() 注入。验证：40 subjects 无 unmapped、组内 env 全在池中、dry-run 确认 badminton→网球馆、ski→雪村、paddleboard→码头、bobsled→滑雪场。目录文档已提交 git。

- **Round 9（15:30）Sports 主体扩容（体育场之外的野趣户外特写）**：用户要求在 Round 6/8 基础上继续扩容，重点补充**非体育场类**体育项目。新增 14 个主体（catalog + CN_TO_EN + `SPORTS_SUBJECT_ENV_GROUP` 三处同步）：越野滑雪森林雪道、攀冰冰瀑冰镐、冰钓冰洞钓竿、雪地摩托深粉雪、登山冰镐冰爪冰川（→snow 5 个）；风筝冲浪浪湾、帆板顺风滑行、赛艇双桨静水、尾波滑水玻璃浪（→water 4 个）；越野跑泥泞林道、徒步山脊登山杖、定向越野检查点旗、飞盘高尔夫铁链篮、沙漠越野摩托沙丘（→track 5 个）。配套新增 8 条 env 场地（林间越野雪道/蓝色冰瀑/冰川裂缝坡→snow，白浪风湾/金秋静水河道→water，泥泞林道/多雾山脊步道/金色沙丘拉力赛道→track），使 env 池 snow 3→6、water 4→6、track 5→8（均 ≥3，无回退）。去重核对：排除龙舟（Holidays 独占）、longboard（Transportation 独占 `skateboard longboard`）；提示 Transportation 已有 `wooden kayak canoe`，与 Sports `whitewater kayak` 近义但不完全重叠，未擅自改动待用户确认。验证：重建后 Sports catalog=54（总 1219）、env=24、翻译全过；JSON 完整性脚本确认 54 subjects ↔ 54 mapped、0 orphan、0 invalid group、0 group venue 缺失；dry-run 54 jobs，14 个新主体全部产出且 env 亲和正确（snow 主体只配雪场/冰湖/冰瀑/冰川，water 主体只配泻湖/激流/码头/泳池/风湾/静河，track 主体只配跑道/自行车馆/山路/泥道/山脊/沙丘）。备份 `temp/backups/build_v5_library.py.bak-20260902-sports-outdoor-expansion`、JSON 同后缀。

## Sports court 环境组拆分（方案 A：同质子组）（2026-09-02 16:20 GMT+8）

- **问题**（JigsawV5_Sports 抽检）：体育场内 18 个 court 主体生成图"一团糟"——主体不突出，全是环境。根因：`court` 组 4 个 env 互不相容（木地板球馆/看台/草坪/网球场），3-env 层叠模板每次抽 3 个 = 3 种不同运动场地硬拼到一张图，主体被淹没。户外组（snow/water/track）env 同质，3 个层叠自然融合，故户外效果尚可。
- **修复**（`temp/build_v5_library.py`）：将 `court` 拆为 6 个同质子组，每组 3 个视觉兼容的场地变体（同一场地的不同光照/角度/细节）：
  - `grass_field`（3 env）：足球/橄榄球/曲棍球 → 阳光草坪+白线+角旗 / 泛光灯草坪+清晰标线 / 晨露草坪+柔和阴天
  - `hardwood_arena`（3 env）：篮球/艺术体操 → 室内球馆+木地板+看台 / 明亮体育馆+枫木地板+阶梯看台 / 宽敞运动馆+木地板+顶部聚光灯
  - `indoor_court`（3 env）：网球/羽毛球/乒乓球/台球/击剑 → 室内运动馆+标线球场+网柱 / 明亮室内球场馆+顶部照明 / 洁净室内场地+地面标线+分区
  - `outdoor_field`（3 env）：高尔夫/棒球/射箭/马术 → 开阔草地+蓝天 / 阳光户外运动场+远处树线 / 阴天户外球场+柔和漫射光
  - `gym_studio`（3 env）：举重/拳击/奖牌 → 训练健身房+器械架+镜面墙 / 室内健身工作室+器材+明亮灯光 / 洁净训练室+软垫+顶部灯光
  - `beach_court`（3 env）：排球 → 沙滩球场+网柱+海风 / 阳光沙滩排球场+细沙 / 海风海滨沙地球场+晴空
  - `SPORTS_SUBJECT_ENV_GROUP` 18 个 court 主体全部重映射到新子组；CN_TO_EN 补 17 条新 env 中文翻译。track/snow/water 三组不变。
- **验证**：重建后 Sports catalog=54、env=24 不变；JSON 中 env_groups 6 新子组各 3 env、subject_env_group 18 主体全映射、0 残留 court；dry-run 18 主体 3 层 env 全来自同一子组、0 跨类型污染；小批量实图 18 张 0 失败，抽检 7 张（足球/篮球/乒乓/台球/高尔夫/排球/花滑）环境同质连贯、主体突出，旧版"一团糟"问题消除。
- **备份**：`temp/backups/build_v5_library.py.bak-20260902-court-split`。



- `lib/pages/game_page.dart` AppBar 与顶部状态收敛：
  - 移除 AppBar 标题文本（去“第x关”），释放横向空间；
  - 右侧 actions 收敛 6 个图标，按用户指定排序：① 显示边缘碎片（`cornersOut`）② 智能提示（`lightbulb`）③ 遮罩底模（`stack`）④ 眼睛看原图（`eye`）⑤ 扫把一键整理（`broom`）⑥ 换壁纸背景（`image`）；
  - 彻底移除 AppBar 下方的短状态条 `_buildShortStatusBar()`，拼图进度线贴合 AppBar 底边展示；
  - 页面 Body 直出游戏画布，消除多余高度占用。
- `lib/game/jigsaw_puzzle_game.dart` 托盘高度与上下间距重构：
  - 碎片在托盘中高度静态推导解耦：`_trayPieceHeight = srcPieceH * (targetTrayPieceBaseSize / maxSrcSide)`；
  - 托盘高度由屏幕比例缩减为由碎片高度决定：`targetTrayH = _trayPieceHeight * (1.0 + 0.15 + 0.30) = _trayPieceHeight * 1.45`（约 92.8px，净释放 ~70px 垂直高度给中央棋盘）；
  - 托盘内部碎片排布改为：上边间距为碎片高度的 15%（`py = trayPosition.y + _trayPieceHeight * 0.15`），下边自然留出碎片高度的 30% 作为触控/横条安全边距；
  - 绝不改动棋盘触摸拖拽、多指缩放漫游、吸附判定及撤销重做等拼图核心逻辑。
- `lib/widgets/victory_dialog.dart` 与 `GamePage` Victory 结算全链路统一：
  - `VictoryDialog` 支持 `newAchievements` 新解锁成就横条展示，统计栏支持用时、规格（`$pieceCount 块`）、金币翻滚；
  - 按钮体系自适应：支持有下一关展示“下一关”，无下一关自适应展示“返回列表”；接入“保存壁纸”本地写入与“分享成绩”（`ShareCardGenerator`）；
  - `game_page.dart` 彻底移除 200+ 行手写旧内联 `AlertDialog`，统一接入 `VictoryDialog.show(...)`；
  - 浮动通关 Banner（`Floating Victory Banner`）统一深色炭灰卡片微质感规范，支持再次打开结算。
- 测试与验证：
  - `test/widget_test.dart` 移除复制的假 AlertDialog 测试，改为断言真实 `VictoryDialog` 渲染与交互，全部 8 例 PASS；
  - `test/game_layout_test.dart` 同步更新托盘上边距 15%、下边距 30% 及浮点数容差断言，全部 38 例 100% PASS；
  - `flutter analyze` 0 error, 0 warning；
  - `flutter build windows --debug` 编译成功验证通过。

## Phase 0 首页重构实施（2026-09-02）

- `lib/main.dart:58` 启动并行化：最前置 `await AppLogger.init():26`，组1 `await [ImageCacheManager, GameRepository, EconomyService, AchievementStore]`（`main.dart:80` 需首帧就绪），组2/3 `DownloadManager/AppContent/SoundService` 后台 `unawaited`，首帧先出壳。
- `lib/pages/main_screen.dart:72` AppBar精简：移除 `🪙_CoinBadge`，仅保留 `🏆`；`⚙️` 仅 `My` Tab（`_currentIndex==3`）显示。
- `lib/data/models/level_item.dart:1` / `lib/logic/content/models/puzzle_level_item.dart:1` 补 `tags/List<String>` + `addedAt/DateTime?`（NEW 7天口径）+ `unlockCoins/int?`/`unlockCode/String?` 预留及 `toJson/fromJson/copyWith/isNew`；`LevelItem.isNew` 7天内且未完成。
- `lib/data/game_repository.dart:132` 禁用关卡墙：`_initLevels` 全量 `isUnlocked:true`，`addedAt` 按 `i>90` 近7天其余30天前生成便于 NEW 演示；`updateLevelProgress` 保留空实现兼容旧存档；`lib/logic/content/pipelines/main_content_pipeline.dart:213` 解析 `addedAt/unlockCoins/unlockCode` 并持久化。
- `lib/pages/tabs/home_tab_view.dart:1` 重构：删 `LevelFilter` 伪难度（`starter/intermediate/master`）与排序；Header `PageView`（每日+活动3个）不吸顶+dots；Tag单行横滑 `kHotTagIds` 6个 + 右侧固定 `☰` 吸顶 `SliverPersistentHeader 44dp`；`_AllTagsSheet` 3列21宫格；Grid 2列 `220/10/8圆角` 纯图卡 + `New` 橙棕斜角；移除 `#index` 序号与 `需X星解锁` 锁态 `home_tab_view.dart:898`；`ChooseDifficultySheet` 固定 `isUnlocked:true`。
- 验证：`dart format` 7文件，`flutter analyze` 1 warning（`_CoinBadge` 未引用），`flutter build windows --debug` 成功；`flutter test` 10失败中8项为 Phase0 预期变更（`全部关卡`→`全部`、`关卡未解锁`等），2项为既有 `events_tab` 空态文案，待测试同步。

## 文档交叉验证修正（2026-09-02）

- 修正 `docs/jigsaw-app-comprehensive-review-20260902.md` 6处代码引用：`LevelSortOrder` 虚构→仅 `LevelFilter`（`home_tab_view.dart:16`）；`game_repository.dart:898`→`home_tab_view.dart:898` 锁态文案；`game_repository.dart:34`→`image_source.dart:7` 定义/`game_repository.dart:137` 循环；`main.dart:56`→`58` 起 8步（含 `AppLogger.init():26`）；`main.dart:124`→`123`；`7个await`→`8个`。并行化方案调整：`EconomyService`/`AchievementStore` 移入组1 await（`main.dart:80` 需首帧就绪），仅 `DownloadManager/AppContent/SoundService` 后台；`§3.3` NEW 定为方案A `addedAt` 7天并说明废弃B原因；`§8.4` 日帽明确首期不动维持200；`§3.5/§13` 补 `home_tab_view.dart:898` 锁态UI移除任务。
- 修订 `docs/hometab-reference-implementation-20260902.md`：标题/Header 不复刻参考大字/斜切，仅复用布局与Tag。

## HomeTab 参考实现拆解（2026-09-02）

- 新增 `docs/hometab-reference-implementation-20260902.md`：基于 `temp/hometab` 3张 Jigsawscapes 实机截图 1:1 拆解——Header PageView+dots/Tag单行横滑+固定☰→3列Sheet(27→21映射)/仅Tag吸顶/Header不吸顶/2列纯图+New斜角飘带；标题/Header保持现有简洁AppBar，不复刻大字与斜切；输出可直接进 Figma/Flutter 的 Sliver/Chip/Sheet/Card 像素规格与热门N数据驱动、addedAt 7天 NEW 口径、二期块数可选等落地清单。

## 整App综合体检与对标优化方案 v1.1 修订（2026-09-02）

- 修订 `docs/jigsaw-app-comprehensive-review-20260902.md` 至 v1.1：采纳6条定向反馈——AppBar仅留成就/设置仅我的Tab/启动并行化/活动官方与DIY双轨/首页全量可浏览无墙无排序、单行热门Tag+全部Sheet、卡片去序号仅图+NEW(基于addedAt 7天)、Header可滚Tag吸顶、manifest预留unlockCoins/unlockCode、经济“星荣誉币流通”、GamePage延后；P0聚焦首页2-3天。
- 新增 `docs/jigsaw-app-comprehensive-review-20260902.md` v1.0：对齐 Magic Jigsaw(Easybrain/ZiMAD/Jigsawscapes) 全链路走查，覆盖导航/首页/每日/活动/自制/游戏内核/难度星级解锁经济/成就/内容管线/性能无障碍 12 模块，提出 6项P0 + 12项P1 缺口与分4阶实施路线；核心判断为“线性闯关×画廊浏览”范式冲突，建议冻结星墙转画廊轻进度模型。

## 图片缓存：缩略图档位 enum 化 + 内存/缓存修复（2026-09-02）

### 类型安全：缩略图档位改为枚举
- 新增 `enum ThumbnailDimension { card(360), eventCover(720) }`（`lib/logic/cache/image_cache_manager.dart`），
  取代原先「任意 int + 向上收敛到 {360,720,1440}」的设计；调用方只能在两档中选择，从编译期杜绝
  传 360/500/600 等零散尺寸造成同图多份缩略图。
- `ImageCacheManager` 全部公开 API（getCacheKey/getThumbnailBytes/getThumbnailFile/
  prewarmThumbnail/isThumbnailCached/removeThumbnailForSource 等）的 `int targetDimension`
  参数改为 `ThumbnailDimension dimension`；删除 `kThumbnailDimensionBuckets` 与 `normalizeDimension()`。
- `removeThumbnailForSource` 改为遍历 `ThumbnailDimension.values` 全档位失效。
- `AppCachedImageProvider` / `AppImageKey`：`targetDimension:int` → `dimension:ThumbnailDimension`。
- `AppCachedImage`：`targetWidth/targetHeight(int?)` → `targetDimension(ThumbnailDimension, 默认 card)`，
  所有分支统一 `ResizeImage(width: dimension.pixels)`。
- 调用点更新（11 处）：home_tab 今日挑战大卡与 events_tab 封面 → `eventCover(720)`；
  其余卡片（daily/event_levels/pack_levels/my_puzzles/素材匣/首页关卡）→ 默认 `card(360)`。
- 测试同步：`test/image_cache_manager_test.dart` 3 处 `dimension: ThumbnailDimension.card`；
  新增「ThumbnailDimension Enum Tests」组 3 例（档位值 / 同源不同档位键区分 / 全档位清理）。
- 缓存键结构（`thumb_<fnv63>_<pixels>.jpg`）与 hash 算法不变，已存在的 360 缩略图仍可命中，正向 360 键无迁移成本；负号键与旧档位文件首次启动自动清理。

### 解码期降采样（fix #1）
- `Image.memory` 补充 `cacheWidth`：victory_dialog 600、continue_dialog 1080、
  choose_difficulty_sheet 预览 1080、share_card_generator 1080、
  **GamePage 暂停缩略(300×160) 1080、全屏原图预览 1440**（大号缩略，避免超分图 50MiB 全量解码；GamePage 拼图核心仍用原图按设计保留）。
- `choose_difficulty_sheet._decodeImageSize` 改用 `ui.ImageDescriptor.encoded` 只解析头部取尺寸
  （零像素解码），并 `dispose()` buffer/descriptor，消除原 `instantiateImageCodec` 的位图/Codecs 泄漏。
- `game_page.dart:_loadHeaderColor` `crop_puzzle_page.dart:_decodeImage` 补充 `codec.dispose()` 与 `image.dispose()`，消除后台解码泄漏。

### 网络封面落地缓存（fix #3，events_tab）
- 新增 `AppCachedNetworkImageProvider`（`lib/logic/cache/app_cached_network_image_provider.dart`）与
  `ImageCacheManager.getNetworkThumbnailBytes()`：网络 URL 同样走 `L1 内存 → L2 磁盘 thumbnail_cache/thumb_<hash>_<dim>.jpg → L3 下载+后台缩略` 三级流水，与本地文件共用同一缓存目录/同一 `getCacheKey` 命名空间与 `EngineTaskQueue` 单飞去重。
- `AppCachedImage` 网络分支由 `NetworkImage`（无磁盘缓存、每次重联网）改为 `AppCachedNetworkImageProvider`，`events_tab_view.dart:121` 活动封面 `event.coverUrl` 无需页面额外改动即享磁盘缓存与 `eventCover(720)` 解码期降采样。
- 封面首次下载后由 `ThumbnailGenerator.generateThumbnailFromBytes` 在 Isolate 生成 720 缩略，后续命中 L1/L2 零网络；`getCacheSizeBytes/clearCache` 已覆盖网络缩略。

### 设置页缓存管理（fix #2）
- 设置页「数据管理」新增「缩略图缓存」行：显示占用（`getFormattedCacheSize`）+ 清理按钮（`clearCache`）。
- 全部失败 Toast 图标由 material `Icons.error_outline` 统一为 `PhosphorIconsRegular.warning`（风格一致）：设置页缓存清理、`my_puzzles_tab_view`、`event_levels_page`、`pack_levels_page` 共 5 处。

### 杂项
- FNV 掩码改为 63 位（`hash & 0x7FFFFFFFFFFFFFFF`），消除约 50% 负号文件名；正数键与旧算法一致。
  新增启动期孤儿清理：`_rebuildDiskKeyIndexAsync` 自动删除 `thumb_-*.jpg` 负号键与旧档位 `_600/_1440` 孤儿文件。
- `getCacheSizeBytes` 仅统计 `thumb_*.jpg`，避免误算目录内其它文件。
- `AppCachedImage` 移除死参数 `color: null / colorBlendMode`（实际着色由 `ColorFiltered` 完成）。
- `choose_difficulty_sheet.dart` 混合行尾统一为 LF。

### 备注
- `test/events_tab_view_test.dart` 2 个用例为既有失败（HEAD 上即失败）：UI 空状态文案已改为
  「小狐狸没找到正在进行的活动」，测试仍断言旧文案「暂无正在进行的活动」，待 UI 文案侧同步。
