# CHANGES-20260910

## [Fix] ImageCacheManager 磁盘计数体系修复（外部 review 第 2 项：L2 覆盖写/删除计数漂移）

针对外部 review 指出 `_diskKeyIndex.add()` 覆盖写（返回 false）时 `_diskCacheBytes` 未减旧加新、长期覆写失准的问题，重构计数体系为精确增减：

- **结构**：`_diskKeyIndex` 由 `Set<String>` 改为 `Map<String, int>`（key → 字节大小），便于覆盖写/删除时取旧值精确增减；公共 API 零改动（`isThumbnailCached`/`diskCacheBytesForTest`/`getCacheSizeBytes` 全部不变）。
- **重建索引**（`_rebuildDiskKeyIndexAsync`）：扫描时先 `await entity.length()` 取 size，`_diskKeyIndex[fileName] = size` 并同步累加 `_diskCacheBytes`（原实现 add 与加计数分离，length 异常时会留下有 key 无计数的不一致）。
- **覆盖写**（本地 L3 与网络下载两条写盘路径）：先取 `previousSize = _diskKeyIndex[cacheKey]`，写入后 `_diskCacheBytes += newSize`，若为覆盖则再 `-= previousSize`，并加 `<0 → 0` 负值保护——新增只加新、覆盖净变化 = 新-旧，计数与磁盘一致。
- **删除路径**（`removeThumbnailForSource`）：移除索引时同步取旧 size 减计数（原代码删文件不移除计数，长期删除会虚高）。
- **读失败移除**（本地 L2 命中、网络 L2 命中、网络排队二次检查共 3 处）：`_diskKeyIndex.remove(cacheKey)` 返回值若非 null 则同步减计数，避免"索引已移除但计数未减"。
- **LRU 淘汰**：保持原实现（`entry.size` 来自 `stat()` 精确值，减除后 `Map.remove` 移除索引），无需改动。
- **clearCache**：`_diskKeyIndex.clear()` 适配 Map；`_diskCacheBytes = 0` 不变。

### 验证

- `dart format`（仅 1 个改动文件，0 改动残留）。
- `flutter analyze --no-pub`：0 error / 0 warning（241 info 与基线 241~243 一致，无新增）。
- `flutter test --no-pub`：313 passed / 8 skipped 全量通过（含 LRU 淘汰计数断言与 clearCache 归零断言）。
- `flutter build windows --debug`：编译成功。

## [Review] 单飞派生链错误态修复单飞助手错误态 + 架构文档 String? 同步（外部 review）

外部 review 核验结果（三项全部真实，无虚假/夸大）：

1. **`single_flight.dart` L28 派生链未处理错误态（真实，本次修复）**：`unawaited(future.whenComplete(...))` 中 `unawaited` 是空实现（仅告知 linter 有意忽略），不吞错误——原 future 以 error 完成时 `whenComplete` 派生 future 以同样 error 完成且无人接，触发未处理异步错误（原 main 管线 HEAD L341 同模式）。修复：改为 `future.whenComplete(...).ignore()`（内部 `then((_) {}, onError: (_) {})` 完美屏蔽结果与错误）；未选 review 建议的 `catchError((_) {})` 是因为 T 为非空类型（`bool`）时回调无法返回合法值。
2. **`image_cache_manager.dart` L331/L585 覆盖写计数漂移（真实，既有问题非本次引入）**：`_diskKeyIndex.add(cacheKey)` 返回 false（覆盖写）时 `_diskCacheBytes` 不减旧大小、不加新大小，长期覆写失准。修复方案（未实施，单列）：`_diskKeyIndex` 由 `Set<String>` 改 `Map<String,int>`（key→size），覆盖时先减旧再加新；顺带覆盖 `removeThumbnailForSource`（L416）与读失败移除索引（L280/459/482）删文件未减计数的同类漂移。
3. **`docs/image-cache-and-thumbnail-pipeline-architecture.md` L116 文档滞后（真实，本次修复）**：签名仍写 `String`，实际已为 `String?`（P0-2）。已同步为 `String?` 并注明 null 语义（2026-09-10 更新）。

## [Fix] 内容管线单飞收口（代码审查 P1-7）：新增 runSingleFlight 泛型助手，四管线统一接入

基于 2026-09-10 代码审查报告 P1-7（内容管线缺少单飞防护，同资源并发下载互删 temp_extract 临时目录），采用方案二实施：抽取泛型单飞助手，消除四份重复样板。

### 新增 `lib/logic/single_flight.dart`

- 泛型 `runSingleFlight<T>(Map<String, Future<T>> inFlight, String key, Future<T> Function() create)`：同 key 并发调用复用进行中 Future；创建的任务存入表后，在 `whenComplete` 中以 `identical(inFlight[key], future)` 复核通过才移除（防 ABA：旧任务完成误删已被新任务替换的记录）。
- 包装器设计为同步返回（非 async），保证"查表-返回"同一同步帧内完成，命中即原样返回，无额外事件循环让出点。

### 四条管线接入（各保留独立 `_inFlightDownloads` 状态表，id 命名空间隔离）

- `collections_content_pipeline.dart`：`ensureCollectionDownloaded` 改同步包装器调 `runSingleFlight(_inFlightDownloads, collection.id, ...)`，原方法体下沉为 `_ensureCollectionDownloadedImpl`；`onProgress` 回调透传（复用方收不到进度，与 main 既有行为一致）。
- `events_content_pipeline.dart`：`ensureEventDownloaded` 改包装器（key = `event.id`），方法体下沉为 `_ensureEventDownloadedImpl`。
- `daily_content_pipeline.dart`：`ensureMonthReady` 改包装器（key = `yyyyMm`），monthDir 已就绪检查随方法体整体下沉为 `_ensureMonthReadyImpl`；命名参数签名（含 `overrideToday`）保持不变。
- `main_content_pipeline.dart`：`ensureLevelImageDownloaded` 移除原有内联查表/存入/whenComplete 清理样板，统一改调 `runSingleFlight`（key = `level.id`，`timeout` 透传保留）；原 `fine` 级 "in-flight reuse" 调试日志随样板移除（helper 不感知各管线 logger，impl 内部 severe/warning 错误日志不受影响）。

### 验证

- `dart format`（仅 5 个改动文件，2 文件有格式调整，0 残留）。
- `flutter analyze --no-pub`：0 error / 0 warning（info 241，与基线 241~243 一致，无新增）。
- `flutter test --no-pub`：313 passed / 8 skipped，全量通过。
- `flutter build windows --debug`：编译成功，产物 `JigsawFox.exe`。

## [Fix] ImageCacheManager / GameRepository 三处修复（代码审查 P0-2 / P0-3 / P1-5）

基于 2026-09-10 代码审查报告（`docs/flutter-code-review-20260910.md`）及相关修订版，跟进三项修复；P0-4（EXIF 方向忽略）与 P1-3（磁盘缓存越限）经评审降级暂缓，仅供记录。

### P0-2 缩略图缓存目录未就绪时不再拼出非法路径

- `image_cache_manager.dart` `getThumbnailFilePath()`：返回类型 `String` → `String?`；`_cacheDir == null` 时返回 null 而非 `''/thumb_xxx.jpg` 空串拼接。
- 8 处调用点全部适配 null 契约：L2 磁盘命中（本地/网络两处、排队二次检查）在 diskPath 为 null 时跳过磁盘读取；L3 写盘（本地/网络两处）在 targetPath 为 null 时跳过 L2 写入、仅保留 L1 内存缓存；`getThumbnailFile()` 在 targetPath 为 null 时直接回退原图 `File(sourcePath)`；`removeThumbnailForSource()` 无磁盘路径可清时 continue。
- 按用户约束**未**加降级标志、未改动 init 的 `completer.complete()` 语义（保持 P0-1 的设计：debug 版遇错直接 crash 便于排查）。

### P0-3 L2 缩略图写盘改为临时文件 + rename 原子替换

- `image_cache_manager.dart` 本地生成与网络下载两条 L2 写盘路径（原 `targetFile.writeAsBytes(generatedBytes, flush: true)` 裸写最终路径）统一改为：先写 `$targetPath.tmp`，`rename` 原子替换到最终路径；Windows 下 rename 不能覆盖已有文件，先删旧文件再 rename。
- 写盘异常时经既有 `_deleteTempFile()` 清理残留 `.tmp`，再打 warning。
- 按用户约束**未**加 JPEG 魔数校验、未改动 L1 内存缓存与网络 `.part` 下载逻辑（.part 由原有 finally 兜底清理）。

### P1-5 addCustomPuzzle 先落盘成功再更新内存

- `game_repository.dart` `addCustomPuzzle()`：由"先插内存 → `await` 落盘"改为"先 `await _saveCustomPuzzle(item)` 落盘成功 → 再插入 `_customPuzzles` 并刷新 `customPuzzlesNotifier`"。
- `putJson` 透传 Hive 写盘 Future，落盘失败会上抛：内存列表与 notifier 保持不变，调用方 `crop_puzzle_page` 的 catch 会 toast `saveFailedToast` 且不 pop，不再出现"UI 已显示但磁盘无此条"的幽灵条目。

### 降级记录（暂不修改）

- **P0-4 EXIF 方向在缩略图/裁剪管线被忽略**：影响面收窄（仅本地相机原图 Orientation 6/8，网络素材多已转码），按决策降级，暂不修改。
- **P1-3 `_enforceDiskCacheLimit` 重入保护吞掉淘汰时机**：磁盘占用阶段性越限无实质危害，按决策降级，暂不修改。

- 验证：`dart format`（仅改动两文件）；`flutter analyze --no-pub` 0 error / 0 warning（info 243 与基线 241~242 相当，为依赖版本波动）；`flutter test --no-pub` 313 passed / 8 skipped；`flutter build windows --debug` 编译通过。