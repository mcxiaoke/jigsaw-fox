# CHANGES 2026-09-16

## 2026-09-16 14:15 对接 better-updater 专属打包工具 packer.exe 与全套优化

- **集成专属打包工具 packer.exe**：
  - 同步引入 `better-updater` 最新产出的专属打包工具 `tools/windows/packer.exe`（单文件原生 Rust 工具，368 KB，带构建身份注入）；
  - 更新 `scripts/release.py`：Windows 打包流水线全面对接 `packer.exe`，一键自动化完成 `updater.manifest` 清单生成、启动闭包（根目录入口、DLL 与 `data/*.so`、`data/*.dat`）连续排尾、流式压缩与回读机械自检。
- **更新器客户端调用优化**：
  - 更新器二进制同步升级为 `better-updater` 最新构建（注入 Git 提交短哈希与 UTC 构建时间戳）；
  - `UpdateInstaller._installWindows` 移除硬编码 `--gui-title` 与 `--delete-zip`，完全依托更新器从 `--launch` 自动派生的多语言自适应标题，并遵循最新规范保留更新包作为极端中断时的人工兜底材料。
- **全链路实测**：通过真实 Release 产物与 `packer.exe` 端到端打包验证，`flutter analyze` 0 issues，全量单测全部通过。

## 2026-09-16 12:05 Windows 自动更新升级为 better-updater（支持影子 Worker 与崩溃自愈）

- **替换并升级更新器内核**：将 Windows 原生更新器从旧版 lite 升级为 `better-updater`（单文件原生 Rust 编译，版本 1.0.0，476 KB，带 Journal 事务、L1/L2 崩溃自愈收敛、看门狗保护）。
- **客户端调用与自愈接入**：
  - `UpdateInstaller._installWindows`：基于 `better-updater` 影子 Worker 特性，支持直接从应用根目录调用 `updater.exe` 原地更新与自更新，以 `detached` 模式启动并传递 `--gui-title JigsawFox`；
  - 新增 `UpdateInstaller.checkForCrashRecovery()`：在 `UpdateService.checkForUpdate` 检查前探测是否存在 `.updater/journal` 未完事务，若存在则同步调用 `updater.exe --recover` 自动收敛旧现场。
- **打包规范加固**：
  - 更新 `scripts/release.py`：打包 Windows 绿色 zip 时遵循 `better-updater` §13.3 规范，将启动闭包（根目录入口、DLL 与 `data/*.so`、`data/*.dat`）连续排序在 zip 压缩包最末尾，最大限度保证更新中断时应用仍处于完整旧闭包状态可正常启动。
- **质量验证**：全量单元测试、发版验证测试、`flutter analyze` 静态检查 100% 通过。
