# CHANGES 2026-09-14

## 2026-09-14 19:14 Windows 端正式集成通用原生更新器 (updater-rs)

- **集成 updater-rs 原地自更新方案**：
  - 弃用 Inno Setup 安装器弹窗方案，全面升级为通用原生极简 `updater.exe`（单文件原生 Rust 编译，仅 ~366KB，零外部依赖）。
  - 安装器分发层重构：`lib/update/update_installer.dart` 落地黄金法则，启动前将 `updater.exe` 复制到临时目录 `%TEMP%\jigsawfox_updater\` 执行（确保主程序目录内的 `updater.exe` 自身随包自更新），以 `ProcessStartMode.detached` 分离进程拉起，主程序安全退出释放内核句柄。
  - 原生交互与安全特性：启用 `--gui` 极简 Win32 进度条（独立 UI 线程防假死、原生系统双语自适应）；Win32 `ReplaceFileW` 原子覆盖，内存回滚栈保障失败自动还原旧版并重新拉起；支持通过 `--pid` 进行 Win32 内核句柄绑定等待，彻底免疫 PID 复用。
- **打包脚本与保护清单对接**：
  - 在 `windows/.updatekeep` 建立保护清单，确保更新时 `logs/` 与临时文件绝不被覆盖丢失（明确避让 Flutter `data/` 引擎目录）。
  - 更新 `scripts/release.py`：构建 Windows Release zip 时自动从 `tools/windows/` 注入 `updater.exe` 与 `.updatekeep` 进根目录。
- **设计文档同步**：同步修订 `docs/app-auto-update-design-20260914.md`，明确 Windows 原地绿色便携免安装更新架构与时序规范。

## 2026-09-14 15:26 自动更新支持 Android 分 ABI 包与设备架构自动匹配

- **分 ABI 架构匹配**：
  - 更新 Android 端架构获取机制：改用官方合规的 `device_info_plus` 读取 `Build.SUPPORTED_ABIS`，彻底解决高版本 Android 系统权限限制导致 `getprop` 读取失败的问题。
  - 数据模型升级：`UpdateManifest` 支持 `platforms.android` 下的 ABI 映射（`arm64-v8a`, `armeabi-v7a`, `x86_64`, `all`），根据设备支持的 ABI 降序列表优先命中最佳包，并自动兜底至通用包。
- **发布脚本支持**：`scripts/publish/release_app.py` 全面适配分 ABI APK 目录结构、远端强校验与验签比对。
- **测试保障**：单测用例覆盖精准 ABI 命中、多 ABI 优先级降级回退、通用包回退等各种边界场景，全量 373 个单测全绿。

## 2026-09-14 15:06 App 自动更新系统设计重构与全链路落地实施

- **设计重构**：修改 `docs/app-auto-update-design-20260914.md` 为极简可靠版本。彻底砍掉 `dev/beta/release` 多渠道与晋升机制，改为单一扁平 `updates.json` 清单；弃用 Windows 易被杀软误杀且锁句柄的批处理自覆盖方案，改用 Inno Setup 安装器方案；补全 Android FileProvider 与未知来源权限引导规范。
- **发版与巡检脚本 (`scripts/publish/release_app.py`)**：
  - 支持 `prepare` 提取 `pubspec.yaml` 版本并计算 SHA256/Size。
  - 支持 `verify --local` 与 `verify --remote`：端到端流式抓取远端 `updates.json` 及引用的全部平台安装包与备用镜像，强校验 HTTP 状态码 200 与 SHA256 哈希值、文件大小，拦截 404 与任何损坏。
  - 支持发布后将远端二进制安装包实际下载到本地临时目录，调用 `apksigner verify` 深度验签，调用 `aapt dump badging` 提取并严格比对真实 `versionCode`，全绿后再更新 `updates.json` 并终检巡检。
  - 编写并全部通过 Python 单元测试 `scripts/publish/test_release_app.py`。
- **客户端自更新模块 (`lib/update/`)**：
  - 数据模型：`update_models.dart` 实现极简 updates.json 反序列化、平台与语言过滤。
  - 核心服务：`update_service.dart` 支持启动静默检测（异常静默吞掉、支持用户忽略此版本）与设置页手动检测；下载采用流式进度、主源自动回退备源镜像、SHA256 强校验失败即刻熔断清理。
  - 平台安装：`update_installer.dart` Windows 启动安装程序后优雅退出，Android 调用 `open_filex` 规范拉起安装。
  - 弹窗交互：`update_dialog.dart` 支持强制更新（禁用返回/关闭）与普通更新（支持立即更新、稍后、忽略此版本），平滑进度展示。
  - 界面接入：`SettingsPage` 增加“检查更新”入口并动态展示底层真实版本号；`MainScreen` 启动 3 秒后执行防打扰静默更新检测（并在测试环境中安全跳过）。
  - 日志规范：`AppLogger` 新增 `update` Logger，全生命周期详细记录并持久化到本地 `logs/` 目录。
- **质量验证**：
  - 编写并全部通过 `test/update/update_test.dart` 客户端单元测试。
  - `flutter analyze` 0 issues，全部静态检查全绿。
  - `flutter test` 372 个单元/组件测试 100% 通过。
  - `flutter build windows --debug` 编译成功通过。
  - `flutter test .\integration_test\app_test.dart -d windows` 4-Tab 完整集成测试通过。

## 2026-09-14 13:50 发布与打包脚本适配重构 (scripts/release.py)

- 移除对不存在的 `scripts/generate_build_info.dart` 的硬依赖（改为存在才调用）。
- 修正目标应用标识与二进制名称：将原 `CarroNote` 统一修正为本项目 `JigsawFox`（可执行文件名 `JigsawFox.exe`，产物前缀 `JigsawFox-{version}-*`）。
- 增强 Windows 打包：支持多路径动态探测，打包过滤 `*.pdb` 等非必要调试符号与中间文件。
- 增强 Android 打包：新增 `find_apk_file` 兼容 `apk/release` 与 `flutter-apk` 输出路径，适配 Gradle 产物。
- 新增 CLI 参数支持：`-p/--platform {all,windows,android}` 支持独立构建指定平台；`--clean` 可选全量清理；`--no-split-abi` 可选跳过分 ABI 构建；`--dry-run` 支持预演。
- 完整通过 Windows 与 Android 平台的构建、打包与 SHA256SUMS 清单生成验证。

## 2026-09-14 12:16 文档与代码一致性核查与修正（全库文档巡检）

- 本轮为纯文档修正，未改动任何代码；所有被改文件已先备份至 `temp/backups/20260914-doc-sync/`。
- **README.md**：删除不存在的 LICENSE 徽章；技术栈修正（无 flame_riverpod / flutter_riverpod，实际为原生 setState/ValueNotifier）；4-Tab 描述 Events→Collections；移除不存在的根目录 `deploy/`（部署脚本在 `scripts/deploy/`）；修复失效文档链接（ui-ux 架构文档已删除、comfyui 指南为 v5 版、CHANGES 链接更新至 0912/0913）；minSdk 25→26。
- **AGENTS.md**：目录说明移除不存在的 `deploy/` 条目。
- **docs/jigsaw-puzzle-game-architecture.md**：架构图 GhostOutlineComponent→BoardGhostComponent（吸附高亮由 PuzzlePieceComponent 内部绘制）；棋盘 0.90 占满公式→桌面散落二分搜索 0.50~0.92（基准 0.56、覆盖率 0.80、收缩 0.97）；§3.11 单击抓取状态机重写（按下即持握、无 350ms/8px 双阈值、取消仅 ESC 无右键）；§3.13 移除"短边 2160 上限"（代码无此常量）、裁切 maxScale 公式修正为 `min(boxW,boxH)/(baseScale*1080)`；§3.10 我的自制 aspectRatio 0.95；§4.1/§4.3 快照 v2→v3；§4.3 三级缩略图缓存章节按已删除的现实重写（现由 LevelImageResolver + ThumbnailGenerator 承担）；测试矩阵同步。
- **docs/data-architecture-current.md**：顶部加"已过时"警告（描述 Hive 迁移前旧架构）。
- **docs/image-cache-and-thumbnail-pipeline-architecture.md**：顶部加"已过时"警告（ImageCacheManager 三级缓存体系已整体删除）。
- **docs/app-storage-directory-architecture-20260911.md**：hive_data box 清单 6→3（favorites/economy/achievements 独立 box 不存在，实际以 `favorite:`/`econ:`/`ach:` 前缀存于三 box）；备份保留 3→5 份；快照文件名格式修正；启动自愈描述修正（仅清 `temp_*` 与 `*.bak_<ts>`，无 .part/.tmp 扫描）。
- **docs/home-network-migration-and-boot-init-design-20260907.md**：首启总预算 20s→25s（kFirstBootTotalBudget）。
- **docs/puzzle-content-storage-and-expansion-design.md**：manifest 离线回退由 assets/data/manifest_default.json 修正为代码内置 `_createDefaultFallbackManifest()`；顶部加部分取代说明（thumbnail_cache 删除、levels/network 路径、packs 实际位置、快照命名、进度存 Hive、内置关卡下线）。
- **docs/jigsaw-taxonomy-ssot-codegen-specification.md**：基线 14 主 Tag/3×5=15/v3.0.0→17 主 Tag/3×6=18/v3.2.0(2026-09-07)；kTag 常量示例对齐生成物（含 Colors/Composition/Others，无 Travel/Cozy）。
- **docs/jigsaw-tag-subject-catalog.md**：标题与依据修正（v1.0 20 Tag 基线，v1.1 新增 Sports 共 21，注明与 SSOT 17 主类经 tag_patterns 映射）。
- **docs/jigsaw-difficulty-scoring-achievements-design.md**：§2.1"删除 3:4/4:3"加注（v3.4 已撤销恢复）。
- **docs/jigsaw-star-rating-casual-two-track-design-20260904.md / recalibration**：失效行号更新/加时效说明（estimatedMinutes 现约 L246-259；game_page 起表 L110、看原图停表 L1158 等）。
- **studio/README.md、studio/docs/studio-server-architecture-and-api-20260908.md**：主 Tag 14→17（含核心大类中文名更新）；移除不存在的 `/api/exported`；补记 `/api/export/limits`、`/api/export/check-outdir`、`/api/export/check-pack-id`、`/api/ledger/ops|records|audit`、`/api/rollback`；export_tracker 描述改为账本只读视图（旧 exported.json 兼容层已移除）；软删除目标 `Deleted/`→`.deleted/`；缓存库 `.studio.db`→`.studio/cache/studio.db`；日志双 Handler→三 Handler（含源库日志 `--logfile off` 联动）；依赖缺失降级→启动强校验 fail-fast；JobStore 更新（quality/export/rollback 三类互斥、cancelled 独立终态、匿名导出自动注册）；core 模块清单补 export_rollback/git_guard/log_routing；导出执行链补 git 守卫链。
- **docs/sound-system.md、sound-system-reconstruction-and-fix-report-20260904.md**：init() 常驻 6 播放器→懒建池；coinsFly 已接入成就解锁（非预留）；全部埋点行号更新至当前代码；超时熔断 2.5s→800ms；pubspec 行号修正。
- **docs/save-resume-continue-design-20260830.md**：头部勘误（快照 v3、checksum/gzip 未实施、Hive 后 key 为裸 canonicalId、快照文件名格式、continue_dialog.dart 与按钮色、restart 流程）。
- **docs/dart-manifest-pure-route-migration.md**：标注纯路由改造未落地（DailyModuleConfig 仍保留 currentMonth），兜底示例同步。
- **docs/temp-storage-isolation-and-startup-gc-design-20260912.md**：解压目录命名补 prefix 段。
- **docs/network-level-lazy-download-plan-20260902.md**：加状态标注（EngineTaskQueue 全局限并发已随三级缓存删除，现行为 per-targetPath 单飞）。
- **docs/home-navigation-sorting-implementation-plan-20260902.md / online-image-picker / i18n-persistent-source-labels**：补实施状态标注（部分采纳/未采纳/已落地区分）。
- **docs/comfyui-batch-generation-guide-v5.md**：构建脚本路径 temp/→scripts/；--dry-run-count 默认值修正；参数表改为"常用参考"并补列缺失参数。
- **docs/image-source-and-batch-download-guide.md**：质检/导出命令参数全部对齐实际 argparse（--input/--output、--input-json/--min-grade/--output-dir）；限速与 .env 描述修正。
- **docs/puzzle-content-packaging-tool-design.md**：setup_test_server 描述更新；tags.json 示例 taxonomy_version v1.1-21→v1.0-21、模型统一 qwen3-vl:4b；API 表补 /api/health、/api/file、/api/export。
