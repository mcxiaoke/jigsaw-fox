# JigsawFox Changes - 2026-09-13

## 19:32 (GMT+8)

### Feat: 将「燕麦暖米」设为默认拼图背景并置于纯色首位

- **背景与调整**：响应用户偏好，将温润治愈的莫兰迪低饱和纸本色「燕麦暖米」（`#EBE5DC`）设为游戏全局默认背景。
- **改动**：
  - `lib/data/game_repository.dart`：调整 `kBackgroundColors` 顺序将燕麦暖米置于首位（索引 0），`selectedBackground` 默认首选解析直接指向燕麦暖米；
  - `lib/l10n/`：更新中英文色彩名称列表首项为燕麦暖米（Warm Oatmeal）；
  - `lib/pages/game_page.dart`：同步将游戏页底色回退值、AppBar 混合默认色以及开局渐隐遮罩统一对齐为燕麦暖米色（`#EBE5DC`）；
  - `test/widget_test.dart`：同步测试用例断言默认背景。
- **验证**：`dart format`（仅限改动代码）通过，`flutter analyze` 0 issues，`flutter test` 367 项测试全部通过，`flutter build windows --debug` 编译成功。

## 18:38 (GMT+8)

### Fix: 修复背景选择卡片选中框四角被内容剪裁的问题

- **背景与根因**：`ChooseBackgroundSheet` 的网格项使用 `Container.decoration` 绘制边框并开启 `clipBehavior: Clip.antiAlias`。由于 Flutter 的 `Container.decoration` 在背景层（`DecorationPosition.background`）先于子组件绘制，子组件内容（`ColoredBox`、`Image.asset` 及底部半透明标签）在 `ClipPath` 内全量绘制覆盖在背景层之上，导致 3.0px 选中边框在圆角拐角处及底部被子组件截断/覆盖，造成四角显示不全。
- **改动**（`lib/widgets/choose_background_sheet.dart`）：
  - 将 `border` 移至 `foregroundDecoration`（前景色层），在子组件内容渲染完毕后置顶绘制；
  - 边框完整保留 `BorderRadius.circular(14)`，四角与底部标签无任何遮挡与剪裁，线条圆润完整。
- **验证**：`dart format` 通过，`flutter analyze` 0 issues，`flutter test` 全部通过，`flutter build windows --debug` 编译成功。

## 18:08 (GMT+8)

### Feat: 新增 6 款柔和护眼纯色拼图背景并置顶前置

- **背景与调研**：原拼图界面仅提供无缝纹理图片（`assets/bg/tile_*.webp`）。经视光舒适度与拼图游戏场景色彩调研，精选 6 款低饱和（13%~29%）、柔和不刺眼、不抢戏的纯色背景：晨雾冷灰（`#E2E6EA`）、鼠尾草绿（`#D5DFD7`）、燕麦暖米（`#EBE5DC`）、静谧灰蓝（`#D2DCE6`）、暮云烟粉（`#E2D9DC`）、远山青黛（`#2B3338`）。
- **架构与数据**（`lib/data/game_repository.dart`）：
  - 定义 `kBackgroundColors`（6 个纯色）和组合全量列表 `kAllBackgrounds`，将 6 个纯色严格置于最前面（索引 0~5），纹理图片随后；
  - 提取 `GameBackground` 辅助类，支持纯色格式判断与 hex 色值解析；
  - `selectedBackground` 默认指向首个纯色背景（晨雾冷灰），并完全向后兼容旧版图片背景存储。
- **选择浮层与界面渲染**（`lib/widgets/choose_background_sheet.dart`, `lib/pages/game_page.dart`）：
  - `ChooseBackgroundSheet` 切换为全量列表，纯色选项直接绘制 `ColoredBox`，底部标签前 6 项展示中英多语言色彩名称，后续展示桌板序号；
  - `GamePage` 针对纯色背景直接渲染 `ColoredBox`（零 I/O、零纹理解析）；`_loadHeaderColor` 针对纯色直接取色混合计算导航条配色，避免图片异步解码与卡顿。
- **国际化与测试**（`lib/l10n/` & `test/widget_test.dart`）：
  - `zh.i18n.json` 与 `en.i18n.json` 新增 `background.colorLabels`；
  - 增补纯色与纹理背景展示及切换点击的单元/组件测试用例。
- **验证**：`dart format`（仅限代码）通过，`flutter analyze` 0 issues，`flutter test` 367 项测试全部通过，`flutter build windows --debug` 编译成功，Windows 平台集成测试 `app_test.dart` 100% 通过。

## 16:18 (GMT+8)

### Fix: 禁用亚麻布纹压花纹理（消除碎片表面经纬方格网纹）

- **背景**：高倍放大视口下，每个拼图碎片表面均显现出明显的规则方格网纹。
- **根因**：`LinenTextureManager` 的 4px 经纬线纤维贴图在碎片局部坐标系平铺绘制，未做抗缩放，放大 225% 时膨胀为 27px 的清晰方格；且原 `srcOver` 模式半透明硬盖在原图上造成遮盖。
- **改动**（`lib/logic/rendering/linen_texture_manager.dart`）：
  - 将 `LinenTextureManager.enabled` 设为 `false`，彻底停用碎片正面的方格纹理绘制，还原高清原图纯净色彩，并减少碎片渲染每帧的 GPU 绘制开销。
- **验证**：`dart format` 通过，`flutter analyze` 0 issues，`flutter test` 通过，`flutter build windows --debug` 成功。

## 15:50 (GMT+8)

### Fix: 修复 main.dart 两处 lint（prefer_final_locals / omit_local_variable_types）

- **改动**（`lib/main.dart:94`）：`WidgetsBinding widgetsBinding = WidgetsFlutterBinding.ensureInitialized();`
  改为 `final widgetsBinding = WidgetsFlutterBinding.ensureInitialized();`，消除两处 info 级告警；
- **验证**：`flutter analyze lib` 通过，No issues found。

## 15:45 (GMT+8)

### Perf: 碎片边线与阴影改为「基线屏幕恒定」逆缩放（高倍放大不再变粗）

- **背景**：高分片关卡（如 20×20）玩家放大视口后，碎片边线与阴影位移会被成倍放大——
  吸附绿框变粗胶带、冲压切缝变粗黑沟、贴地阴影下沉成黑条，同时遮挡图案细节。
- **根因**：`PuzzlePieceComponent.render` 运行在未缩放的局部坐标系中，Flame 调用前施加
  `canvas.scale(scale)`，故线宽/位移的屏幕呈现宽度 = `局部值 × scale`；而此前这些值是固定常量。
- **改动**（`lib/game/puzzle_piece_component.dart`，唯一改动文件）：
  - 新增 `static double screenInvariantValue(double screenValue, double scale)`，
    实现 `局部值 = 目标屏幕值 / max(scale, 1.0)`：放大时屏幕宽度恒等于基线，
    `scale <= 1`（托盘 / 1x / 过渡带下探）时退化为原值，**1x 与低难度托盘与改动前逐像素一致**；
  - 4 个描边画笔（暗线 1.2 / 高光 0.8 / 纸板底边 0.8 / 吸附绿框 2.5）线宽与 3 组位移
    （拖拽阴影 `(2,6)`、贴地阴影 `0.8`、纸板厚度 `(0.8,1.6)`）统一接入逆缩放，
    赋值集中置于 `render` 开头且无条件执行，避免静态画笔跨碎片复用时遗留上一片的值；
  - 拖拽阴影 `MaskFilter.blur` 因 `MaskFilter` 不可变、无法原地改半径，改为
    **3 档静态 Paint 位池**（`_dragShadowBlurTiers`）按缩放取档，保持零堆分配；
  - `triggerSnapGlow()` 移除边缘筛选的常驻绿框复辟赋值（筛选职责本就由
    `updatePieceVisibility` 的 `isFilteredOut` 承担），并引入**代数令牌**消除
    "连续两次吸附时前一次回调截短后一次流光"的竞态；连带删除失效的 `layout` 身份校验。
- **注意**：托盘态 `scale = _trayPieceScale`（= 64/碎片最大边，可为 0.2 左右）、
  拖拽过程为逐帧插值，二者**均不等于 `game.zoom`**，故逆缩放必须取组件自身 `scale.x`；
  视锥剔除仍使用原始 `scale`（口径不同，不可合并），已在代码注释中固化该约束。
- **验证**：新增 `test/screen_invariant_stroke_test.dart`（5 项契约测试，含极端缩小防暴增、
  非法 scale 防除零）；`flutter analyze` 改动文件 0 问题；`flutter test` 367 项全通过；
  `flutter build windows --debug` 成功。
- **文档**：方案与交叉评审见 `docs/puzzle-piece-zoom-stroke-render-optimization-final-20260913.md`
  及 `...-review-ds-...` / `...-bd` / `...-gm` 三份评审。

## 14:05 (GMT+8)

### Fix: 窗口尺寸变化不再复位缩放与平移（保留用户视野）

- **根因**：`_syncResizeTransform` 在每次真实尺寸变化时执行 `_setZoom(1)` + `_panOffset.setZero()`，
  于是 Windows 拖拽/最大化窗口、移动端旋转屏幕等都会把用户放大好的视图清成 1.0x。
- **改动**（`lib/game/jigsaw_puzzle_game.dart`）：
  - `onGameResize` 在 `_computeLayout()` **之前**记录"旧几何 + 旧视口"下的归一化视图中心（新增
    `_normalizedViewCenter`），并在 `onLoad` 结束时记录 `_lastGameSize` 作为基准尺寸；
  - `_syncResizeTransform` 改为：保留 `_zoom` 并收敛到新几何推导出的 `_maxZoom` 之内
    （窗口变大 → 碎片变大 → `maxZoom` 下降，不收敛会出现 `_zoom > _maxZoom` 越界）；
    把同一归一化视图中心重新对准新视口中心后调用 `_clampPanOffset()` 收敛平移；
  - 新增 `_legalDomain()` 作为归一化合法摆放域的**唯一权威定义**，
    `_clampPanOffset` / `_legalDomainScreenRect` / 新增的 `_isNormalizedInDomain` 全部复用它，
    杜绝多处域定义漂移；
  - 游离碎片收拢判据由"是否在棋盘 `[0,1]` 内"升级为"是否在**当前模式合法域**内"：
    保留缩放后桌面散落碎片与"放大后合法停放在视口外"的碎片都不再被误判为越界而反复搬动
    （这是保留缩放的**必要前置**，否则改窗口仍会把碎片拽回）。
- **测试**：新增 2 条用例（尺寸变化保留缩放 / 保持视图中心 / 平移已收敛；窗口变大导致 `maxZoom`
  下降时缩放被收敛在 `maxZoom` 之内）。
- **验证**：`flutter analyze` 零告警；全量 362 项测试通过；Windows debug 构建与
  `integration_test/app_test.dart` 通过。

## 13:45 (GMT+8)

### Fix: 棋盘放大后已拼合碎片组无法移动、松手被钉回中部（核心拼图体验）

**现象**：棋盘放大后，多块已吸附的碎片组几乎拖不动、松手被钉回屏幕中部；单块碎片手感正常。
放大后也无法把拼好的碎片组推出可视区暂存。

**根因**：碎片拖拽与松手的"安全限位"以**屏幕视口**为边界，却用放大后的实际像素尺寸计算
**整簇包围盒**。当缩放 ≥ 2（各难度 `maxZoom` 恒为 2.0）且集群投影尺寸超过可用空间时，
`safeMin > safeMax`，边界区间被 `min/max` 兜底反转成**一个点**，落点退化为极少数离散位置
（实测 4×4 盘 2×2 集群垂直行程为 0）。该约束由提交 `a5deb7b`（2026-09-03）引入。

**改动**（`lib/game/jigsaw_puzzle_game.dart`）：

1. 拖拽/松手限位对象由"整簇包围盒"改为"**主片中心**"单点约束，新增唯一入口
   `_clampDragTarget` 与 `_dragCenterSafeBounds`，拖拽期与松手期共用，杜绝两处公式漂移；
   约束区间恒为正，数学上不可能退化。
2. 集群其余成员允许伸出视口 → 恢复"放大后把拼合好的碎片组推出可视区暂存"的能力；
   同时"主片中心恒在视口内"保证碎片永不完全消失、随时可再次抓取。
3. 窗口尺寸变化时的游离碎片收拢判据由"屏幕视口"改为"**合法摆放域**"
   （新增 `_legalDomainScreenRect`，与 `_clampPanOffset` 的归一化域同源），
   否则每次改窗口都会把已摆到视野外的碎片再次拽回、问题复发。
4. `missingPieceCheck`（仅剩 ≤2 块未归位时）的回收目标由"固定搬回屏幕正中"
   改为"就近落在视口内的完整可见位置"，避免刚刚推到视野边的碎片被瞬移回中央。

**测试**：重写 `test/game_layout_test.dart` 旧契约 1 条（原断言"拖出屏幕必须被拉回视口内"），
新增回归用例 2 条（极端拖拽下主片中心恒在安全区且松手不回中；放大 2.0x 后集群行程不再为 0
且可推出视口暂存）。全量 360 条测试通过；`flutter analyze` 无新增问题；
Windows debug 构建与 `integration_test/app_test.dart` 均通过。

**遗留（未做，待确认）**：

- `_syncResizeTransform` 在窗口尺寸变化时仍会 `_setZoom(1)` 复位缩放并清空平移（丢失用户缩放状态），
  属独立问题，未纳入本次范围。
- 托盘模式下放大后的集群允许覆盖底部托盘区域：原"集群绝不遮挡托盘"约束与"允许移出视口"
  在数学上不可兼得，本次按后者（用户核心诉求）优先取舍。
