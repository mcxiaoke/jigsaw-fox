# JigsawFox Changes - 2026-09-12

## 19:35 (GMT+8)

### Fix: 彻底根治无扩展包时 UnifiedCatalogIndex 与 MyCenterTabView 的 300ms 死循环性能缺陷

- **斩断索引构建死循环**:
  - `catalog_index.dart`：在 `UnifiedCatalogIndex.build()` 中直接读取内存中的 `packsNotifier.value`，彻底移除 `packsNotifier.value.isNotEmpty ? ... : await loadAllPacks()` 逻辑；
  - 索引构建彻底收敛为纯内存只读操作（与主线、每日、自制、活动、画集对齐），消除每 300ms 一次的磁盘 I/O、无意义的 `loadAllPacks` 扫描与错误置脏。
- **扩展包管线通知去重防御**:
  - `pack_content_pipeline.dart`：`loadAllPacks()` 在更新 `packsNotifier.value` 前增加 `listEquals` 比对保护；当本地无包或包内容未发生变化时，绝不重新赋值 `packsNotifier.value`，切断无变化的虚假通知源。
- **数据模型相等性规范**:
  - `puzzle_pack_item.dart`：为 `@immutable` 类 `PuzzlePackItem` 实现 `operator ==` 与 `hashCode`，保障 `listEquals` 在包列表重扫描时能准确判等。
- **测试与验证**:
  - `test/logic/pack_content_pipeline_test.dart` 新增用例验证 `loadAllPacks` 在列表无变化时不触发 `packsNotifier` 监听器；
  - `flutter analyze` 零告警（No issues found）；
  - 全量 359 项单元测试 100% 通过（359 passed）；
  - Windows 原生 Debug 构建（`flutter build windows --debug`）验证成功。

## 19:30 (GMT+8)

### Refactor: 彻底精简并移除未发布版本的旧版自愈代码（减负 214 行）

- **精简移除生产兼容代码（93 行）**:
  - `TempStorageConfig` 移除 `legacyLevelsSubDirsToHeal`、`legacyTempPrefixes`、`legacyTempSuffixes`、`legacyTempContains`；配置类彻底收敛为仅保留 `temp`、`downloads`、`extract` 三个核心目录名常量；
  - `TempStorageManager` 彻底移除 `selfHealStaleLevelsArtifacts()` 扫描与清理逻辑；
  - `ContentManager.initialize()` 移除自愈调用，冷启动 GC 仅执行极简高效的 `cleanStaleTempDirectory()`（直接清空重置 `temp/` 暂存区，无需遍历 `levels/` 下各业务目录）。
- **精简测试代码（121 行）**:
  - 移除对已废除常量的断言以及针对多业务子目录历史自愈的测试用例，保留核心路径生成、覆盖提升、拷贝回退、红线 R1 失败回滚及启动清空暂存区测试。
- **验证通过**:
  - `flutter analyze` 零告警，全量 358 项单元与 UI 测试 100% 通过。

## 19:18 (GMT+8)

### Fix & Fortify: 落实审查反馈，恢复目录提升备份回滚保障（红线 R1）并支持 .bak_/.staging_ 崩溃自愈

- **恢复原子提升备份回滚范式（P0 / 红线 R1）**:
  - 重构 `TempStorageManager.promoteExtractDir`：废除直接 `deleteSync` 破坏性预删旧目录的做法；
  - 严格落实**备份-替换-回滚**范式：若目标目录存在，先原子改名为同级 `.bak_<ts>` 备份；
  - 优先尝试同卷原子 rename；若捕获异常（跨卷或特殊环境）则进入安全 staging 拷贝回退；
  - 成功落位后安全删除备份并触发 `sweepStaleBackupSiblings` 回收旧备份；
  - 任何环节异常失败时，自动清理 staging 临时残留，并将 `.bak_<ts>` 完整回滚还原至正式目标路径，彻底杜绝数据损坏与丢失。
- **跨卷拷贝分阶段隔离保护（P2）**:
  - `promoteExtractDir` 拷贝回退不再直接拷入目标目录，而是先拷贝到目标同级 `.staging_<ts>` 临时目录，完整拷完后再同级原子 rename 替换目标，避免拷到一半断电/失败留下残缺损坏数据。
- **冷启动自愈覆盖崩溃遗留（P1）**:
  - `TempStorageConfig` 补充 `legacyTempContains = ['.bak_', '.staging_']`；
  - `selfHealStaleLevelsArtifacts` 新增对子串包含 `.bak_` 与 `.staging_` 的孤儿残留清理，确保原子替换过程中的崩溃残留能在冷启动阶段被彻底自愈。
- **测试与验证**:
  - `test/logic/temp_storage_manager_test.dart` 新增提升失败完整回滚验证（断言目标目录原文件完好无损、无孤儿残留）、分阶段拷贝回退验证、以及 `.bak_` 与 `.staging_` 残留自愈验证；
  - `flutter analyze` 零告警，全量 359 项单元测试通过，Windows 原生 debug 构建与端到端集成测试（`integration_test/app_test.dart`）全绿通过。

## 19:05 (GMT+8)

### Refactor: 下载暂存区物理隔离与冷启动零竞态自愈清扫

- **物理隔离（Temp Isolation）**:
  - 新增 `TempStorageManager` 与常量配置 `TempStorageConfig`，统一定义暂存目录与历史扫描规则；
  - 所有下载中的临时文件（`*.part`、临时 `*.zip`）重定向至专用暂存区 `appSupportDir/temp/downloads/`；
  - 所有解压与校验中的临时目录重定向至专用暂存区 `appSupportDir/temp/extract/`；
  - 校验通过后通过同卷原子移动（`promoteExtractDir`）一次性提升至正式业务目录，彻底杜绝 `levels/` 正式目录被临时碎片污染。
- **全业务管线接入**:
  - `CollectionsContentPipeline`、`EventsContentPipeline`、`DailyContentPipeline`、`PackContentPipeline` 统一接入 `TempStorageManager` 生成下载与解压路径；
  - 移除运行时扫描清理逻辑（运行时 0 GC），杜绝误删正在下载中 `.part` 文件的风险。
- **冷启动清理与历史自愈（Startup GC & Self-Healing）**:
  - 在 `ContentManager.initialize()` 启动最早期、前后台尚未发起任何网络请求时执行启动清理；
  - 清空暂存区 `appSupportDir/temp/`，回收所有异常中断或强杀遗留的死文件；
  - 执行一次性自愈扫描，安全清除此前遗留在 `levels/collections/`、`levels/events/` 等正式目录中的历史 `*.part` 与 `temp_*` 碎片，绝不触碰正式业务数据（严格遵循红线 R1）。
- **测试与验证**:
  - 编写 `test/logic/temp_storage_manager_test.dart` 覆盖常量配置、隔离路径生成、原子提升、冷启动清扫与旧目录自愈（全部通过）；
  - `flutter analyze` 零告警，单元测试全绿，Windows 原生端端到端集成测试（`integration_test/app_test.dart`）全绿通过。

## 18:42 (GMT+8)

### Test: 补全小包自动下载核心过滤与真下载正向断言

- **注入 FakeContentHttpClient**: 在 `test/logic/auto_download_test.dart` 中实现 `_FakeDownloadHttpClient` 与动态内存 ZIP 构造器，记录真实请求下载的 URL 列表并向管线供给合规包含图片的 ZIP 二进制。
- **真逻辑断言**:
  - 精准断言仅有合规的 2 个小包（`event_small`、`col_small`）进入下载队列并被请求；
  - 精准断言超大包（>20MB）、未知大小（0字节）、已下架（`delisted`）、已禁用（`disabled`）、本地已就绪（`already_downloaded`）等排除项绝不被触发下载；
  - 正向验证合规小包在本地解压后完成状态落位（`isLocalDownloaded = true`），排除项保持未下载。

## 18:22 (GMT+8)

### Optimize: 下载进度节流与自动下载启动等待微调

- **下载进度通知节流微调**: 将 `CollectionsContentPipeline` 与 `EventsContentPipeline` 的下载进度通知节流由 2000ms 调整至 1000ms（每秒派发一次中间进度），大幅缓解下载过程中进度条停滞迟缓的感知体验。
- **自动下载启动等待延时扩展**: 将 `ContentManager.triggerAutoDownloadSmallPacks` 的默认启动前检查延时 `initialCheckDelay` 由 1 秒提升至 5 秒，给首屏 UI 加载和首页图片初始请求预留更宽裕的缓冲窗口。

## 18:02 (GMT+8)

### Feature: 活动与图集小包（<=20MB）低优先级静默自动下载

- **小包安全过滤**: 在 `ContentManager` 中新增小包判定条件（`kAutoDownloadMaxSizeBytes = 20MB`），仅对 `0 < fileSizeBytes <= 20MB`、有效未下架、未下载的 Zip 活动与图集进行自动下载；未知大小排除，防止下载超大包。
- **低优先级与避让机制**:
  - 启动及全量元数据同步完成后进入后台队列（`unawaited`，零阻塞 UI 刷新与 `syncAll` 返回）；
  - 进入首页启动后，先等待所有前台在途请求完成（`LevelImageResolver.hasInFlightRequests` 与 `MainContentPipeline.hasInFlightDownloads` 全部为 0）；
  - 单包下载采用严格串行 FIFO 队列，单包下载前后主动让行前台图片下载，包间设有 1.5s 冷却间隔，杜绝抢占首页关卡和封面图。
- **与既有功能无缝兼容**: 复用已有的 `ensureEventDownloaded` 与 `ensureCollectionDownloaded`（Single-Flight 单飞复用与原子落位），UI 卡片角标（`DownloadBadge`）自然感知并同步进度，用户手动点击直接合流。
- **测试与验证**: 新增 `test/logic/auto_download_test.dart` 覆盖阈值、条件过滤、前台避让与防重入测试；`flutter analyze` 零告警，`flutter test` 及 Windows 原生端 debug 构建与端到端集成测试验证全绿。

## 17:30 (GMT+8)

### Fix: flutter analyze 全量清零（148 → 0，含全部 info）

- **总计修复 148 条 analyzer 提示**，涉及 lib 25 个文件 + test 12 个文件；最终 `flutter analyze` 输出 **No issues found**，`flutter test` 345 项全部通过（8 skipped），改动文件已 `dart format`。
- **discarded_futures / unawaited_futures（约 60 处）**: initState/dispose/回调中的 Future 调用统一 `unawaited()` 包裹并注释原因（启动加载、音效、弹窗、导航跳转、订阅取消等）；必要文件补 `dart:async` import。
- **ignore+注释（3 处，唯一不能直改项）**: Flame 引擎 `add()` 返回 `FutureOr<void>`（未加载时同步完成），无法用 `unawaited` 包裹，`jigsaw_puzzle_game.dart` / `puzzle_piece_component.dart` 改用行级 `// ignore: discarded_futures` 并附说明。
- **API 替代**: `locale_service.dart` 与测试中 slang `setPluralResolver`（返回 Future）改用官方同步版 `setPluralResolverSync`；`victory_dialog`/`game_toast` 的 `AnimationController.forward()`（TickerFuture）补 unawaited。
- **机械修复**: 构造函数参数顺序（`always_put_required_named_parameters_first`）、test 内 23 处 `prefer_const_constructors` 及嵌套 const 去重、2 处多余 import 删除、`(_, __, ___)` → `(_, _, _)`、文档注释方括号私有成员引用改反引号、`!f.isFile == false && f.isFile` 化简为 `f.isFile`、`document_ignores` 补说明注释。
- scripts/publish/app_reference 的 16 条 lint 随目录删除而消除（历史文件，未修改）。

## 16:35 (GMT+8)

### Fix: P2/P4 重点项（避免慢异步IO全清 / 持久化收敛 / 并发锁与防错位）

- **P2-6 / P4-9 慢异步IO（`avoid_slow_async_io`）全仓清零（61 处）**:
  - 严格遵循 Dart 官方推荐规范（[avoid_slow_async_io](https://dart.dev/tools/diagnostics/avoid_slow_async_io)），仅将**元数据查询方法**（`File/Directory.exists()`, `stat()`）替换为同源同步等价方法（`existsSync()`, `statSync()`），消除底层无意义线程调度开销；
  - 真实的数据操作与文件系统 I/O（`create()`, `delete()`, `copy()`, `rename()`, `read()`, `write()`, `length()`）**严格保持异步 `await`**，绝无阻塞主线程；
  - 覆盖 `lib/` (50 处)、`test/` (8 处)、`tool/` (2 处)、`scripts/` (1 处)，经 `dart analyze` 验证，该诊断彻底由 61 处归零。
- **P2-3 `game_repository.dart` 进度与快照逻辑去重**:
  - 提取统一的私有方法 `_commitProgress(...)`，收敛 `updateLevelProgress`、`updateCustomProgress` 与 `updateGenericProgress`（覆盖 `updateDailyProgress`）三处重复约 90 行的残局快照校验、保存、精准按难度键清理与纯进度更新逻辑，消除 120+ 行脆弱冗余。
- **P2-5 `CanonicalId` 领域真值源（SSOT）收敛**:
  - 补全 `CanonicalId.forMain`（统一格式化为三位数字补零 `main:001`）与 `CanonicalId.forDaily`（统一剔除横线 `daily:20260912`）；
  - `GameRepository.canonicalForLevel / canonicalForDaily / canonicalForCustom / canonicalForPack` 全部委托至 `CanonicalId`，彻底消除 ID 命名分叉风险。
- **P2-7 `recordSnapStats` 防竞态保护**:
  - 为 `recordSnapStats` 增加 `Future<void> _snapStatsLock` 链式调用队列，避免拼图多块连续或并发吸附时引发 read-modify-write 数据竞态覆盖。
- **P2-8 `triggerSnapGlow` 关卡边缘布局快照防错位**:
  - 在 `triggerSnapGlow()` 的 380ms 异步延迟前提前捕获 `final layout = game.edgeLayout;`，杜绝用户在残局快速切关时延迟回调读取到新关卡布局导致数组越界或发光高亮错乱。
- **P2-2 `atomic_replace.dart` 补齐回滚失败日志**:
  - 将原子替换中回滚失败时的空 `catch (_) {}` 替换为 `AppLogger.content.severe(...)`，告警关键数据目录回滚失败。
- **P4-4 / P4-5 卫生项**:
  - `pubspec.yaml`：清理顶部和底部的 Flutter 默认脚手架注释；
  - `snapshot_store.dart`：收敛 `init()` 中三处重复的 `_initialized = true` 赋值至 `finally` 块。
- **验证**:
  - `dart analyze` 0 error / 0 warning（`avoid_slow_async_io` 全清）；
  - `dart format` 仅改动文件通过；
  - `flutter test` 345 用例全绿；
  - `flutter build windows --debug` 编译成功；
  - `flutter test .\integration_test\app_test.dart -d windows` 端到端全通。
- **备份**: 改动前原件备份至 `temp/backups/p2-p4-20260912-162000/` 与 `temp/backups/game_repository.dart.20260912_1630.bak`。

## 15:42 (GMT+8)

### Fix: P1 批次——小改动 / 高收益（7 项，对应遗留台账 §3 P1）

- **P1-1 `pieceById` O(n)→O(1)**: `PuzzleBoardState` 增加惰性 `Map<int, PieceState>` 索引（`puzzle_state.dart`），`pieceById`/`pieceByIdOrNull` 改查索引；`computePlantedPieceIds` BFS 由 O(n²) 降 O(n)，24×24 大图收益最明显。附带发现并修正：构造函数去 `const` 会波及 test/ 下 8 处 `const PuzzleBoardState(...)` 调用点（已同步去 const；曾尝试 `late final` 保 const，被 `late_final_field_with_const_constructor` 证伪——Dart 禁止该组合）。
- **P1-2 死代码清除**: 删 `kHotTagIds`（home_tab_view.dart，零引用）；删 3 处 `_ensureXxx*` l10n 验证脚手架（choose_difficulty_sheet / achievements_page / my_center_tab_view，共约 90 行 `print` 拼接 + 注释桩，含 `avoid_print` 压制）。
- **P1-3 废弃 API 与死码**: 删恒返 0 的 `GameRepository.totalCompletedLevels`（@Deprecated，零引用）；删 `ResumeHelper` 中已废弃不生效的 `isCompleted` 参数（`fetchResume` 可选参 / `maybeShowResumeDialog` / `tryHandleResumeFlow` 共 3 处）及 4 个调用点传参——`displayProgress` 的 `isCompleted` 是生效逻辑，保留。核查修正：`_initLevels()` 并非死码——`reloadBuiltinLevelsForTest()` 被 3 个迁移测试用作桩数据源，保留。
- **P1-4 恢复 `avoid_catches_without_on_clauses`**: analysis_options.yaml 由 false 恢复 true；存量 285 处裸 catch 分层豁免——数据/管道/服务/缓存层等 45 个文件文件级 `ignore_for_file` + 理由头（防御式容错是既定设计），UI 层 20 个文件 47 处逐处 `// ignore` + 理由（best-effort 语义显式化）。豁免布局采用「catch 上一行独立注释」——行尾式会被 `dart format` 重排进 catch 体导致抑制失效（已实测踩坑并统一修正）。新增裸 catch 从此必须带理由豁免或改 `on` 子句。
- **P1-5 pubspec 卫生**: `intl: any` → `^0.20.2`（slang 生成代码直接 import，须显式）；`flutter_launcher_icons` 移入 dev_dependencies；删零使用的 `cupertino_icons` 与未接线的 `flutter_lints`（lint 走 very_good_analysis）。
- **P1-6 补 `@immutable`**: `PieceState` / `PuzzleBoardState` 之外的 5 个重载 `==`/`hashCode` 的类补注解：`CropRatioOption` / `EdgeCurveDescriptor` / `PieceEdges` / `PuzzleDifficulty` / `PieceState`（其中 4 个纯逻辑文件引入 `package:meta`，pubspec 显式声明 `meta: ^1.18.0` 避免 depend_on_referenced_packages）。等值 lint 10 处全清。
- **P1-7 快照测试隔离**: `SnapshotStore` 新增 `@visibleForTesting initForTest(Directory)`（同步建目录 + 置 `_initialized`，业务侧 init 自动短路）；`test_helper.dart` 的 `initTestStorage`/`initTestStorageWithBackups` 注入临时 home 下的独立 `snapshots/` 目录，随 tearDown 一并物理清理——测试不再读写全局 `%TEMP%\jigsaw_snapshots`（实测该目录已有历史残留 6 个文件，改后零增长）。
- **验证**: `flutter analyze` 0 error / 0 warning（210 info，较批次前 220 净降 10）；`flutter test` 全绿（345）；`dart format` 0 changed；`flutter build windows --debug` 成功；集成测试通过。
- **备份**: P1 改动前原件备份至 `temp/backups/p1-20260912-143454/`。

## 14:30 (GMT+8)

### Fix: 代码质量 Review 高危项修复（写盘防丢 / 启动可重试 / 结算去重）+ 遗留问题台账

- **范围**: 从三份独立审查（`flutter-code-quality-review-a/b/c-20260912.md`）中筛出「真实 + 严重 + 紧迫 + 改动小收益大」的问题并修复；其余去伪存真后合并为遗留台账。
- **数据安全（核心）**:
  - 新增 `putRaw()` / `deleteRaw()` 统一原语（`storage_manager.dart`），与既有 `putJson()` 同源，均纳入 `_pendingWrites` 关窗挂起队列；原 `_box.put`/`_box.delete` 直写不跟踪，强杀/关窗可能静默丢档；
  - `achievement_store.dart` 4 处写 + `economy_service.dart` 14 处写 + `game_repository.dart` 6 处写全部改走新原语——**成就/金币/券此前完全不在防丢队列**（review-c H9 只发现 achievement，economy 的 12 处为本次新发现，金币比成就更敏感）。
- **可观测性**: `achievement_store` 4 处 `catch (_) {}` 全静默改为 `catch (e, st)` + `AppLogger.repo.warning`（含 reset 删除路径）。
- **可恢复性**: `AppContent._initFuture` 由「无条件缓存」改为 `.onError` 清除缓存并透传（`app_content.dart`），修复 manifest 损坏/版本不兼容时「进程内永久失败、只能重启 App」。
- **可维护性**: `jigsaw_puzzle_game.dart` 抽出 `_applySnapSettlement()`，消除 `handlePieceDragEnd` 与 `hint` 之间约 90 行的结算管线逐行复制（同步 clusterId/rot → 动画 → 刷新 → 4 回调 → 胜负判定），两侧只传差异参数（动画时长/层级提升/兜底 ID/日志 tag），杜绝改动一侧漏另一侧的行为分叉。
- **测试**: 删除 `test/services/unlock_service_test.dart`（3 个「只调不验」假测试，仅断言常量，删实现也全绿，反而制造虚假安全感）；按用户决定暂不做行为补测。
- **文档**: 新增 `docs/code-quality-review-remaining-issues-20260912.md`——三份 review 的去伪存真合并版，含已修复项、**误报/夸大清单**（如 "25 处裸强转" 实为 16 处、"50 处慢 IO 应改 existsSync" 方向相反、`locale_helper` 并非死码）、P1~P4 分级遗留清单与执行顺序。
- **核验结论**: `pieceById` 为 O(n) 线性扫描（`puzzle_state.dart:272`）属实；`avoid_slow_async_io` 实测 61 处（lib 50 / test 8 / tool 2），但多数在非 UI 路径，review-b 建议的「全改 existsSync」方向错误。
- **备份**: 改动前原件备份至 `temp/backups/*.20260912-141753.bak`。
- **验证**: `flutter analyze` 0 error / 0 warning（147 info）；`flutter test` 全绿；`flutter build windows --debug` 成功；`flutter test ./integration_test/app_test.dart -d windows` 通过。

## 14:05 (GMT+8)

### Docs: 架构债务重构方案 v2（对照源码核验后修订）

- **范围**: 对 `docs/architecture-debt-refactor-roadmap-20260912.md`（v1）进行逐条源码核验，输出修订版 `docs/architecture-debt-refactor-roadmap-v2-20260912.md`；v1 就地标注为已废止并保留对照。
- **核验发现（v1 与代码不符处，共 10 项，节选关键 5 项）**:
  - `app_content.dart:154-156` 的 `_attachPipelineForwarding()` 仅挂 events/collections/pack 三条通知，`mainPipeline`/`dailyPipeline` 均无 `updateNotifier`；main/daily 仅靠 `syncAll` 成功后手动 bump（`:301`/`:317`），游戏内单关懒下载不 bump。v1 称"已完成各管线聚合"不成立；
  - 页面置脏信号实为 3 类，其中 UGC 走 `GameRepository.customPuzzlesNotifier`（`my_center_tab_view.dart:71-73`）、Locale 走 `LocaleService`（`catalog_index.dart:66-71`），均不经过 `contentUpdateNotifier`；
  - `_reloadSeq` 是页面装配级并发保护（`_loadAllData` 在 `current()` 后仍有 2 个 await，`:128`/`:130`），不属索引代次体系，不可随 P2-1 退场；
  - v1 示例调用的 `getLevelById`/`getLevel`×3/`getLevelByDate`/`getCustomPuzzleLevel` 全仓库零命中（6 个方法均不存在），且现有 `getLevelsForCollection`/`getLevelsForEvent`/`getPackLevels`/`getLevelsForMonth` 均为 `listSync()` 扫盘实现，"O(1) 路由"措辞修正为"确定性路由"；
  - 删除 resolver main 分支会连带丢失 P0-6 fileSizeBytes 校验与 `levels/main/` ↔ `levels/network/` 去重（`content_manager.dart:31` vs `level_image_resolver.dart:54`），v1 未列调用方迁移表。
- **v2 主要修订**:
  - 新增第〇节"v2 修订说明"对照表（10 项，逐条附代码证据），并显式保留 v1 中核验属实的部分；
  - Step 划分由 4 步改为 6 步并按依赖重排：**先定 main 下载出口（含 5 个调用方迁移表）→ 再补 7 个查询方法 → 删 Locator（含 2 处红线测试迁移 + 未收藏进行中关卡补集专项验证）→ 补信号源后自愈 → 引擎持有 → 收紧必传**；
  - 抓手 1 补充 main 分支四项职责（下载/回灌/fileSizeBytes 校验/防双副本）的承接取舍（方案 A 保留只读 probe / 方案 B 接受双副本）；
  - 抓手 2 明确"确定性路由"与"真 O(1)"两条路线取舍，建议选项 ①；并指出 UGC 分支需新增 `CustomPuzzleItem → PuzzleLevelItem` 适配方法（当前不存在）；
  - 抓手 3 增加"索引信号源清单"交付物，明确 `_reloadSeq` 保留或串行化；
  - 抓手 4 拆为 4a（引擎持有）/4b（收紧必传）/4c（写盘事务），并明确"写路径去默认、读路径兼容保留"（`puzzle_state.dart:210` 按红线 R2 保留）；
  - 验收指标口径修正：跨层 import 当前实为 2 处（删 Locator 后剩 1 处，是否归零取决于 probe 决策）、不预设"净减 ~300 行"、测试基线不写死条数；行数 247→**246** 修正。
- **备份**: 原 v1 文档已备份至 `temp/backups/architecture-debt-refactor-roadmap-20260912.md.bak-20260912-140245`。
- **验证**: 纯文档变更，未触及代码；核验结论均附 `文件:行号` 证据。

## 13:41 (GMT+8)

### Feature: P2-2（图片失败重试与自愈）与 P2-3（下载进度 2 秒节流）极简落地

- **范围**: 实施 `docs/code-review-and-fix-plan-20260912.md` 中留待后续的 P2-2（图片失败重试入口）与 P2-3（下载进度局部刷新/节流）两项体验与性能项。
- **改动**:
  - **P2-3 下载进度 2 秒节流**（`events_content_pipeline.dart`、`collections_content_pipeline.dart`）：
    - 在管线进度更新源头（`_updateDownloadProgress` / `_updateDownloadState`）增加时间戳节流门控：中间下载进度限制为至多 2 秒派发一次，终态（0.0 或 >= 1.0）无条件立即放行，彻底消除 HTTP 数据流高频 chunk 击穿 UI 线程高频重建的问题；
    - 对齐 `collections_content_pipeline.dart` 的 `isDownloading` 与 `getDownloadProgress` 辅助 API。
  - **P2-2 图片失败点击重试与再进自愈**（`app_cached_image.dart`、`lazy_level_image.dart`）：
    - 针对未落盘的远程网络图片与懒加载缩略图，在失败态（`_failed == true`）下包裹 `GestureDetector(onTap: _retry)`，用户点击裂图区域即可原地清空失败标志并重新触发下载；
    - 默认失败占位图标由静态 `imageBroken` 切换为更具交互暗示的 `arrowClockwise`；
    - 在 `didUpdateWidget` 中增加 `_failed && path == null` 状态检测，当页面重新构建或再进入刷新时自动发起重试。
  - **单测补充**（`test/widgets/app_cached_image_test.dart`）：
    - 新增 `AppCachedImage` 与 `LazyLevelImage` 点击失败态重试自愈用例（共新增 2 条单测）。
- **验证**:
  - `flutter analyze lib/`：**0 error / 0 warning**（145 条均为既有 info）；
  - `flutter test`：**348 通过 / 8 跳过 / 0 失败**（在 346 基线基础上净增 2 条重试单测全部通过）；
  - `flutter build windows --debug`：**通过**（生成 `JigsawFox.exe`）。

## 13:09 (GMT+8)

### Fix: v8 实施后的独立复审修正（① ② ③ ⑤ a b c e）

- **范围**: 对 `docs/code-review-and-fix-plan-20260912.md` v8 实施结果做逐条代码复审 + 实跑验证后，修完全部 8 项（计划文档已升 v9，见其 §9）。
- **逻辑修正**:
  - **① hash 延迟推进**（`main_content_pipeline.dart`）：远端 hash/url 变更只登记 `_pendingRefresh`，**不再先推进 hash**；刷新成功才写回新 hash/url/localPath。此前失败后两边 hash 已同值，下次 sync 不再重试，首页会长期显示旧图（旧图本身按 R1 保留，问题在"永不重试"）。`pendingRefresh` 随缓存持久化，跨重启保留待重试项。
  - **② 有界并发刷新**：元数据循环只做检测/登记，循环后由 `_refreshPendingImages` 每批 4 个并发执行，避免一次全量更新时串行下载拖慢首启同步（首启同步有预算）。
- **i18n**: **③** 9 处硬编码中文全部改用 slang key —— 新增 `myCenter.toast.imageNotFound / imageNotReady / openFailed`、`collections.delistedBadge / delistedCantDownload`（zh + en），`dart run slang` 重新生成；英文环境不再出现中文。
- **红线收口**: **e** `main` 全新关卡分支的 2 处 fileSizeBytes/sha256 失配删除改为**只标记不可引用**（`localPath=null` / `isLocal=false`），后续懒下载经 `.part` 原子覆盖 —— 至此 `lib/` 内**再无删除既有图片文件的路径**；**a** 新增 `cleanupStaleAtomicArtifacts`（`temp_*` + `*.bak_*`）供 events Auto-GC 使用，并在两个 swap 成功后调用 `sweepStaleBackupSiblings`（10 分钟龄期保护，避免误删并发中的备份），回收"改名为备份→删备份"之间崩溃的残留。
- **健壮性/清理**: **⑤** 删除 `_resolveImageBytes` 第三级裸 `HttpClient`（上一级已按 Single-Flight 落盘；重复请求不落盘且让离线用户多等 8s+），失败直接返回 `null` 交由 Toast 收口；**b** `LocalImageLocator` 扩展名探测列表改为派生自共享常量 `kImageExtensions`（消除重复清单与未引用常量）；**c** `UnifiedCatalogIndex` 新增 `_invalidations` 代次计数，构建期间发生 `invalidate()` 时保持 dirty，避免失效被在飞 build 吞掉。
- **验证**: `flutter analyze` **0 error / 0 warning**（201 条均为既有 info）；`flutter test` **346 通过 / 8 跳过 / 0 失败**；`flutter test integration_test/app_test.dart -d windows` 通过（含 Windows debug 构建）；红线静态检查正式目录递归删除仅剩白名单 5 处、`main_content_pipeline` 内删除调用归零。
- **计划文档**: 升 v9（头部修订说明 + §9.1 修正明细表 + §9.2 实测 + §9.3 未纳入项）；§7.3 债务登记表新增 `LocalImageLocator` 跨层依赖与"失配文件不删除"两行；§7.5 指标补 v9 实测列（跨层调用仍存 2 处、静默吞异常 0、远端驱动删除 0、补丁退场率 1/10）。

## 11:45 (GMT+8)

### Redline: 代码审查修复计划阶段一~三实施完毕（P0/P1/P2/P3）

- **范围**: 实施 `docs/code-review-and-fix-plan-20260912.md` 阶段一（P1-1/P0-1~P0-7/P0-3）、阶段二（P1-3/P1-4/P1-5/P1-7；P1-6 已提前解决）、阶段三（P2-1/P2-4/P2-7/P2-9/P2-10/P2-11、P3-1~3/P3-5/P3-6）。P2-2（图片失败重试入口）、P2-3（下载进度局部刷新）为体验优化未纳入本次止血范围，转后续。
- **红线合规（R1/R2）**:
  - events/collections 差集物理删除与 Auto-GC 物理删除已移除，改为 `isDelisted` 标记（重新上架自动清除）；统一索引数据源扩为「可见 ∪ 本地已下载」；货架下架项显示「已下架」并禁用下载（已下载仍可玩）。
  - main 管线 hash/url 变更不再预删旧图，新图经临时路径下载校验后原子替换，失败保留旧图。
  - 孤儿卡主点击不再弹删除框，改为 `LocalImageLocator` 多级取图后正常入局；删除收敛为长按二次确认（文案已明确仅删记录、不动已下载图片）。
  - 解压替换（collections/events/daily/主缓存 JSON）统一经 `atomic_replace.dart` 备份回滚；空包（0 张有效图）不再标记已下载。
- **数据链路**: `LevelImageResolver` 双重守卫（sourceModule + `main:` 前缀）+ 管线内侧拒收非 main；「我的」页读图复用 Resolver 缓存（离线可玩）；`_flushSync` 注入真实 canonicalId + `PuzzleBoardState.canonicalId` 默认改空串回落 levelId；图片白名单收敛为 `image_formats.dart` 共享常量（webp/jpg/jpeg/png/gif/bmp/jfif/avif）；`_loadAllData` 入口 invalidate + AppContent 聚合管线通知；`_loadAllData` 代次防重入；索引 Single-Flight；`SnapshotStore.saveSync` 未初始化直接返回；结算链路 finally 保弹窗；events 补 `deleteDownloadedEvent` + 详情页清理入口；Array 分支补持久化/通知（best-effort 不阻塞）；死代码清理（DownloadBadge/LazyLevelImage/不可达分支）；移除 `cleanLegacyThumbnailCache`；快照清理补红线边界注释；中英 orphanDialog/clear 文案更新（`dart run slang`）。
- **验证**: `flutter analyze` 0 error / 0 warning；`flutter test` 346 通过 / 8 跳过 / 0 失败（含新增 `test/logic/redline_retention_test.dart` 12 项：R1 留存×2、P1-7 空包×2、P1-1 防污染×2、P0-6 旧图保留、P0-7 只读定位、R2 孤儿保进度、P0-2 下架可索引、P1-4 回落、P2-10 单飞）；`flutter build windows --debug` 成功；`flutter test integration_test/app_test.dart -d windows` 通过；`main_levels_cache.json` 无污染（180 项全 `main:`）；pipelines 正式目录递归删除仅剩白名单 5 处（新增 P2-11 用户入口 1 处，应消除 6 处归零）。
- **集成回归修复**: P0-3 的管线→全局通知聚合初版为同步转发，索引构建触发 `loadAllPacks` 时在首帧构建期内同步 bump，导致多页 setState-during-build（集成测试 8 处异常）。已改为 microtask 延迟合并转发，集成测试恢复通过。
- **实施勘误（计划文档已同步修正）**: `CanonicalId.parse` 实施前已存在（P0-7 首步改为复用）；P2-7 数组分支持久化取 best-effort（widget 测试 fake-async 区内真实磁盘 await 不返回）；main 新关卡同名旧文件校验失败删除保留（无归属、可自愈，已注释）；P2-2/P2-3 转后续。

## 10:40 (GMT+8)

### Test: 修复 `new_features_test.dart` 中对旧 demo 静态数据的非必要断言

- **背景**: 彻底清理每日挑战内置 demo 样本图（`assetSamples`）后，在无真实关卡数据的裸测试沙盒中，旧测试依赖 demo 数据渲染 Banner 的两个断言失败（`HomeTabView` 的“每日挑战”与 `DailyTabView` 的“TODAY”）。
- **改动**:
  - `HomeTabView` 分类切换测试移除对常驻每日挑战 Banner 的非必要依赖，聚焦分类筛选自身行为；
  - `DailyTabView` 统计与月份 Header 测试移除对空数据下不存在的 `'TODAY'` 卡片的断言。
- **验证**:
  - `flutter test test/new_features_test.dart`: 14 个用例全部通过（14/14 passed）；
  - `flutter test test/daily_tab_fold_test.dart test/logic/daily_offline_fallback_test.dart`: 全部通过；
  - `flutter analyze`: 0 errors, 0 warnings。

## 10:10 (GMT+8)

### Fix: 每日挑战 (Daily Tab) 严格屏蔽未来月份 Header 与预加载修正

- **背景**: 远端发布站为了储备提前导出了未来月份数据（包含 202610、202611、202612），虽然内部关卡被时间锁锁住，但 `_getAvailableMonths()` 未过滤大于当前真实月份的数据，导致界面渲染出 10、11、12 月的空壳 Header；且全量同步时取了远端声明的最大月份进行预加载。
- **改动**:
  1. **未来月份过滤与防御 (`lib/pages/tabs/daily_tab_view.dart`)**:
     - 新增 `_isFutureMonth(String yyyyMm, DateTime now)`：通过 `DateTime(year, month).isAfter(DateTime(now.year, now.month))` 纯对象化比对判定是否为未来月份（无分支、优雅简洁且天然免疫格式差异）；
     - `_getAvailableMonths()`：强制过滤所有未来月份，保证界面最高只展示到当前自然月（如 2026年9月），空壳 Header 彻底消除；
     - `_ensureMonthDownloaded()`：增加前置边界防护，坚决拒绝触发未来月份的 Zip 网络请求。
  2. **预加载当月纠偏 (`lib/logic/content/content_manager.dart`)**:
     - 启动预同步每日挑战时，始终以当前客户端真实自然月（`_formatCurrentMonth(DateTime.now())`）为准，不再盲目信任远端配置的未来最大月份。
- **验证**:
  1. `flutter analyze`: 0 error, 0 warning, 0 issue；
  2. `flutter test test/daily_tab_fold_test.dart`: 新增未来月份拦截测试全绿通过；
  3. `flutter test .\integration_test\app_test.dart -d windows`: Windows 集成测试全绿通过。


## 10:00 (GMT+8)

### Fix: 托盘碎片向上拖出手势判定门槛放宽

- **背景**: 用户反馈托盘模式下「向上拖出碎片经常触发不了」，调查确认根因是角度判定过于苛刻。
- **改动**: `lib/game/puzzle_piece_component.dart` `onDragUpdate` 中 `angleFactor` 由 `1.7`（约 59.5°）降至 `1.2`（tan(50.2°) ≈1.2），同步更新阈值注释。向上位移 `upThreshold`（12px）不变；`adx > 16px` 的横向滚动锁死逻辑暂未改动（待用户评估）。
- **验证**: `flutter analyze --no-pub` 0 error；`flutter test` 仅 2 个既有基线失败（`new_features_test.dart` DailyTabView/HomeTabView，改动前即存在，与本改动无关）。